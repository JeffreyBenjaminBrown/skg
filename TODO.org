* git add for both sides of moved source
* Emacs, not the server, should remember foldedness and point.
Keep a tree of IDs.
If the ID that had point is lost,
destroy its spaces-in-the-text-of-that-node record,
and hand it to the nearest surviving ancestor.
* Split Vognode into
Vognode = Active | Inactive
Phantom = Diff | Deleted | Unknown

Promote Phantom to be one of the variants of ViewNode.
What's the difference between the three kinds of phantom?
* The AIs want jq, perl
* each new root view sohuld be drawn with a link sources ancestry
that is, linksource-ancestry as view-descendents
* more diff supplement data
containers before and after
heralds (alias action, etc.)
* must (?) stage them separately, but commit all repos at once
* command to reverse the [where I learned it] contains [it] link
One can then easily place the source under a branch named 'source'.
* the link stack should not be expected to be cleared
Someone can if they find a way to use it.
They might never use it.
It's cheap.
* text search across containment paths
* Bug ? Saving doesn't regenerate heralds if there were no changes?
* ONGOING [[./TODO/local-view-update/plan-critique.org]]
claude --resume afa9f264-86a1-475f-9fae-203731cd28a1
* u
** the diff report should
*** explain the maximum badness in each category
*** include empty top-level categories
so a reader can remember what those categories are,
and thus (e.g.) that nothing was orphaned.
** bugfix : strip last empty line from buffer on save
Or strip pure-whitespace bodies.
** bug : graphnodestats are not recalculated in collateral buffers
** goto in magit should work on raw IDs too
* [[./TODO/better-than-regex-in-emacs-tests.org]]
* suppressed container count rule for subscribees-as-such is wrong
It suppresses 1, should suppress 0.
Maybe simplify the rule: always suppress 1 and nothing else.
* ? replace ViewNode with (Vnode_Viewstate, Vnode)
where Vnode is the new name of what was ViewNodeKind.
* ParentIs, Birth and maybe other things are vestigial for Phantoms
* autoload elisp/skg-view-org-ancestry.el
* PAUSED [[./TODO/full-schema]]
codex resume 019e64c9-1ff6-75a3-91c5-2c54b644440b
* DONE ? [[./TODO/source-sets/]]
Seems to work.
* [[./TODO/incremental-init-might-be-broken.org]]
* return point to not just the same line but the same* char
  * or max within that line if too big)
* Need text search across paths.
* critical
** I can't demo without privacy mode.
The backlinks from skg itself contains unsafe references.
* AWAITS ? to be more sure of the git diff analysis
Have independent code count the number of disappeared files,
moved files, new roots, and other anomalies,
and check it against the report.
(Backgrounded.)
* AWAITS links in before contents
** graphstats should precede even the link source count
but follow parentIs and indef
** containsParent is a viewstat, not just an isParent value?
Seems good to use two heralds of the same shape, like "}}",
but one is the color of isParent and the other the color of viewStats.
* AWAITS speed up test suite
Consolidate redundant tests.
* ONGOING [[./TODO/birth-to-parent.org]]
* Each parentIs herald should be a different color.
and always first
* understand
** [[./rerender_view.org][understand rerender_view]]
** OOPS : meant to review this diff range
*** command
git diff 0668ddb6f78b2d678cde26389fa3d04cb6f98614..adf5603bdaf412c418e1e7a40853bb0bdc093941
*** hash interp
ending
  adf5603bdaf412c418e1e7a40853bb0bdc093941
starting
  0668ddb6f78b2d678cde26389fa3d04cb6f98614
*** if it's good
then the next item can be deleted as done,
"further subdivide the completion logic"
** TODO I want this todo list in Skg.
Git review after reshaping it is hell.
*** merge & export these notes
**** DONE "TODO" items in this repo
**** [[id:9c5619e5-81ad-4a67-9705-e4761bdd6839][skg]]
**** [[id:627da2c2-2f34-46ac-a6d3-9c625c4ff31d][AI]]
**** [[id:001d7913-c431-461c-92ae-a6a39394856c][processing]]
** use the .emacs in skg3
requires rebuilding NixOS
That was so that M-? would
not interpret comments as search hits.
* easy
** beep also once the db has rebuilt
** remap C-c C-o during Skg
in skg buffers
** show the herald it is responsible for next to each field in the metadata
** ? request containerward paths *excluding* the view-rootward one
** IDs should not be writeable in the metadata view
And what about extra-ids?
** Is it a misfeature to let config_path differ from data_root?
Why would you split up your skg stuff?
* bugs
** making a node multicontained does not update its container count
in the buffer it was copied from
** after a big edit to the CS for econ course notes, C-c t A took forever
I had created a big tree of new nodes.
** What's the second blue { in the herald for?
Saw it looking at 'address', common to Valentina and Maps.
** 'compute_interleaved_diff' can collide for large unique-item lists
WARNING: That is a verbatim heading title from TODO/problems.org
* document
** content is like "what is this idea looking at", and links sources are "what are looking at this idea"
** strategy for moving source
Commit all uncommitted changes from both sources.
Do the move.
Compare the state of both sources.
* maybe
** a way to present more nodes on one line
Just triple the relationship character and color it differently.
Emacs could quickly toggle bbetween these two representations.
** maybe a symbol for 'on screen definitive elsewhere' for links
to allow recording
multiple trails from a root to its content
** unify the three skg instances
See TODO/docker-unification-strategies.org
* using AI
** I liked this prompt at the top of a vision document.
Please read and think about this.
Report anything unclear, inelegant, inefficient, or awkward about it,
in the following section, 'problems'.
Subdivide your questions and other feedback org-style
(remember, the tree does not have to be uniform depth!)
and I will respond inline.
Please don't perform any git write ops for now, but read all you want.
** INTERNALIZE: Use lightweight models for easy but wordy things.
Like fixing interior imports.
* references
** how to import from org-roam
*** automatically
./bash/import-org-roam.sh public-copy/ data/public/ public
*** manually
**** remove the .skg files and .git
**** cargo run --bin skg -- import-org-roam public/ data/public/ public
**** git init & initial commit
initial import from org-roam
**** restart the server
so it has time to ingest the data
** DONE ? not using lately
*** [[./next.org]] might be in progress
*** For minimalism, remember [[id:b6747d36-711b-42e7-aa80-b9b2be53fe6c][my Skg priorities]].
*** commit history
top is latest
**** UNREVIEWED : the Rust-v-Typedb audit code
ad9b84c8ad476b93b911c0513107ab70df15c449..34b23094fc839224dc6accdbffcd4adf58eebc51
**** 'many-and-better-node-types' : some terminus commits
***** plan began
d4dd2e850b449aa4ff92820f9dd66aad48cd6b0b
***** plan part A complete
656d66e7ea52a070ec23e26e7cc54b10364ae6a8
***** plan ended
f6664360241ec9de0c89a0934445eeb37d147a87
**** TODO review commit 7dacceadcee6ea260ae19625bc8e7da1c77986d1
which was to fix test db accumulation
* food for thought
** Skg projects graph relationships into trees.
** ? I wanted the 'helps' relation.
And 'needs', surely.
** Where to use overlapping containment?
** maybe : how to use AI
*** Maybe ask for criticism *before*
before asking to be asked for missing details.
** DONE ?
*** Have Claude report bugs.
*** an important constraint on any graph serialization format
Deleting a node file should make sense.
*** Report bugs about Claude.
**** whitespace for distinct org domains: illegible
It doesn't understand nested one org passage quoted in another org document,
if the quotation is represented as leading whitespace.
*** if needed, convert to haskell
  If it becomes a mess, Haskell is easier to refactor.
  Rust could sit between TypeDB and Haskell.
* maybe good but not sure why
* goals, by highest aim
** TODO how|what to dogfood
*** spots to patrol
**** git : view changes to
***** a source move
***** merge
***** delete
***** aliases
**** move something to a different repo
**** merge, delete
**** multi-contain
**** views : id stack, metadata, diff, heralds
*** It is valid to dogfood even as only reading.
*** internalized
**** Dogfood and develop in separate places on disk.
That way I can keep investigating the latest usable commit
while a new one is being developed.
*** why to do a complete patrol
  If a neighborhood of implemented feature space
  is left unused|unexplored,
  that neighborhood becomes more likely to
  become ensnared in structural chamges
  that would have been thought better of in the neighborhood's light.
** TODO easily worth it now
*** dired should offer skg-goto shortcuts
*** skg-move-node (and -recursive)
*** skg-merge-here
Should prompt for the acquiree node (ID or link).
** TODO legibility to user
*** independent of parent /= contained in nothing
so decouple these two heralds
*** ? Have only be one definitive view across all buffers.
**** why
When going from magit-view to a modified node,
it's very nice if that one jump goes directly to
the one big context where it and its brothers were made.
**** how
See TODO/one-definitive-view/
*** parentContains (deleted) does seem like valid graph data
**** Neither seems critical.
**** History
It was part of the graphnodestats, until just after commit
2e02c90013f17459f8251f32d1564b69ca36bda2
**** debate
and interesting enough to be heralded,
and reasonably separate from 'birth'
(although birth might imply it sometimes).
But maybe 'anAncestorContains' would be more useful.
*** ? Warn user if they create a containment cycle.
** TODO legibility to coder
*** ONGOING fix lying function names
see lying-names.org
*** ONGOING read the code
Using
command-tree-to-read-from.org
*** "incremental init" might be garbage
see TODO/what-affects-dbs.org
but that was before some tremendous improvements in performance
on <2026-04-22 Wed> and before and maybe after
*** eliminate some 'tracing' commands
  Anything that's blazing fast can be safely removed.
*** let user promote and demote a node in another's backlinks
  Whichever means explicitly labeling fewer link sources.
  e.g. relationship nodes like 'instances'
  should promote a few nodes that are actually about the node
  'instances'
  vs. a lot that use it as a relationship label
** speed
*** in reading backward
**** ? "any rel" ought to be what a single backward view shows
link sources, containers, subscribers
(but not overriders or hiders -- those should be more obscure)
**** absorb a link source org-child into a herald
if it has text identical to the target's,
i.e. is just a link to the target with the target's title as label
*** in making links
If there is exactly one node in the search results
that exactly matches each (possibly regex)
term in the search query, go straight there.
*** in git
**** TODO commit eval
Can ask for it for staged, unstaged or all changes.

If a node is new, it's fine as long as it's valid.
(What are the validation rules?
Some relationships enable/preclude others.)

If a node was removed, and all references to it removed,
it is reported deleted and listed.

Anything orphaned by a deletion is reported,
with the corresponding deletion.
Anything whose container(s) changed is reported as such.

A new content is fine if _.

[New|deleted] [rel type] is fine if _.

Changes to text are reported.

To test, compute this two ways: From disk and from dbs.
**** TODO be able to visit ghost nodes
**** view all changes in one buffer
**** theoretically faster : auto-stage some nodes for code
***** not sure much of the user experience is here
***** the idea
****** or mark in a way that identifies node as probably stageable
If a new or deleted node, it (is still in the graph and) has a parent.
*** shortcuts
**** Goto could (also?) be C-C RET.*
* privacy
*** TODO Privacy: Permit user to hide certain sources from view.
**** Figure out the diff logic for insertions when some are hidden.
I think I see how: Just pin each thing to its last visible sibling.
** convenience | comfort
*** importing org-roam should be possible while server is running
and automatically update the other dbs (not just disk as now)
*** saving from a narrowed buffer
When the buffer is narrowed, if the user saves, skg should walk backward from the first character until it finds the start of a headline. It's likely the first character *is* the start of a headline, but maybe not -- the user might be lookoing, e.g., at a proper subset of a body.

Similarly, on the other side, skg should search forward until it finds the first character of a new headline (or the end of the buffer), and truncate right before that character (or including it, if it's the end of the buffer). Again it's likely that the very next character is the start of a new headline, but not neecessarily.

Then skg can just restore the narrowed window so that it starts and ends at those two points. This way, even if things change outside the context, it will know exactly where to start and end.

This isn't perfect, because there might be multiple headlines with the same IDs. If there are, screw it, skg should just report that duplicative IDs are interfering with its ability to save while narrowed, and let the user worry about it.
*** having to start and stop the server is annoying
*** some skg-request-* functions save, others don't
*** ? trying to delete an indef node should warn, not err
That is, the save should still succeed.
* present view of node's root(like) containerward ancestors
** Hopefully a root, otherwise a cycle; links are ignored.
** Useful when checking changes
   "This node changed; show it to me in context."
   e.g. with git
* dogfooding
** commands to probe better
*** TODO C-c g m : skg-view-magit
    Opens a magit view of the changes to the file.
*** TODO C-c v d : skg-view-diff-mode
    If you use git to manage your data,
    enable this view to see what you've changed since your last commit,
    i.e. how your worktree differs from HEAD.
*** TODO C-c k a : skg-local-aliases
    See what other text you could search for to find this node.
    You can always search for its title.
    Most nodes have a title and no aliases.
*** TODO skg-reveal-stats
    Computes the containerward path stats for a node
    (path length, fork count, whether it cycles)
    and saves. The stats then appear in the node's metadata
    and in the heralds view.
** things to try
*** look at my import collisions
    rg Was_Overloaded
*** git diff view
** I may have damaged my data.
   I think I did. Searching for 'motivation' used,
   I'm sure, to return a hit with exactly that title.
* [[./docs/en-espanol.org][En Español]]
* deciding whether a node is a relationship is up to the user
* generalize it : a node is a map from one to many
* figure out how to work in parallel with Claude
  Need two copies of the repo.
  Claude advances features while I review older code.
  When I commit, Claude pulls in those changes,
  and fixes any merges that break.
* internalizing
** It is legitimate to commit before testing.
   Even AI-generated code.
   Just mark it so and test it in a later commit.
   The later test will help straighten out the code
   so that it is easier to review thereafter.
** don't erase discussion in plan until it is settled
** move 'merge' into the type containing 'save' and 'delete'.
** identify relevant extant tooling when asking for a given task
   Even if vague, like
   'this code includes utilities for doing things in trees'.
** If my request led it to make a function that addresses one case among the types badly, I likely screwed up.
   This happened when I asked AI to merge
** Don't refactor something without reading its comments.
   Even if it's as 'simple' as a type --
   non-obvious decisions often go into those brief definitions.
** Awkward or good ? Exactly some OrgNodes can have diffs.
   That makes some invalid state unrepresentable.
** how to prompt
*** when asking for a refactor
**** Name _v2 functions consistently.
**** Duplicate comments from _v1 functions
     (which keep their original names)
     into the _v2 functions.
*** when asking to move code
    Emphasize leaving whitespace, formatting unchanged.
** Don't reload a buffer without checking.
   If a buffer is empty when I don't think it should be,
   don't reload it. Check on disk if it is in fact empty.
   If not, try hitting 'undo' to restore it.
** Develop a cleanup routine?
*** for local changes
**** Add type signatures to everything touched.
**** Update comments for anything that was touched.
     Including the header comment for any file, if needed.
*** for the whole codebase
**** Add type signatures.
**** Update list of user-facing functions in elisp/*.el.
** Any place I can separate concerns by re-traversing the tree, do it.
* later
** 'timed' and 'timed_async' are noisy
   Maybe, rather than using them at the function call,
   use them at the function's definition.
** Can I download my git repo and read it ony phone with goto definition, find uses, etc?
** 'content' count is wrong in Subsribees if any are hidden
** add 'numSubscribees' near 'numLinksIn'
** Is it natural that skg-request-save-buffer issues so many distinct calls to process-send-string?
* internalize
** ask AI to research and present options before implementing
*** Clear its context before doing that.
*** Includes even asking for refactoring plan.
*** why : helps me
*** why : helps the AI
    Loads up its context with good stuff,
    for when it finally implements the chosen path.
** cargo nextest run
** cargo test --doc
   runs 'doctests', which ensure code in documentation works.
** claude --dangerously-skip-permissions
   Less ergonomically, can say:
   "Don't do anything you don't have permission to do.
   I'm going to be away so I can't grant it."
* QA
** add tests
*** collect_generation_ids
    test the new effective root logic
** This seems like it could have been more efficient.
   server/to_org/definitive_branch.rs
   sharing more code with
   server/to_org/initial_view_bfs.rs
** Appending from indef nodes moght be a misfeature.
** completeContents seems too long
** Birth could be generalized as RelToAncestor.
   It would need both a relation and a number of levels.
   (Birth would translate to 1 level; to grandparent, 2; etc.)
** nah
*** 'merge' should be encodeable in NodeSaveAction
    Then I wouldn't need to parse the action twice,
    in two different ways.
** test validate_merge_requests
** in elisp/skg-sexpr/skg-lens.el, rather than concatenate with ":"
   (that's done by skg--tokens->string)

   Instead of returning a :-separated string,
   return a list of lists of propertized strings,
   which the calling function can decide how to concatenate.
** Indefinitive nodes skip the visited-checking logic
   when saving a buffer.
   This is not clearly a problem.
** Maybe indefinitive nodes and repeated nodes should be merged.
** these integration tests could be merged
focus-and-folded-markers
fold-preservation
** redundancies
*** linecol_to_orgnode from save/buffer_to_orgnodes/uninterpreted.rs
    does something already done in mk_org_text/
*** Buffer_Cannot_Be_Saved and SaveError
** use the tests at not_using/pids_from_ids/tests.rs
** test the propagation of Buffer_Cannot_Be_Saved errors
   in commit 130318842f5ad0ad92279d14f85d8b950e6f844a,
   I started using
     find_buffer_errors_for_saving,
   which covers more cases than what I had been using,
     find_inconsistent_instructions
   But the tests only cover what
     find_inconsistent_instructions
   might return.
** integration test for containerward view
** not pressing
*** use s-exp parsing
**** in server/serve/containerward_view.rs
     fn extract_containerward_view_params
**** in server/serve/node_aliases.rs
     fn extract_node_aliases_params
**** in server/serve/util.rs
***** definitely
      fn request_type_from_request (
      fn node_id_from_single_root_view_request (
      fn search_terms_from_request (
***** and maybe
      fn extract_quoted_value_from_sexp (
*** Avoid uses of `unwrap` in Rust.
*** find 'panic's, replace with Result.
*** Use anyhow or eyre crates for better error handling
**** Cargo.toml
  anyhow = "1.0"
**** usage
  use anyhow::Context;

  let driver = TypeDBDriver::new(...)
      .await
      .context("Failed to connect to TypeDB server")?;
*** once it's possible, make a good pids_from_ids
    see [[./not_using/pids_from_ids]]
** dedup some code
*** is_ancestor duplicates something done a different way elsewhere
* ? lessons learned
** Validate ancestry, not descendents.
   For instance, "an alias should have an aliascol parent"
   is easier to say than "an aliascol's children can only be aliases".
   (Because it prevents combinatorial explosion?)
** Sourceward paths could have been easier.
   Just let the user add all sources from one node at a time.
   Linear paths seem unlikely backward anyway.
* document
** that filenames must correspond to PIDs
** the dangers of repeated nodes to the user
   The original data model was that each node would have only one container. That proved infeasible, because the user can copy data at will. So skg accepts such data. But bear in mind that it is dangerous. The danger is this: If a node has branches, and is copied somewhere earlier in the same document, then that new copy will take precedence. Edits to it will be treated as *the* edits. If all you did was copy the node but not its branches, its branches will be lost when you save.
** find where to put this comment
 // Titles can include textlinks,
 // but can be searched for as if each textlink
 // was equal to its label, thanks to replace_each_link_with_its_label.
** change graph -> web
** Didactically, concept maps > knowledge graph.
** [[../docs/data-model.md][The data model]] and [[../docs/sharing-model.md][The sharing model]] overlap
   as documents.
** "point" seems a better name than "node"
   But it conflicts with the Emacs 'point'.
* solutions
** search for type-oid
*** solution
    regex :
      (struct|enum|type)\s+.*TYPE_NAME
      (struct|enum|type|trait|union)\s+.*TYPE_NAME
    \s+ handles whitespace.
    To avoid matching superstrings, follow type name with \b
      \b is a word boundary to avoid matching SourceDiffFoo.
*** problem statement
    How would I search for the regex 'struct or enum or type followed by the name of it'? To find a typedef where I know the thing's name but not whether it's a sum type, product type or alias.
** reduce 4-space indentation to 2-space indentation
   sed -i 's/^\( *\)\1/\1/' THE_FILE
** to extract Emacs properties into Rust
   use [[~/hodal/emacs/property-dump.el][property-dump]]
** If I need to graft a forest's trees onto a false root,
   rctree can
   egotree can't
* dubious
** fold body into false headline
   there in Emacs but (at worst ignored) when Rust rebuilds
* if anyone else is reading this
** Most plans are not here but online.
   e.g. here:
   github.com/JeffreyBenjaminBrown/public_notes_with_github-navigable_links/blob/main/shareable_knowledge_gardens.org
   This document is mostly things I considered highest-priority.
