* semi-permanent
** [[./next.org]] might be in progress
** import org-roam
*** to run it
cargo run --bin skg -- import-org-roam public/ data/public/ public
** commit message -- with blank to fill
begin

  imported from public org-roam notes:
    git@github.com:JeffreyBenjaminBrown/public-notes-in-org-roam-format
  as of commit
    [FILL IN THIS DETAIL]
** when dogfooding
*** spots to patrol
**** make edits
***** move something to a different repo
***** write
***** move
***** multi-contain
**** commit to git
**** ? read git history
**** DONE visit things
**** DONE request view changes
*** It is valid to dogfood even as only reading.
* TODO bugs
** C-<return> from a root gives an error message
*** I want it to create a new root and prompt for source.
*** I get this error message.
Debugger entered--Lisp error: (error "sexp-to-org: input is not paren-terse: (skg (node))")
  error("sexp-to-org: input is not paren-terse: %S" (skg (node)))
  (if (sexp-paren-terse-p sexp) nil (error "sexp-to-org: input is not paren-terse: %S" sexp))
  sexp-to-org((skg (node)))
  (let ((sexp-as-org (sexp-to-org sexp))) (if is-truenode (skg-truenode-expand-defaults-in-org sexp-as-org default-source) sexp-as-org))
  (let* ((sexp (read metadata-str)) (is-truenode (skg-truenode-sexp-p sexp)) (sexp-start (if no-metadata (car positions) (+ (line-beginning-position) (length (car split))))) (sexp-end (if no-metadata (cdr positions) (+ sexp-start (length metadata-str)))) (default-source (if no-metadata (progn (skg-sexp-edit--default-source)))) (org-text (let ((sexp-as-org (sexp-to-org sexp))) (if is-truenode (skg-truenode-expand-defaults-in-org sexp-as-org default-source) sexp-as-org)))) (skg-sexp-edit--open-edit-buffer org-text source-buffer sexp-start sexp-end is-truenode) (if no-metadata (progn (skg-sexp-edit--goto-field-value "source") (message "Choose a source."))))
  (let* ((metadata-str (car (cdr split))) (no-metadata (string-empty-p metadata-str)) (positions (if no-metadata (progn (skg-sexp-edit--insert-default-metadata)))) (source-buffer (current-buffer))) (if no-metadata (progn (setq metadata-str "(skg (node))"))) (let* ((sexp (read metadata-str)) (is-truenode (skg-truenode-sexp-p sexp)) (sexp-start (if no-metadata (car positions) (+ (line-beginning-position) (length (car split))))) (sexp-end (if no-metadata (cdr positions) (+ sexp-start (length metadata-str)))) (default-source (if no-metadata (progn (skg-sexp-edit--default-source)))) (org-text (let ((sexp-as-org (sexp-to-org sexp))) (if is-truenode (skg-truenode-expand-defaults-in-org sexp-as-org default-source) sexp-as-org)))) (skg-sexp-edit--open-edit-buffer org-text source-buffer sexp-start sexp-end is-truenode) (if no-metadata (progn (skg-sexp-edit--goto-field-value "source") (message "Choose a source.")))))
  (let* ((headline (skg-get-current-headline-text)) (split (skg-split-as-stars-metadata-title headline))) (if split nil (user-error "Not on a headline")) (let* ((metadata-str (car (cdr split))) (no-metadata (string-empty-p metadata-str)) (positions (if no-metadata (progn (skg-sexp-edit--insert-default-metadata)))) (source-buffer (current-buffer))) (if no-metadata (progn (setq metadata-str "(skg (node))"))) (let* ((sexp (read metadata-str)) (is-truenode (skg-truenode-sexp-p sexp)) (sexp-start (if no-metadata (car positions) (+ (line-beginning-position) (length ...)))) (sexp-end (if no-metadata (cdr positions) (+ sexp-start (length metadata-str)))) (default-source (if no-metadata (progn (skg-sexp-edit--default-source)))) (org-text (let ((sexp-as-org ...)) (if is-truenode (skg-truenode-expand-defaults-in-org sexp-as-org default-source) sexp-as-org)))) (skg-sexp-edit--open-edit-buffer org-text source-buffer sexp-start sexp-end is-truenode) (if no-metadata (progn (skg-sexp-edit--goto-field-value "source") (message "Choose a source."))))))
  skg-edit-metadata()
  (progn (skg-edit-metadata))
  (if (and (and (boundp 'skg-content-view-mode) skg-content-view-mode) (org-at-heading-p) (= (org-outline-level) 1)) (progn (skg-edit-metadata)))
  skg-sexp-edit--after-insert-heading()
  apply(skg-sexp-edit--after-insert-heading nil)
  org-insert-heading-respect-content()
  funcall-interactively(org-insert-heading-respect-content)
  command-execute(org-insert-heading-respect-content)
*** and in the buffer I get a new top-level headline "* (skg (node))"
** Containerward stats are missing from link sources.
* TODO slow things
** save took forever after changing a node from public to private.
And this is repeatable,
and happens in the reverse direction too,
and regardless of git diff view on or off.
** requesting metadata edit view took forever
*** when it happened
I was in diff view mode, and requested it for this id:

  a4b10f33-94bb-4368-a011-577607b1e4c7

from this buffer:

  (I have replaced asterisks with hash marks, to distinguish this buffer from the org document it is quoted within.)

  # (skg (node (id c71a2ad0-4a34-44d2-b344-a75c124b5111) (source public) (graphStats (contents 3) (containsHerald {3) (linksInFromLeaves 2) (linksHerald →2)) (viewStats (sourceHerald ⌂:public)))) power from nothingness / The list of things you don't have to worry about is impossibly too long to record, know.
  ## (skg (node (id 0acfbfef-eea6-4ba9-9ccf-2515a460796c) (source public) (graphStats (linksInFromLeaves 1) (linksHerald →1)))) truth vs. lies
  ## (skg (node (id 69664107-ffdb-470d-bc91-367d2dea29b4) (source public))) Neo dodging bullets, easy bad guys, hundred-hand slap.
  ## (skg (node (id a4b10f33-94bb-4368-a011-577607b1e4c7) (source public) (diff new))) There is so much existence in the answer to what is impossible.
*** the second time, it was fast
** diff view updates should be backgrounded for the other buffers
Lock each separately to edits until updated.
* TODO shortcuts
** C-c v v not working from magit
* TODO new features
** skg-new-empty-view should permit cycling
** should go to new nodes, not just modified nodes
when switching to the magit view with C-c v m
** heralds
*** if node is a top-level headline in the buffer, different herald rules
We must see how many containers it has, even if only 1.
*** New source should be first herald and a different color, as should relation to parent and reason for existence when unusual (e.g. containerward path).
This information should be a different color from the graph stats.
*** If heritable in the view tree, source does not have to be restated when something is unconnected to its parent.
That is, if a is public, and ignores its child b which is public,
b does not need to restate 'public'.
** There should only be one definitive view across all buffers.
And they should all be indexed for C-c v v to jump to,
not just the roots.
** Hide sources.
*** Figure out the diff logic for insertions when some are hidden.
I think I see how, just pin each thing to its last visible sibling.
** buffer list
*** need longer name field in buffer view
*** Use novel characters as skg buffer prefixes.
And|or make Skg a major mode,
so Skg buffers are distinguishable in the buffer view.
This would permit stripping the leading "skg: " from buffer names.
**** maybe > and >? for normal and search buffers
** (Again?) Want link sources to show containerward ancestries.
** The number of contents herald is less interesting.
** server can't verify connection initially
* DONE bug: can't visit magit
These headlines form a story, somewhat sequentially.
They don't make sense out of context of the others.
** When I run C-c v m
** with point on the first line
** of this buffer
(The following is org-formatted, but have made the asterisks into hashes
to distinguish it from the org document that contains it.
That helped Claude read it.)

    # (skg (node (id 90c32efc-92c8-490f-afb3-50342e6d616c) (source public) (graphStats (containers 0) (contents 9) (containsHerald 0{9) (linksInFromLeaves 3) (linksHerald →3)) (viewStats (sourceHerald ⌂:public)))) how to form a study group
    ## (skg (node (id 4530e8f7-ad3f-4206-8262-476633a8b168) (source public))) SOURCE : Largely asking ChatGPT
    to sum up the research.
    ## (skg (node (id 5b4522d9-91f6-4673-8926-593801f1936f) (source public) (graphStats (contents 7) (containsHerald {7)))) Self-selected groups (tend to)
    ### (skg (node (id 3f0c73fa-0639-4e54-87c7-cdad42081779) (source public))) enjoy the experience more.
    ### (skg (node (id cf580964-78e3-4754-bd21-0b64b12327ac) (source public))) underperform the alternative.
    "Instructor-assigned or algorithmic groups (mixed skill, random, or balanced by GPA, gender, etc.)"
    The best-performing groups were "thoughtfully balanced", rather than random, but even random outperformed self-selection.
    ### (skg (node (id 49832d0d-dc95-4929-8f0a-f845f6e2ad70) (source public))) early on, have more comfort, cohesion and communication.
    ### (skg (node (id 70e30cca-b218-4380-ac50-a73320e9575d) (source public))) lower initial coordination cost.
    ### (skg (node (id 18475cc2-db9d-44ca-a3de-3d0550a9e6be) (source public))) plateau earlier.
    ### (skg (node (id 297df3a1-3c26-4b2d-8e6c-0db31e700f44) (source public))) have less diverse ideas and skills.
    It wasn't clear that they meant only "friends or familiar peers"
    or whether that was just an illustration.
    ### (skg (node (id a25ca1dc-3643-4949-b020-30ba4d7de8d0) (source public))) avoid conflict and role differentiation.
    ## (skg (node (id 2759a806-afd0-4631-82dd-cec8debe41ac) (source public))) "Explicit discussion of roles early on improves outcomes regardless of formation method."
    ## (skg (node (id ce905ec2-8ad7-43f4-8a6c-fb93f0ec0a0a) (source public))) With time group cooperation improves.
    ## (skg (node (id 77a2bda5-93e8-475e-ae4e-163d28214e85) (source public))) overlapping interests
    ## (skg (node (id b0729ba1-6eb3-4172-9273-2ba35187ce91) (source public) (graphStats (contents 4) (containsHerald {4)))) specialization
    ### (skg (node (id 50711408-599a-4002-9052-c4d2711803ae) (source public) (graphStats (contents 2) (containsHerald {2)))) kinds
    #### (skg (node (id 52cfa1ef-f61f-4297-a4ea-05f27ac09402) (source public))) in subject area
    #### (skg (node (id b4265b00-ccec-41cc-bffe-15c6b33f7039) (source public))) in role
    alias: some roles

    editor
    explainer
    investigator
    manager
    note-taker
    organizer
    question-generator
    reconciler
    recorder-reporter
    reviewer
    social integrator
    strategy evaluator
    summarizer
    timekeeper
    ### (skg (node (id c7e26515-6ad9-471b-881b-1001ee762ffb) (source public))) Roles are better at least someowhat fluid.
    Indeed formally rotating roles can work well.
    ### (skg (node (id 1d82985b-2e98-498f-be21-dc5ddba90c11) (source public))) It can emerge without being named.
    ### (skg (node (id 8c3d5ecf-6f3d-4fee-ad3f-44e55c75692c) (source public))) Subject area expertise is more helpful if known.
    If everyone knows you can ask X about T,
      that makes T's specialization in X more useful.
    ## (skg (node (id 4f359057-3de0-402b-b295-e09aef9a1a5f) (source public) (graphStats (contents 2) (containsHerald {2)))) problems
    ### (skg (node (id 50dbce86-1334-4c06-9efe-d71b056406f0) (source public))) "diffusion of responsibility"
    ### (skg (node (id 70052f0f-a043-456f-b20f-6b7184a1b02c) (source public))) "coordination overhead"
    ## (skg (node (id 879e111d-0b67-463c-9427-e1b455dcb16d) (source public))) Some tasks have more potential for specialization.
    complementarity
    ## (skg (node (id 907528a5-7ab6-4bc1-a3d6-7dd8cfa805c1) (source public))) Over-specialization feels narrow-minded.
** I get this error
Debugger entered--Lisp error: (scan-error "Unbalanced parentheses" 3 3695)
  forward-sexp-default-function(1)
  forward-sexp(1)
  (let ((start (point))) (forward-sexp 1) (if (> (point) line-end) (progn (error "Sexp does not end on this line"))) (read (buffer-substring-no-properties start (point))))
  (progn (forward-char (- (or 1 1))) (let ((start (point))) (forward-sexp 1) (if (> (point) line-end) (progn (error "Sexp does not end on this line"))) (read (buffer-substring-no-properties start (point)))))
  (if (search-forward "(" line-end t) (progn (forward-char (- (or 1 1))) (let ((start (point))) (forward-sexp 1) (if (> (point) line-end) (progn (error "Sexp does not end on this line"))) (read (buffer-substring-no-properties start (point))))))
  (let ((line-end (line-end-position))) (if (search-forward "(" line-end t) (progn (forward-char (- (or 1 1))) (let ((start (point))) (forward-sexp 1) (if (> (point) line-end) (progn (error "Sexp does not end on this line"))) (read (buffer-substring-no-properties start (point)))))))
  skg-first-sexpr-on-line()
  (let ((sexp (skg-first-sexpr-on-line))) (if (not sexp) (message "No metadata sexp found on this line.") (if (not (skg--metadata-sexp-contains-id-p sexp)) (message "Not a truenode line (no id in metadata).") (let ((id (skg--extract-id-from-metadata-sexp sexp)) (source (skg--extract-source-from-metadata-sexp sexp))) (if (not (and id source)) (message "Could not extract id or source from metadata.") (skg-register-response-handler 'get-file-path #'skg--file-diff-result t) (skg-lp-reset) (let* ((tcp-proc ...) (request-sexp ...)) (process-send-string tcp-proc request-sexp)))))))
  (save-excursion (beginning-of-line) (let ((sexp (skg-first-sexpr-on-line))) (if (not sexp) (message "No metadata sexp found on this line.") (if (not (skg--metadata-sexp-contains-id-p sexp)) (message "Not a truenode line (no id in metadata).") (let ((id (skg--extract-id-from-metadata-sexp sexp)) (source (skg--extract-source-from-metadata-sexp sexp))) (if (not (and id source)) (message "Could not extract id or source from metadata.") (skg-register-response-handler 'get-file-path #'skg--file-diff-result t) (skg-lp-reset) (let* (... ...) (process-send-string tcp-proc request-sexp))))))))
  skg-view-magit()
  funcall-interactively(skg-view-magit)
  command-execute(skg-view-magit)
** If I instead try when point is on the second line, it works.
** Third line: fails.
** Fourth and fifth lines: works.
* TODO review commit 7dacceadcee6ea260ae19625bc8e7da1c77986d1
which was to fix test db accumulation
* ? containerwardPathCol, sourceCol
** ? distinguish those from the other 'skg-local-*' commands?
** labels
*** containerwardPathCol would say "is recursively contained in these descendents"
*** sourceCol would say "is targeted by links from these children"
**** discarding
each of which is recursively contained in its descendents"
*** two varieties of source children depicted
- a root flag with source
- a containerwardPathCol
* Bug ? forward-sexp chokes on {
skg-find-sexp-end (line 62 in this same file) counts only () and works fine.
So Skg developers have to know to use Skg-specific sexp navigation.
But maybe that's appropriate?
* Have Claude report bugs.
* Report bugs about Claude.
** whitespace for distinct org domains: illegible
It doesn't understand nested one org passage quoted in another org document,
if the quotation is represented as leading whitespace.
* Dogfood and develop in separate places on disk.
That way I can keep investigating the latest usable commit
while a new one is being developed.
* Does the first visit take a lot longer?
* ONGOING I am committing to it in my personal data,
linking to things only skg has.
* etags should ignore **/abandoned
* ? "any rel" ought to be what a single backward view shows
  link sources, containers, subscribers
    (but not overriders or hiders -- those should be more obscure)
* eliminate some 'tracing' commands
  Anything that's blazing fast can be safely removed.
* if needed, convert to haskell
  If it becomes a mess, Haskell is easier to refactor.
  Rust could sit between TypeDB and Haskell.
* why to do a complete patrol when dogfooding
  If a neighborhood of implemented feature space is left unused|unexplored,
  it becomes more likely to embark on structural chamges
  that would have been thought better of in the neighborhood's light.
* move data to another source
* TODO get containerward view within link sourceward view
  for every link that is not a root
* promote and demote in backlinks
  Whichever means explicitly labeling fewer link sources.
  e.g. relationship nodes like 'instances'
  should promote a few nodes that are actually about the node
  'instances'
  vs. a lot that use it as a relationship label
* What does skg-show-stats do?
* TODO Warn user if they create a containment cycle.
* present view of node's root(like) containerward ancestors
** Hopefully a root, otherwise a cycle; links are ignored.
** Useful when checking changes
   "This node changed; show it to me in context."
   e.g. with git
* 'C-c C-o' ought to be the skg-visit-node shortcut.
  To match what it does (follow a link) in org-mode.
* dogfooding
** commands to probe better
*** TODO C-c g m : skg-view-magit
    Opens a magit view of the changes to the file.
*** TODO C-c v d : skg-view-diff-mode
    If you use git to manage your data,
    enable this view to see what you've changed since your last commit,
    i.e. how your worktree differs from HEAD.
*** TODO C-c k a : skg-local-aliases
    See what other text you could search for to find this node.
    You can always search for its title.
    Most nodes have a title and no aliases.
*** TODO skg-show-stats
    Computes the containerward path stats for a node
    (path length, fork count, whether it cycles)
    and saves. The stats then appear in the node's metadata
    and in the heralds view.
** things to try
*** look at my import collisions
    rg Was_Overloaded
*** git diff view
** I may have damaged my data.
   I think I did. Searching for 'motivation' used,
   I'm sure, to return a hit with exactly that title.
** I wanted immediately upon trying it.
*** delete in view-descendents all parent-ignores branches
*** do until all of node's children
** Branching containerward is slow.
* magit bug!
I just copoied this text from magit:

  ```elisp
  -(progn
  -  (load-file "../elisp/skg-init.el")
  -  (skg-client-init "../data/skgconfig.toml"))
  +  (progn                                                                   (load-file "../elisp/skg-reload.el")
  +    (skg-reload)                                                           (skg-client-init "../data/skgconfig.toml"))
   ```
But when I look at it in magit,
I don't see any of the lines without a + or - in front.
What's going on?
* [[./docs/en-espanol.org][En Español]]
* deciding whether a node is a relationship is up to the user
* generalize it : a node is a map from one to many
* nodes as relationships ? solved
  split links in 2, containers and non-containers
* These should be unavailable to the user.
  skg-request-title-matches
  skg-request-single-root-content-view-from-id
  skg-request-save-buffer
  skg-validate-id-stack-buffer
  skg-edit-sexp-at-or-after-point
  skg-show-indefinitive
  heralds-minor-mode
* review, comment the new context-generation code
* to reload skg in Emacs
  (load-file "~/hodal/skg/elisp/skg-init.el")
* some skg-request-* functions save, others don't
* figure out how to work in parallel with Claude
  Need two copies of the repo.
  Claude advances features while I review older code.
  When I commit, Claude pulls in those changes,
  and fixes any merges that break.
* TODO merge & export these notes
** DONE "TODO" items in this repo
** [[id:9c5619e5-81ad-4a67-9705-e4761bdd6839][skg]]
** [[id:627da2c2-2f34-46ac-a6d3-9c625c4ff31d][AI]]
** [[id:001d7913-c431-461c-92ae-a6a39394856c][processing]]
* internalizing
** It is legitimate to commit before testing.
   Even AI-generated code.
   Just mark it so and test it in a later commit.
   The later test will help straighten out the code
   so that it is easier to review thereafter.
** don't erase discussion in plan until it is settled
** move 'merge' into the type containing 'save' and 'delete'.
** identify relevant extant tooling when asking for a given task
   Even if vague, like
   'this code includes utilities for doing things in trees'.
** If my request led it to make a function that addresses one case among the types badly, I likely screwed up.
   This happened when I asked AI to merge
** Don't refactor something without reading its comments.
   Even if it's as 'simple' as a type --
   non-obvious decisions often go into those brief definitions.
** Awkward or good ? Exactly some OrgNodes can have diffs.
   That makes some invalid state unrepresentable.
** how to prompt
*** when asking for a refactor
**** Name _v2 functions consistently.
**** Duplicate comments from _v1 functions
     (which keep their original names)
     into the _v2 functions.
*** when asking to move code
    Emphasize leaving whitespace, formatting unchanged.
** Don't reload a buffer without checking.
   If a buffer is empty when I don't think it should be,
   don't reload it. Check on disk if it is in fact empty.
   If not, try hitting 'undo' to restore it.
** Develop a cleanup routine?
*** for local changes
**** Add type signatures to everything touched.
**** Update comments for anything that was touched.
     Including the header comment for any file, if needed.
*** for the whole codebase
**** Add type signatures.
**** Update list of user-facing functions in elisp/*.el.
** Any place I can separate concerns by re-traversing the tree, do it.
* later
** 'timed' and 'timed_async' are noisy
   Maybe, rather than using them at the function call,
   use them at the function's definition.
** Can I download my git repo and read it ony phone with goto definition, find uses, etc?
** 'content' count is wrong in Subsribees if any are hidden
** add 'numSubscribees' near 'numLinksIn'
** Is it natural that skg-request-save-buffer issues so many distinct calls to process-send-string?
* internalize
** ask AI to research and present options before implementing
*** Clear its context before doing that.
*** Includes even asking for refactoring plan.
*** why : helps me
*** why : helps the AI
    Loads up its context with good stuff,
    for when it finally implements the chosen path.
** cargo nextest run
** cargo test --doc
   runs 'doctests', which ensure code in documentation works.
** claude --dangerously-skip-permissions
   Less ergonomically, can say:
   "Don't do anything you don't have permission to do.
   I'm going to be away so I can't grant it."
* QA
** add tests
*** collect_generation_ids
    test the new effective root logic
** This seems like it could have been more efficient.
   server/to_org/definitive_branch.rs
   sharing more code with
   server/to_org/initial_view_bfs.rs
** Appending from indef nodes moght be a misfeature.
** completeContents seems too long
** RelToParent could be generalized as RelToAncestor.
   It would need both a relation and a number of levels.
   (RelToParent would translate to 1 level; to grandparent, 2; etc.)
** nah
*** 'merge' should be encodeable in NodeSaveAction
    Then I wouldn't need to parse the action twice,
    in two different ways.
** test validate_merge_requests
** in elisp/skg-sexpr/skg-lens.el, rather than concatenate with ":"
   (that's done by skg--tokens->string)

   Instead of returning a :-separated string,
   return a list of lists of propertized strings,
   which the calling function can decide how to concatenate.
** Indefinitive nodes skip the visited-checking logic
   when saving a buffer.
   This is not clearly a problem.
** Maybe indefinitive nodes and repeated nodes should be merged.
** these integration tests could be merged
focus-and-folded-markers
fold-preservation
** redundancies
*** linecol_to_orgnode from save/buffer_to_orgnodes/uninterpreted.rs
    does something already done in mk_org_text/
*** Buffer_Cannot_Be_Saved and SaveError
** use the tests at not_using/pids_from_ids/tests.rs
** test the propagation of Buffer_Cannot_Be_Saved errors
   in commit 130318842f5ad0ad92279d14f85d8b950e6f844a,
   I started using
     find_buffer_errors_for_saving,
   which covers more cases than what I had been using,
     find_inconsistent_instructions
   But the tests only cover what
     find_inconsistent_instructions
   might return.
** integration test for containerward view
** not pressing
*** use s-exp parsing
**** in server/serve/containerward_view.rs
     fn extract_containerward_view_params
**** in server/serve/node_aliases.rs
     fn extract_node_aliases_params
**** in server/serve/util.rs
***** definitely
      fn request_type_from_request (
      fn node_id_from_single_root_view_request (
      fn search_terms_from_request (
***** and maybe
      fn extract_quoted_value_from_sexp (
*** Avoid uses of `unwrap` in Rust.
*** find 'panic's, replace with Result.
*** Use anyhow or eyre crates for better error handling
**** Cargo.toml
  anyhow = "1.0"
**** usage
  use anyhow::Context;

  let driver = TypeDBDriver::new(...)
      .await
      .context("Failed to connect to TypeDB server")?;
*** once it's possible, make a good pids_from_ids
    see [[./not_using/pids_from_ids]]
** dedup some code
*** is_ancestor duplicates something done a different way elsewhere
* ? lessons learned
** Validate ancestry, not descendents.
   For instance, "an alias should have an aliascol parent"
   is easier to say than "an aliascol's children can only be aliases".
   (Because it prevents combinatorial explosion?)
** Sourceward paths could have been easier.
   Just let the user add all sources from one node at a time.
   Linear paths seem unlikely backward anyway.
* document
** that filenames must correspond to PIDs
** the dangers of repeated nodes to the user
   The original data model was that each node would have only one container. That proved infeasible, because the user can copy data at will. So skg accepts such data. But bear in mind that it is dangerous. The danger is this: If a node has branches, and is copied somewhere earlier in the same document, then that new copy will take precedence. Edits to it will be treated as *the* edits. If all you did was copy the node but not its branches, its branches will be lost when you save.
** find where to put this comment
 // Titles can include textlinks,
 // but can be searched for as if each textlink
 // was equal to its label, thanks to replace_each_link_with_its_label.
** change graph -> web
** Didactically, concept maps > knowledge graph.
** [[../docs/data-model.md][The data model]] and [[../docs/sharing-model.md][The sharing model]] overlap
   as documents.
** "point" seems a better name than "node"
   But it conflicts with the Emacs 'point'.
* solutions
** search for type-oid
*** solution
    regex :
      (struct|enum|type)\s+.*TYPE_NAME
      (struct|enum|type|trait|union)\s+.*TYPE_NAME
    \s+ handles whitespace.
    To avoid matching superstrings, follow type name with \b
      \b is a word boundary to avoid matching SourceDiffFoo.
*** problem statement
    How would I search for the regex 'struct or enum or type followed by the name of it'? To find a typedef where I know the thing's name but not whether it's a sum type, product type or alias.
** reduce 4-space indentation to 2-space indentation
   sed -i 's/^\( *\)\1/\1/' THE_FILE
** to extract Emacs properties into Rust
   use [[~/hodal/emacs/property-dump.el][property-dump]]
** If I need to graft a forest's trees onto a false root,
   rctree can
   egotree can't
* dubious
** fold body into false headline
   there in Emacs but (at worst ignored) when Rust rebuilds
* if anyone else is reading this
** Most plans are not here but online.
   e.g. here:
   github.com/JeffreyBenjaminBrown/public_notes_with_github-navigable_links/blob/main/shareable_knowledge_gardens.org
   This document is mostly things I considered highest-priority.
