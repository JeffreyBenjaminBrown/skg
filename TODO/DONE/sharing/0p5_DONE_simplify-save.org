* Simplify the save-buffer pipeline before adding subscribee-edit hides

This report reviews:

- [[./0_DONE_overview-and-test.org][0_DONE_overview-and-test.org]]
- [[./1_DONE_dedup-sharing-rel-completion.org][1_DONE_dedup-sharing-rel-completion.org]]
- [[./2_DONE_hide-subscribee-content-deletions.org][2_DONE_hide-subscribee-content-deletions.org]]

and the current save-buffer path through:

- [[../../server/serve/handlers/save_buffer.rs][server/serve/handlers/save_buffer.rs]]
- [[../../server/from_text.rs][server/from_text.rs]]
- [[../../server/from_text/viewnodes_to_instructions.rs][server/from_text/viewnodes_to_instructions.rs]]
- [[../../server/from_text/viewnodes_to_instructions/to_naive_instructions.rs][server/from_text/viewnodes_to_instructions/to_naive_instructions.rs]]
- [[../../server/from_text/supplement_from_disk.rs][server/from_text/supplement_from_disk.rs]]
- [[../../server/from_text/validate.rs][server/from_text/validate.rs]]
- [[../../server/save.rs][server/save.rs]]
- [[../../server/update_buffer.rs][server/update_buffer.rs]]
- [[../../server/update_buffer/complete.rs][server/update_buffer/complete.rs]]
- [[../../server/update_buffer/complete_preorder/truenode.rs][server/update_buffer/complete_preorder/truenode.rs]]
- the sharing completers under
  [[../../server/update_buffer/complete_preorder/][complete_preorder/]],
  [[../../server/update_buffer/complete_postorder/][complete_postorder/]], and
  [[../../server/to_org/complete/sharing/][to_org/complete/sharing/]].

* Executive recommendation

Finish the save-pipeline simplifications before implementing
[[./2_DONE_hide-subscribee-content-deletions.org][2_DONE_hide-subscribee-content-deletions.org]] or any other new sharing-save
feature. The simplifications in this document are prerequisites, not
optional cleanup to defer around the feature work.

The required order is:

1. DONE Split source-move detection out of disk supplementation.
2. DONE Introduce an explicit read-only pass that classifies each
   checked ViewNode by save role.
3. DONE Route existing SaveNode construction through that
   interpreted structure.
4. DONE Begin splitting save extraction into explicit intent-oriented
   passes by introducing an internal NodeIntent layer.
5. PARTIAL Move unchanged filtering later, or make it intent-aware.
6. DONE Neutralize subscribee-as-such child-list edits so they do
   not become graph 'contains' edits for the subscribee.
7. Define a stricter completion contract for SubscribeeCol branches:
   preserve user-authored branch edits, and reconcile only the parts
   that are generated display.
8. Simplify the sharing completion code by allowing extra cheap tree
   traversals instead of preserving tightly coupled one-pass logic.
9. Only then add fork-edit extraction for as-a-subscribee content
   deletions as another explicit pass over the same interpreted
   structure.

The current temptation is to add another function after
'reconcile_same_id_instructions'. That can work, but it makes the
pipeline harder to reason about because the code still has to infer
semantics from raw tree position at a late stage, after the naive
SaveNodes already encoded the wrong interpretation.

The better shape is:

#+begin_src text
org text
  -> unchecked tree
  -> checked tree
  -> interpreted save tree / node roles
  -> graph edit intents
  -> save instructions
  -> validation
  -> graph write
  -> rerender
#+end_src

The important change is not performance. It is making "what does this
view position mean on save?" explicit and testable.

Update after discussion: the rerender/completion side cannot stay
entirely out of scope. Once edits inside SubscribeeCol branches
matter, completion must stop treating those branches as disposable
rendered projections. The save-role classifier now exists as a
read-only pass, and ordinary SaveNode construction now consults it
for traversal, 'contains', and 'subscribes_to'. The SubscribeeCol
reconciliation contract is still a prerequisite before feature work
begins.

* The main problem

The save pipeline currently conflates three ideas:

- graph edits, like "R contains [A, B]" or "delete node X";
- view/fork edits, like "subscriber S no longer wants to see C under
  subscribee E";
- display scaffolding, like HiddenInSubscribeeCol and
  HiddenOutsideOfSubscribeeCol.

That conflation is visible in
[[../../server/from_text/viewnodes_to_instructions/to_naive_instructions.rs][to_naive_instructions.rs]]:

- 'maybe_instruction_from_treenode' still builds full SaveNodes in
  the first extraction pass.
- 'collect_contents_to_save_from_children' and 'collect_subscribees'
  now use 'SaveRole', but the output is still final graph
  instructions rather than explicit intermediate intents.
- 'hides_from_its_subscriptions' is hardcoded to 'MSV::Unspecified'.
- most scaffolds are skipped entirely.

This is simple for ordinary content editing, but it breaks down for
as-a-subscribee editing. Removing E1 from under foreign subscribee E
is first encoded as "E.contains changed". Only later would another
pass have to reinterpret that as "S.hides gains E1" and suppress the
foreign-node edit. That is the right behavior fighting the current
representation.

* What looks worth doing first

** DONE Add a buffer-role classifier

Added:

#+begin_src text
server/from_text/viewnodes_to_instructions/classify.rs
tests/new/viewnodes_to_instructions/classify.rs
#+end_src

The classifier traverses the checked 'Tree<ViewNode>' and produces
facts like:

- this TrueNode is ordinary graph content of its parent;
- this TrueNode is shown as a subscribee under subscriber S;
- this TrueNode is inside HiddenInSubscribeeCol;
- this TrueNode is inside HiddenOutsideOfSubscribeeCol;
- this TrueNode is a top-level root;
- this TrueNode is ignored content because of parentIs / phantom /
  delete request.

This stayed intentionally small: a 'HashMap<NodeId, SaveRole>' and a
public 'classify_save_roles' function. It has since been wired into
ordinary SaveNode extraction, while the larger intent-oriented split
remains future work.

Implemented role shape:

#+begin_src rust
enum SaveRole {
  BufferRoot,
  OrdinaryNode,
  AsSubscribee { subscriber: ID },
  HiddenInSubscribeeCol { subscriber: ID, subscribee: ID },
  HiddenOutsideOfSubscribeeCol { subscriber: ID },
  AliasDisplay,
  IdDisplay,
  DisplayOnly,
}
#+end_src

This is worth doing because the new feature's core question is not
"what children does this node have?" It is "what does this position
mean?"

Tests currently cover ordinary TrueNodes, independent parentIs,
SubscribeeCol positions, HiddenIn/HiddenOutside positions, alias/id
display scaffolds, deleted/deleted-scaffold/unknown nodes, and error
handling for invalid HiddenIn context.

** Split extraction into several passes

The current 'naive_saveinstructions_from_tree' recursively walks and
builds full SaveNodes in one pass. It is compact, but it has become
the place where too many policies meet.

I would split it into intentionally boring traversals:

- collect definitive graph-node candidates;
- collect ordinary content children for graph-node candidates;
- collect aliases;
- collect subscriptions;
- collect edit requests / deletions;
- collect fork edits.

Each pass can traverse the tree. That is fine. The tree is small
compared to TypeDB, filesystem, Tantivy, git diff, and rendering
costs. The current one-pass shape saves little and costs clarity.

The payoff is that the as-a-subscribee feature becomes a normal pass:

#+begin_src text
For each AsSubscribee(E under subscriber S):
  if E is definitive:
    compare E's pre-save contains to visible children
    subtract S's ordinary content
    append the remainder to S.hides_from_its_subscriptions
#+end_src

** Preserve the distinction between raw buffer edits and graph edits

Before producing final 'DefineNode's, introduce an intermediate
intent type. It can be small:

#+begin_src rust
struct NodeIntent {
  pid: ID,
  source: SourceName,
  title: Option<String>,
  body: Option<Option<String>>,
  contains: Option<Vec<ID>>,
  aliases: MSV<String>,
  subscribes_to: MSV<ID>,
  hides_from_its_subscriptions: MSV<ID>,
  overrides_view_of: MSV<ID>,
  delete: bool,
}

struct ForkEditIntent {
  subscriber: ID,
  hide_from_subscriptions: Vec<ID>,
}
#+end_src

The exact fields can differ, but the separation matters. Foreign-node
validation should receive only graph edits. Fork edits should first be
folded into owned subscriber edits. That avoids the current failure
mode where a foreign subscribee's content-only view edit becomes a
'ModifiedForeignNode' before the subscriber hide can be considered.

** PARTIAL Move unchanged filtering later, or make it intent-aware

Currently:

#+begin_src text
naive instructions
  -> reconcile duplicates
  -> filter unchanged
  -> supplement MSV fields from disk
#+end_src

The new feature wants to add hides before filtering, because a
subscriber whose only change is an inferred hide would otherwise look
unchanged.

That suggests a clearer order:

#+begin_src text
raw edit intents
  -> fork edit inference
  -> fold fork edits into node intents
  -> reconcile duplicate node intents
  -> supplement from disk / construct complete NodeComplete
  -> filter unchanged
  -> foreign validation
#+end_src

The exact position of foreign validation needs care. It should happen
after content-only as-a-subscribee edits have been removed from the
foreign node's graph intent, but before writing anything.

Update: the current DefineNode pipeline now runs unchanged filtering
after same-ID reconciliation, ID canonicalization, source-move
detection, and disk supplementation. Deletes still bypass disk
supplementation and remain kept by the filter; new saves without a
disk node also remain kept. Existing saves have unspecified MSV fields
filled from disk before comparison, so omitted non-definitive fields
continue to behave as no-ops.

This is only partial for the larger sharing-save plan. Future fork
edits still need to be represented and folded into node edits before
filtering once ForkEditIntent exists; the filter is still
DefineNode-based and still compares against the pre-save in-Rust graph
via nodecomplete_from_in_rust_graph.

Verification:

#+begin_src text
cargo check
cargo test --test new classify -- --nocapture
cargo test --test new viewnodes_to_instructions -- --nocapture
cargo test --test new reconcile_same_id_instructions -- --nocapture
cargo nextest run --test move_source
cargo test none_node_fields_are_noops -- --nocapture
#+end_src

'cargo fmt --check' was attempted but this toolchain does not have the
'cargo fmt' subcommand installed.

** DONE Put source moves in their own pass

This has been done. The old
'supplement_none_fields_from_disk_if_save' orchestration function was
removed, and the existing-save path now calls focused helpers at the
caller.

The old function had several responsibilities:

- fills unspecified MSV fields from disk;
- canonicalizes pid / extra_ids from disk;
- detects source moves;
- validates source moves involving foreign sources;
- preserves misc from disk.

That was too much for a function with "supplement" in its name. It
has been split into:

- 'canonicalize_ids_from_disk';
- 'detect_source_move';
- 'supplement_unspecified_fields_from_disk';

This reduces the chance that adding fork edits changes source-move
behavior. The remaining simplifications should preserve this explicit
source-move path.

* What remains intentionally out of scope

** Do not rewrite the whole rerender completer first

The rerender side is complex, especially
[[../../server/update_buffer/complete_preorder/truenode.rs][complete_preorder/truenode.rs]], but it is already split into
preorder and postorder passes with reasonably named internal phases.
It also handles real complications: definitive/indefinitive nodes,
phantoms, diff view, deleted nodes, extra-id resolution, content
reconciliation, scaffold insertion, graph stats, view stats.

Do not fold all of that into the save-intent refactor. A general
cleanup of rerender completion is a separate project and is not one
of the prerequisite simplifications meant by this document.

However, the SubscribeeCol branch is not just ordinary generated
display anymore. If user edits inside it define fork edits, then
rerender completion must preserve those user-authored edits long
enough for the save pipeline to interpret them, and after saving it
must reconcile generated display around the edited branch without
blindly regenerating the whole branch from scratch.

That suggests a narrower completion contract:

- SubscribeeCol children that are subscribee TrueNodes are an editable
  surface for subscriptions and fork edits.

  Jeff speaking: Only somewhat editable. The user can delete
  a content child from a subscribee (to create a hides relationship)
  or replace it if it was hidden (to delete the hides relationship,
  but even in that case it will not display under the subscribee
  if it is content of the subscriber).
  They can't change order, edit text,
  or edit anything at all beyond the first generation
  of the subscribee's content.
  (Deeper layers would not be its 'content',
  but they are its 'recursive content'.)
  Any impossible edits (e.g. changing subscribee content order)
  not caught by the validation step
  will still have no effect; those non-editable things
  will respawn during completion to be as they were before.
- A definitive subscribee shown under SubscribeeCol has user-edited
  visible children whose absence/presence matters for fork inference.
- HiddenInSubscribeeCol and HiddenOutsideOfSubscribeeCol remain
  generated display for now.
- Completion may insert missing generated scaffolds and missing
  generated children, and may mark erroneous children independent,
  but it should not erase or normalize away as-a-subscribee branch
  edits before the save-intent pass has consumed them.

So the rule is not "leave rerender alone"; it is "avoid a broad
rerender rewrite, but make SubscribeeCol completion preservation an
explicit prerequisite before implementing the feature."

Update after revisiting the completion code: we are not ready for
feature implementation. The save-side role/intent work is not enough.
The sharing completion code was written under pressure to minimize
tree traversals, and that premise should be explicitly discarded
before adding subscribee-edit behavior. A few extra passes over an
ego_tree buffer are cheap compared to TypeDB, disk, git diff,
Tantivy, and rendering work; using multiple small traversals is a
better tradeoff than preserving dense traversal/borrow choreography.

Before implementing hide/unhide, add a bite-sized completion
simplification pass that makes the SubscribeeCol / HiddenIn /
HiddenOutside code easier to reason about:

- separate "read current tree facts" from "mutate the tree";
- precompute goal lists, child data, existing scaffold children, and
  erroneous children with straightforward traversals;
- then perform focused mutation passes such as reconcile children,
  insert/move scaffolds, mark erroneous content independent, and
  detach empty scaffolds;
- keep behavior unchanged, including diff-mode phantom handling and
  existing generated-scaffold ordering.

This is not a request to rewrite all rerender completion. It is a
targeted unwinding of premature traversal optimization in the sharing
completion path, so the later preservation contract can be added
without threading new semantics through already-dense code.

The one exception is directly relevant to this feature:
the save rerender path should ensure HiddenInSubscribeeCol exists
when a subscriber's updated hides intersect a subscribee's direct
content. Right now HiddenInSubscribeeCol completion only runs if the
scaffold is present; the initial-render path has
'maybe_add_hiddenInSubscribeeCol_branch', but the save-rerender path
does not appear to have an equivalent insertion step for a newly
needed HiddenInSubscribeeCol.

That should be handled as a focused renderer fix after the save-intent
side is clear and before the feature is considered ready, alongside
tests that prove user edits inside SubscribeeCol branches are not lost
by completion.

** Do not collapse all sharing completers into one function

Plan 1 already records why this was declined. I agree with that
decision. The common helpers now seem useful:

- 'SharingScaffoldKind'
- 'goal_list_for_*'
- 'build_child_data'
- 'reconcile_sharing_scaffold_children'
- empty-scaffold detach helpers

The remaining differences between SubscribeeCol, HiddenIn, and
HiddenOutside are real. Collapsing the shells would likely move
complexity into matches rather than remove it.

** Do not make HiddenInSubscribeeCol editable as part of this

Plan 2 is right to keep HiddenIn/HiddenOutside read-only for now.
Unhiding is a separate UX and data-semantics decision. The save
pipeline should ignore edits inside those scaffolds except perhaps to
preserve or rerender them.

* A concrete staged refactor

Do every simplification stage in this section before implementing new
sharing-save features. The source-move split and read-only role
classifier are already complete; the remaining simplification stages
should be treated as the pre-feature queue. The first
feature-specific stage is marked below.

** DONE Add role classification and tests

The classifier was implemented without changing save behavior. Tests
cover:

- ordinary content;
- SubscribeeCol child;
- HiddenInSubscribeeCol child;
- HiddenOutsideOfSubscribeeCol child;
- deleted, deleted-scaffold, unknown, alias/id display nodes;
- roots and independent parentIs.

The test target 'cargo test --test new classify -- --nocapture'
passes.

** DONE Refactor ordinary SaveNode construction to use roles

'naive_saveinstructions_from_tree' now computes 'SaveRoleMap' once
and uses it so it no longer decides what children count as content by
ad hoc matching alone:

- ordinary graph nodes produce Save/Delete instructions;
- ordinary content roles feed 'contains';
- SubscribeeCol roles feed 'subscribes_to';
- alias/id display roles do not produce ordinary graph SaveNodes;
- hidden scaffold roles are display-only.

This bite intentionally preserves existing behavior: definitive
'AsSubscribee' TrueNodes still produce SaveNodes for now, while
'HiddenInSubscribeeCol' and 'HiddenOutsideOfSubscribeeCol' children
do not become ordinary graph saves or parent 'contains' entries.

Added tests cover:

- ordinary content still produces 'contains';
- 'parentIs independent' children still produce their own SaveNodes but
  are excluded from the parent's 'contains';
- alias and ID display scaffolds do not produce SaveNodes;
- SubscribeeCol children still populate 'subscribes_to';
- HiddenInSubscribeeCol and HiddenOutsideOfSubscribeeCol children do
  not become ordinary graph saves.

Verification run:

#+begin_src text
cargo check
cargo test --test new classify -- --nocapture
cargo test --test new viewnodes_to_instructions -- --nocapture
#+end_src

'cargo fmt --check' could not be run in that environment because
'cargo fmt' / 'rustfmt' was not installed.

** DONE Add a behavior-preserving NodeIntent layer

'naive_saveinstructions_from_tree' now has an internal
'NodeIntent' boundary. The current extraction path is:

#+begin_src text
checked tree + SaveRoleMap
  -> Vec<NodeIntent>
  -> Vec<DefineNode>
#+end_src

This is intentionally only the first split. The intents are still
converted immediately into final 'DefineNode' values, and no fork
edits are inferred yet.

Behavior is unchanged:

- ordinary definitive nodes and definitive 'AsSubscribee' nodes still
  produce instructions;
- delete requests still become 'DefineNode::Delete';
- save intents still become 'NodeComplete' values with
  'extra_ids: vec![]', 'misc: Vec::new()', and hide/override fields
  left as 'MSV::Unspecified';
- aliases, 'contains', and 'subscribes_to' still use the same
  role-aware helper logic as before.

Added a mixed regression test covering ordinary save, delete request,
alias, content, and SubscribeeCol extraction across the intent
conversion boundary.

Verification run:

#+begin_src text
cargo check
cargo test --test new classify -- --nocapture
cargo test --test new viewnodes_to_instructions -- --nocapture
cargo test --test new reconcile_same_id_instructions -- --nocapture
#+end_src

The exact path filter
'cargo test --test new viewnodes_to_instructions::reconcile_same_id_instructions -- --nocapture'
matched zero tests in this harness, so the broader
'reconcile_same_id_instructions' filter was used.

'cargo fmt --check' still could not be run in this environment
because 'cargo fmt' / 'rustfmt' was not installed.

** DONE Split NodeIntent into save/delete variants

'NodeIntent' is now structurally typed:

#+begin_src text
NodeIntent
  -> Save(NodeSaveIntent)
  -> Delete(NodeDeleteIntent)
#+end_src

This removes the temporary 'delete: bool' representation and the
save-only optional fields from delete intents. A save intent now has
required save fields, while a delete intent carries only 'pid' and
'source'.

Behavior is unchanged. The intents are still converted immediately
into the same 'DefineNode' values, and no fork edits are inferred
yet.

Verification run:

#+begin_src text
cargo check
cargo test --test new classify -- --nocapture
cargo test --test new viewnodes_to_instructions -- --nocapture
#+end_src

'cargo test --test new reconcile_same_id_instructions -- --nocapture'
ran 19 tests: 16 passed, and 3 existing DB-backed tests failed with
TypeDB connection refused.

'cargo fmt --check' still could not be run in this environment
because 'cargo fmt' / 'rustfmt' was not installed.

** DONE Split candidate traversal from intent construction

The preliminary extraction now has one more internal boundary:

#+begin_src text
checked tree + SaveRoleMap
  -> Vec<NodeId> candidate TrueNode positions
  -> Vec<NodeIntent>
  -> Vec<DefineNode>
#+end_src

The candidate pass owns only traversal policy: which roles recurse
and which TrueNode positions can produce preliminary intents. Intent
construction then maps those candidates through the existing
'maybe_intent_from_treenode' logic.

Behavior is unchanged:

- candidate order preserves preorder output;
- indefinitive candidates are still skipped when intents are built;
- definitive 'OrdinaryNode' and 'AsSubscribee' positions still
  produce intents as before;
- aliases, 'contains', and 'subscribes_to' still use the same
  role-aware helper logic;
- intents still convert immediately into 'DefineNode' values.

Verification run:

#+begin_src text
cargo check
cargo test --test new classify -- --nocapture
cargo test --test new viewnodes_to_instructions -- --nocapture
cargo test --test new reconcile_same_id_instructions -- --nocapture
#+end_src

'cargo fmt --check' still could not be run in this environment
because 'cargo fmt' / 'rustfmt' was not installed.

** DONE Split save-field collection into explicit passes

The preliminary extraction now separates candidate traversal from the
field collection used to assemble save/delete intents:

#+begin_src text
checked tree + SaveRoleMap
  -> Vec<NodeId> candidate TrueNode positions
  -> NodeIntentLocal by candidate
  -> contains / aliases / subscribes_to by save candidate
  -> Vec<NodeIntent>
  -> Vec<DefineNode>
#+end_src

This is still behavior-preserving and still internal to
'to_naive_instructions.rs'. It does not infer fork edits, does not
change filtering, and does not change the immediate conversion from
'NodeIntent' to 'DefineNode'.

The split makes the remaining fork-edit work less awkward because
ordinary graph fields are now collected in named passes rather than
inside one recursive function. Current behavior is preserved:

- candidate order still determines preorder output;
- indefinitive candidates still produce no intent;
- delete intents still carry only 'pid' and 'source';
- definitive 'OrdinaryNode' and 'AsSubscribee' positions still
  produce save intents;
- 'contains', aliases, and 'subscribes_to' use the existing
  role-aware rules;
- 'hides_from_its_subscriptions' and 'overrides_view_of' remain
  'MSV::Unspecified'.

Added a mixed regression test covering ordinary content, independent
content exclusion, alias collection, SubscribeeCol 'subscribes_to',
and delete output across the split extraction passes.

Verification run:

#+begin_src text
cargo check
cargo test --test new classify -- --nocapture
cargo test --test new viewnodes_to_instructions -- --nocapture
cargo test --test new reconcile_same_id_instructions -- --nocapture
#+end_src

All of those passed in this environment. 'cargo fmt --check' was
attempted but this toolchain does not have the 'cargo fmt' subcommand
installed.

** DONE Guard against edits to subscribee-as-such content after disk supplementation

The current DefineNode pipeline now has a focused prerequisite for
future fork-edit work: a SaveNode for a node whose own buffer position
is directly under SubscribeeCol no longer uses that position's child
list as the node's real graph 'contains' list. This is a guardrail,
not the final semantics for that child list.

Implemented in:

#+begin_src text
server/from_text/viewnodes_to_instructions.rs
tests/new/viewnodes_to_instructions.rs
#+end_src

The new helper 'subscribee_as_such_save_pids' classifies the checked
viewforest with 'SaveRole' and records only TrueNodes whose own role
is 'AsSubscribee'. During
'viewforest_to_nonmerge_save_instructions', after ID canonicalization
and disk supplementation, those subscribee-as-such SaveNodes get
their 'contains' restored from the pre-save disk node.

Important correction: the buffer child list under a subscribee-as-such
is not meaningless and should not be discarded conceptually. It is
user input whose eventual meaning is subscriber visibility:

- if a disk child of the subscribee is absent from the buffer branch,
  that can mean "hide this child from this subscriber";
- if a disk child of the subscribee was previously hidden and appears
  in the buffer branch, that can mean "unhide this child for this
  subscriber";
- order in the buffer branch should be ignored for save semantics,
  because completion can redraw visible subscribee content in graph
  order.

So the current restore-from-disk step is only a temporary way to
prevent the wrong graph edit, namely saving the buffer branch as
'subscribee.contains'. The later fork/visibility pass must read the
buffer child list before this guardrail erases it from the final
SaveNode.

This is intentionally narrow:

- it is not hide inference;
- it does not yet implement unhide inference;
- it does not reinterpret recursive descendants below the subscribee
  branch;
- title and body edits on the same SaveNode still flow through and
  can still be rejected by foreign validation;
- aliases and 'subscribes_to' behavior are unchanged;
- ordinary node positions still define real graph 'contains'.

Added tests prove:

- removing a child (but not a deeper descendent)
  from a subscribee-as-such branch restores the subscribee's disk 'contains';
- an ordinary owned node's child-list edit still changes its
  'contains';
- a recursive descendant under the subscribee keeps its own
  child-list edit;
- a foreign subscribee-as-such title edit remains visible to
  'validate_and_filter_foreign_instructions'.

Future rule recorded separately: editing a foreign node's content
relationships should fork the edited foreign node based on ownership,
whether or not that node appears below a subscribee branch. That later
fork behavior should not depend on detecting "descendent of
subscribee"; ownership is enough.

Verification run:

#+begin_src text
cargo check
cargo test --test new classify -- --nocapture
cargo test --test new viewnodes_to_instructions -- --nocapture
cargo test --test new reconcile_same_id_instructions -- --nocapture
#+end_src

All four commands passed in this environment.

'cargo fmt' and 'rustfmt' were attempted, but neither formatter is
installed in this toolchain.

** Add subscribee visibility inference as a pure pass

Update: the raw-intent extraction precursor is done. See the next
DONE section. What remains here is the actual comparison against disk
and subscriber hides to infer hide/unhide deltas.

After the prerequisite save-intent shape is in place, add a pass that
interprets child-list edits under subscribee-as-such positions as
subscriber visibility edits, not SaveNodes for the subscribee.
Inputs:

- checked tree;
- role map;
- pre-save node lookup.

Output:

#+begin_src rust
struct SubscribeeVisibilityEdit {
  subscriber: ID,
  subscribee: ID,
  visible_content: Vec<ID>,
}
#+end_src

The pass should compare the buffer's visible children under
the subscribee against the subscribee's pre-save graph 'contains' and
the subscriber's current 'hides_from_its_subscriptions':

- disk children absent from the buffer branch become hide intent;
- disk children present in the buffer branch become unhide intent if
  they were hidden;
- buffer children that are not disk children of the subscribee are
  not containment insertions. They should be rejected, ignored, or
  normalized by an explicit policy, but never saved as
  'subscribee.contains';
- child order is not meaningful for save semantics.

This pass should be unit-tested before it is wired into saving. This
is the first feature-specific stage; it should not start until the
preceding simplification stages are complete.

** DONE Extract raw as-subscribee visibility intents

Added a behavior-preserving raw extraction pass:

#+begin_src text
server/from_text/viewnodes_to_instructions/subscribee_hiderel_intents.rs
#+end_src

The pass records the child-list signal under each
'AsSubscribee' position before graph-save construction erases it:

#+begin_src rust
pub struct SubscribeeHiderelIntent {
  pub subscriber: ID,
  pub subscribee: ID,
  pub visible_content: Vec<ID>,
}
#+end_src

The extractor is pure and does not read disk:

#+begin_src rust
pub fn subscribee_hiderel_intents_from_tree(
  viewforest: &Tree<ViewNode>,
  roles: &SaveRoleMap,
) -> Result<Vec<SubscribeeHiderelIntent>, String>
#+end_src

Current behavior is unchanged:

- no hide inference;
- no unhide inference;
- no invalid insertion policy;
- no folding into subscriber 'hides_from_its_subscriptions';
- no change to 'DefineNode' output;
- the current restore-from-disk guardrail for subscribee-as-such
  'contains' remains in place.

The raw intent includes only visible content children:

- child TrueNodes with role 'OrdinaryNode';
- excluding non-content parentIs, phantoms, and delete-request children;
- ignoring scaffolds, hidden scaffold contents, deleted/unknown nodes,
  and recursive descendants.

Tests cover visible children, multiple subscribees in tree
order, recursive descendants, independent/delete/phantom exclusions,
and HiddenIn/HiddenOutside scaffold contents.

Verification run:

#+begin_src text
cargo check
cargo test --test new classify -- --nocapture
cargo test --test new viewnodes_to_instructions -- --nocapture
cargo test --test new reconcile_same_id_instructions -- --nocapture
#+end_src

All four commands passed in this environment.

** Fold subscribee visibility edits into owned subscriber intents

After graph intents and subscribee visibility intents exist
separately, fold the visibility edits into the subscriber's
NodeIntent by updating 'hides_from_its_subscriptions'. If the
subscriber has no graph edit intent yet, synthesize one from pre-save
data.

Only after this fold should final 'DefineNode::Save' values be
constructed.

** Drop graph edits from as-a-subscribee content positions

When a node is shown as a subscribee, its child-list edit should not
define its real 'contains'. That is true even if the subscribee is
owned, per plan 2's conservative policy.

But the construction path still must preserve those buffer children
long enough for subscribee visibility inference. The rule is:

#+begin_src text
subscribee-as-such child list
  -> hide/unhide intent for the subscriber
  -> never a graph contains edit for the subscribee
#+end_src

This is cleaner than producing an incorrect SaveNode and later
restoring its 'contains' from disk. The current restore-from-disk
guardrail should eventually disappear or move behind the visibility
inference pass.

Title/body edits to a foreign as-a-subscribee node should still be
reported as foreign edits. Title/body edits to an owned
as-a-subscribee node are a policy choice; plan 2 says real edits
should happen when the node is viewed directly, not as a subscribee.
I would enforce that consistently unless you decide otherwise.
Jeff speaking: Agreed.

** Simplify sharing completion before feature work

Do this before implementing hide/unhide inference. The immediate goal
is behavior-preserving clarity, not new sharing behavior.

The sharing completion path should stop treating extra tree traversal
as expensive. A 500-node ego_tree traversal should be tiny relative
to the rest of save/rerender, so the code should prefer several
explicit passes over one dense pass that tries to read, classify,
build, reconcile, and mutate in the same control flow.

The bite-sized version:

- for SubscribeeCol, split the current preorder function into small
  helpers that first read parent/subscription facts and compute goals,
  then separately reconcile child subscribees, then separately ensure
  HiddenOutside exists and is last;
- for HiddenInSubscribeeCol and HiddenOutsideOfSubscribeeCol, split
  "compute goal/removed IDs and child data" from "reconcile scaffold
  children" from "mark erroneous content independent" from "detach if
  empty";
- keep the existing shared helpers like
  'reconcile_sharing_scaffold_children', but do not contort callers
  merely to avoid traversing the tree again;
- add regression tests that prove output is unchanged for existing
  hidden-in, hidden-outside, and subscribee-col scenarios.

This should make the later preservation rule easier: completion can
first snapshot the user-authored SubscribeeCol branch edits, then
perform generated-display reconciliation around them.

** Then fix SubscribeeCol rerender preservation

Once the save instructions really persist the hide, run the failing
integration test. If the returned buffer still does not show
HiddenInSubscribeeCol, add a narrow rerender pass or preorder helper
that inserts HiddenInSubscribeeCol under expanded subscribees when
needed.

Also add a test that demonstrates the broader preservation contract:
an edited SubscribeeCol branch should not be regenerated from scratch
before the save pipeline has interpreted the edit. The important
thing is that completion should reconcile around user intent, not
erase it and then infer from the erased result.

This is intentionally after the save-intent work, because otherwise
the renderer can obscure whether the graph mutation happened.

* Why more traversals are probably the right tradeoff

The expensive parts of save are not walking the buffer tree:

- TypeDB lookups and writes;
- filesystem writes;
- Tantivy updates;
- git diff computation;
- graph stats after rerender;
- rendering and collateral view updates.

The buffer tree traversal cost is small. Multiple clear traversals
would be easier to test and safer than one traversal that builds
SaveNodes, handles scaffolds, applies policy, and encodes absences
as 'MSV::Unspecified' all at once.

For this feature, the clarity benefit is large because "missing from
E's children" can mean different things depending on whether E is
ordinary content, a subscribee, a hidden display child, or a phantom.
That is exactly the sort of distinction a role pass should make
explicit.

* Risks and constraints

** Duplicate definitive nodes

The existing validation and 'reconcile_same_id_instructions' logic
prevent or reconcile duplicate definitions. A role-based refactor
must preserve that behavior. I would avoid changing duplicate
semantics while adding the classifier.

** Source moves

Source moves are now detected by an explicit helper called from the
existing-save path, separate from unspecified-field supplementation.
If final SaveNode construction changes, source-move tests should
still be run to preserve the current behavior.

** Foreign validation

Foreign validation should get simpler, not broader. The goal is not
to weaken 'validate_and_filter_foreign_instructions'; it is to make
sure fork edits never masquerade as foreign graph edits.

** Diff mode phantoms

Diff mode should probably be explicitly out of scope for the first
sharing-save implementation, except for ensuring phantoms do not
infer hides. Add one regression test later.

** Rerender behavior can hide save bugs

The overview says that checking the returned buffer can be enough to
infer the graph changed. Usually true, but during implementation I
would still assert the saved '.skg' hide list in at least one test.
That separates "save persisted the hide" from "rerender displayed the
hide."

* Bottom line

Do all save-pipeline simplifications described above before starting
the new feature:

- DONE split source moves out of disk supplementation;
- DONE add role classification;
- DONE route existing SaveNode extraction through it;
- DONE add a behavior-preserving NodeIntent layer;
- DONE split NodeIntent into save/delete variants;
- DONE split candidate traversal from intent construction;
- DONE split save-field collection into explicit passes;
- DONE neutralize subscribee-as-such child-list edits so they do
  not become graph 'contains' edits for the subscribee;
- DONE extract raw subscribee-as-such visibility intents without
  interpreting them yet;
- preserve raw-buffer edits separately from graph edits beyond the
  current immediate NodeIntent-to-DefineNode conversion;
- move unchanged filtering later, or make it intent-aware;
- DONE simplify sharing completion by replacing traversal-minimizing
  control flow with small explicit read/compute/mutate passes;
- DONE define and test SubscribeeCol completion preservation.

Only then implement the feature as:

- DONE infer fork hide deltas from roles;
- DONE infer unhide deltas from subscribee-as-such buffer children that
  are disk content of the subscribee;
- DONE fold hide/unhide deltas into subscriber edits;
- fork foreign content-relationship edits based on ownership,
  independent of whether the foreign node is below a subscribee
  branch;
- fix HiddenInSubscribeeCol insertion in rerender if the failing test
  proves it is needed.

This is more tree traversal, but it removes a much more expensive
kind of complexity: late reinterpretation of SaveNodes that were
already built with the wrong semantics.

* Update 2026-05-12: sharing completion simplification done

The traversal-minimization unwind for the sharing completion path is
done as a behavior-preserving refactor. The three completion shells
now separate the phases more explicitly:

- validate the scaffold kind and structural context;
- read ancestor / graph facts into small local context structs;
- compute the goal list and removed IDs;
- build ChildData before mutation;
- reconcile scaffold children;
- run the existing cleanup mutations.

Behavior is intended unchanged. This did not add hide/unhide
inference, SubscribeeCol preservation, new validation policy, or any
rendered-output change.

Completion preservation and the actual hide/unhide feature work
remain future work.

Verification run:

#+begin_src text
cargo check
cargo test --test hidden_from_subscriptions -- --nocapture
cargo test --test subscribee_col -- --nocapture
cargo test --test new viewnodes_to_instructions -- --nocapture
#+end_src

Results:

- 'cargo check' passed.
- 'cargo test --test subscribee_col -- --nocapture' passed: 1
  passed.
- 'cargo test --test new viewnodes_to_instructions -- --nocapture'
  passed: 24 passed, 53 filtered out.
- 'cargo test --test hidden_from_subscriptions -- --nocapture' did
  not pass in this checkout: 4 passed and 3 failed. One failure was
  environmental TypeDB cleanup:
  '[DBD2] Cannot delete database since it is in use.' The other two
  failed assertions are the future hide-inference behavior tracked
  here: 'test_deleting_foreign_subscribee_content_infers_hide' and
  'test_moving_foreign_subscribee_content_elsewhere_still_hides'
  expected 'e1' under HiddenInSubscribeeCol after save.

* Update 2026-05-12: SubscribeeCol preservation done

Saved-view completion now preserves visible children of a
definitive TrueNode directly under SubscribeeCol instead of
regenerating that child list from disk. This keeps user-authored
as-subscribee branch edits in the returned saved view, so the later
hide/unhide inference work has a stable surface to interpret.

The rule is intentionally narrow:

- only saved-view rerender;
- only subscribee-as-suchs under SubscribeeCol;
- only normal non-diff mode;
- collateral views and diff views still reconcile from graph/diff
  data.

Added regression:

#+begin_src text
cargo test --test hidden_from_subscriptions test_deleting_foreign_subscribee_content_preserves_branch_edit -- --nocapture
#+end_src

Verification run:

#+begin_src text
cargo check
cargo test --test hidden_from_subscriptions test_deleting_foreign_subscribee_content_preserves_branch_edit -- --nocapture
cargo test --test subscribee_col -- --nocapture
cargo test --test new viewnodes_to_instructions -- --nocapture
cargo test --test hidden_from_subscriptions -- --nocapture
#+end_src

Results:

- 'cargo check' passed.
- The new preservation regression passed.
- 'cargo test --test subscribee_col -- --nocapture' passed: 1
  passed.
- 'cargo test --test new viewnodes_to_instructions -- --nocapture'
  passed: 24 passed, 53 filtered out.
- Full 'cargo test --test hidden_from_subscriptions -- --nocapture'
  now has the preservation regression passing and no TypeDB cleanup
  failure. It still fails on the two future hide-inference assertions:
  'test_deleting_foreign_subscribee_content_infers_hide' and
  'test_moving_foreign_subscribee_content_elsewhere_still_hides'.

* Update 2026-05-12: subscribee-as-such hide inference done

The first hide-inference feature slice is implemented:

- subscribee-as-such definitive branches now produce visibility
  intents;
- missing subscribee-as-such content is folded into the owned
  subscriber's 'hides_from_its_subscriptions';
- if the missing child was moved to become ordinary content
  of the subscriber, no hide is inferred;
- subscribee-as-such child-list edits still do not become graph
  'contains' edits for the subscribee;
- saved-view rerender now inserts HiddenInSubscribeeCol under
  expanded subscribees when the updated subscriber hides intersect
  the subscribee's direct content.

Indefinitive subscribees with a pending definitive-view request do
not produce visibility intents. That prevents the expansion request
itself from being misread as "hide every child I have not expanded
yet."

Verification run:

#+begin_src text
cargo check
cargo test --test hidden_from_subscriptions -- --nocapture
cargo test --test new viewnodes_to_instructions -- --nocapture
cargo test --test subscribee_col -- --nocapture
cargo run --bin cleanup-test-dbs
#+end_src

Results:

- 'cargo check' passed.
- 'cargo test --test hidden_from_subscriptions -- --nocapture'
  passed: 8 passed.
- 'cargo test --test new viewnodes_to_instructions -- --nocapture'
  passed: 27 passed, 53 filtered out.
- 'cargo test --test subscribee_col -- --nocapture' passed: 1
  passed.
- 'cargo run --bin cleanup-test-dbs' found no leftover test
  databases.

* Update 2026-05-12: subscribee-as-such unhide inference done

The inverse visibility edit is implemented. Direct, definitive
AsSubscribee children that are disk content of the subscribee now
remove matching IDs from the owned subscriber's
'hides_from_its_subscriptions'. Unrelated hides are preserved, and the
subscribee-as-such child list still does not become a graph
'contains' edit for the subscribee.

The old hide-only helper was renamed from
'add_hiderels_from_subscribees' to
'apply_hiderels_from_intents', and its header comment no
longer describes the feature as temporary hide-only behavior.

Added regressions:

- save-instruction test for visible subscribee-as-such content removing
  an existing subscriber hide;
- save-instruction test proving unrelated existing hides survive an
  unhide edit;
- end-to-end hidden-subscription rerender test moving a node out of
  HiddenInSubscribeeCol into visible subscribee content.

Verification run:

#+begin_src text
cargo check
cargo test --test hidden_from_subscriptions -- --nocapture
cargo test --test new viewnodes_to_instructions -- --nocapture
cargo test --test subscribee_col -- --nocapture
cargo run --bin cleanup-test-dbs
#+end_src

Results:

- 'cargo check' passed.
- 'cargo test --test hidden_from_subscriptions -- --nocapture'
  passed: 9 passed.
- 'cargo test --test new viewnodes_to_instructions -- --nocapture'
  passed: 29 passed, 53 filtered out.
- 'cargo test --test subscribee_col -- --nocapture' passed: 1
  passed.
- 'cargo run --bin cleanup-test-dbs' found no leftover test
  databases.
