#+title: Forks: implementation plan
#+startup: overview

A declarative plan for the fork/clone feature ("fork a foreign node by
editing it"). The decisions here were settled in 'discussion.org'; this
document states them, not the dialog. It points back to 'discussion.org'
only where the rationale is non-obvious.

Glossary: a *foreign* node lives in a source the user does NOT own
(read-only); an *owned* node lives in a source they do. *N* is a foreign
node the user edits; *C* is the *clone* a fork creates in an owned
source. C copies N's title, body, and child-list, *subscribes* to N (so
N's later additions surface to C as unintegrated content), and
*overrides* N (so override substitution draws C wherever N would
otherwise appear). A *source-set* is the subset of configured sources
active in a view.

* What already exists (do not rebuild)
The whole sharing substrate is built and tested (the 'TODO/DONE/sharing/'
arc):

- the relations 'subscribes', 'hides_from_its_subscriptions',
  'overrides_view_of' ('schema.tql'), persisted across FS/Rust/TypeDB;
- all eight PartnerCols, including the writable 'subscribeeCol' (writes
  'subscribes_to' via 'subscribeeCol_members') and 'overriddenCol'
  (writes 'overrides_view_of' via the 'SetOverrides' intent,
  'local_instruction_collection/traverse.rs:307-323');
- override substitution with the round-trip 'overridesHere' marker
  ('update_buffer/reconcile/content.rs'; validated in
  'buffer_to_viewnodes/validate_tree.rs'), visibility-gated so an
  inactive overrider falls back to drawing the original;
- the override invariants -- monogamy (<=1 user-owned overrider per
  node) and acyclicity -- checked at save and rebuild
  ('dbs/in_rust_graph/override_invariants.rs');
- the override-choice menu;
- save-time interpretation of subscribee-as-such child edits into
  hides/unhides ('local_instruction_collection/resolve_visibility.rs').

The gap this plan fills: there is no way to fork. Editing a foreign node
today is rejected outright ('from_text/validate.rs', 'apply_foreign_policy'
-> 'ModifiedForeignNode'). The fork is the affirmative alternative to
that rejection.

* The user experience
The user makes a foreign node N editable (the existing definitive-view
request, 'C-c s d'), edits it however they like -- title, body, add /
remove / reorder children, at any depth -- and saves. Any real change to
a foreign node is read as a request to fork it. The save does not commit
blindly: the server first shows a confirmation buffer listing every node
about to be forked and asks the user to approve. On approval, each edited
foreign node N becomes an owned clone C that overrides and subscribes to
N; from then on the user sees and edits C wherever N appeared, and N's
later additions show up under C as unintegrated subscribed content.

forks.md's auto-generated "<skg< unintegrated subscribed content >>"
branch is NOT built: the 'subscribeeCol' already shows exactly that, once
the prerequisite below lands.

* Prerequisite: a subscribee-as-such shows only the unintegrated remainder
Land this first; it is independent of the rest and fixes a pre-existing
bug on its own.

A subscribee-as-such must show only what the subscriber neither HIDES nor
CONTAINS. Today 'content_goal_list'
('update_buffer/reconcile/content.rs:398-419') computes
'subscribee.contains - subscriber.hides' -- it subtracts hides but not
the subscriber's own contains. Consequence (independent of forks): when a
user moves subscribed content into their own graph, 'resolve_visibility'
deliberately records no hide ("hiding your own content is silly"), so the
moved item still shows under the subscribee-as-such -- it appears twice.

Fix: make 'content_goal_list' for a subscribee-as-such subtract the
subscriber's own 'contains' as well as its hides. Mirror the change in
any sibling that derives subscribee-as-such membership, and confirm the
HiddenIn / HiddenOutside goal lists ('to_org/complete/partner_col/goal_list.rs')
stay consistent (a node both integrated and explicitly hidden must not
double-count).

Why this matters for forks: a fresh clone has 'C.contains = N.contains'.
With this rule, C's own subscribee-as-such view of N starts empty (every
copied child is excluded as contained) and fills only as N gains new
children. So the clone needs NO explicit hides. (This is "Option B" in
discussion.org; the rejected "Option A" would have had the fork
materialize hides, which double-books a node as both hidden and
contained.)

Regression: a subscriber whose own 'contains' includes a node M that a
subscribee also contains; assert M does not appear under the
subscribee-as-such.

* The fork

** Trigger and detection
Editing a foreign node forks it. Only a *definitive* (editable) foreign
node can be forked: an *indefinitive* one is read-only, so the user makes
it definitive first (the definitive-view request, 'C-c s d'). Definitiveness
is merely the prerequisite for editing; the trigger is the edit itself.
Concretely, remove the rejection of foreign edits in
'apply_foreign_policy': a 'SaveNode' for a foreign node whose buffer
content differs from disk ('buffernode_differs_from_disknode') becomes a
fork candidate instead of a 'ModifiedForeignNode' error -- so an unchanged
definitive foreign node forks nothing. (Creating a new node in a foreign
source -- 'CreatedForeignNode' -- is still an error; fork is about editing
an existing foreign node.) Any real change counts -- title, body, or
child-list, at any depth.

** The confirmation stage
A save carrying fork candidates does not commit silently. The server
collects every candidate, runs the monogamy and source checks below, and
returns a *fork-confirmation* response instead of a save result: a buffer
listing each node to be forked as a flat forest (one headline per
candidate under an invisible buffer-root), with explanatory text before
the first. Modeled on the override-choice menu (a server-built buffer the
client adopts) plus a follow-up request carrying the decision.

The buffer is read-only EXCEPT that the user may set each clone's source
(the metadata source-rotation command, 'C-c s s' / 'C-c v m'); edits to
anything else in it are ignored, and the buffer says so. It stays usable
for navigation: the user can run the usual ID-stack-push / search command
from it to look up relevant IDs.

- Approve (y): the server performs the forks and completes the save.
- Decline (n): the server aborts the WHOLE save (no node is written) and
  the client leaves the confirmation buffer open (the user still needs it
  to search for IDs).

** What the clone C gets
- title := the title the gesture produced -- the user's edited title if
  they changed it, else N.title verbatim;
- body := N.body (or the user's edit);
- contains := N.contains, shallow -- the child IDs only, stored RAW (not
  resolved); override substitution applies at render time;
- subscribes_to := [N];
- overrides_view_of := [N];
- hides_from_its_subscriptions := none (the prerequisite display rule
  excludes C's contains from its own subscribee-as-such view);
- source := chosen as below.

** Source selection
C must live in an owned source. The current rule (refined with Jeff; the
mechanics are under "Writeable clone source" near the end of this file):
- INFER from N's nearest VOGNODE ancestor's source IFF that ancestor is
  owned -- do NOT walk past a foreign ancestor. (Corrected from the
  earlier "nearest OWNED ancestor", which wrongly skipped foreign
  ancestors; Jeff's "I found a problem" below.)
- This inference runs at SAVE time, from the saved buffer's ancestry,
  BEFORE the confirmation buffer is built.
- In the confirmation buffer, each clone-to-be's source is the INFERRED
  source if inference succeeded, else a DEFAULT of the user's first owned
  source. Either way it is shown and editable (C-c s s). (This SUPERSEDES
  the earlier "if there is none, the user sets it in the confirmation
  buffer" -- a default is pre-filled, so an explicit pick is optional.)
- Only if the user owns NO source at all does the fork hard-fail
  (ForkSourceUnresolved -- nothing, inferred or default, can be filled).
- If the chosen/inferred source is INACTIVE under the active source-set,
  FORBID the fork with an explanatory error -- under a restricted set the
  user is not meant to touch those sources, and an invisible clone is
  never created silently.

'add_missing_info' only inherits a source from a buffer parent
('buffer_to_viewnodes/add_missing_info.rs:102-121') and a sourceless node
hard-fails at lowering ('lower.rs:250-252'), so the inference and the
buffer-set path both feed a concrete owned source before lowering.

*** TODO I found a problem
"infer from N's nearest OWNED vognode ancestor in the view;"
is not right. It has to be "infer from N's nearest vognode ancestor,
if that ancestor is owned." If the nearest ancestor is foreign,
don't read any further up the view-ancestry;
instead, the user will have to provide a source
at the confirmation buffer stage.
**** ONGOING Claude : you're right -- this is a real bug in what shipped.
'owned_ancestor_sources_for_foreign_vognodes' (server/from_text/fork.rs)
walks rootward and keeps going PAST foreign vognode ancestors until it
hits an owned one. Per your rule it must instead STOP at the nearest
VOGNODE ancestor (skipping only scaffolds like cols) and use that
ancestor's source ONLY if it is owned; if that nearest vognode ancestor
is foreign, infer nothing and leave the source for the user to set.

The shipped fork test did not catch it: there P directly contains N
(nearest ancestor owned), so the two rules agree. The bug needs the
shape owned-grandparent -> foreign-parent -> N, where the old code
wrongly files the clone under the grandparent's source.

Fix (one-line in the loop): at the first Active vognode ancestor, insert
its source iff owned, then BREAK unconditionally -- never continue past
it. I'll also add a regression with the owned->foreign->N shape (it must
yield ForkSourceUnresolved, i.e. fall to the user-set path). Folding
this into the writeable-source work below (it produces exactly the
"nearest ancestor is foreign -> user provides the source" case that work
must handle). Marking DONE once both land.

** Monogamy
A node may have at most one user-owned overrider; there is no analogous
constraint on subscriptions. So forking a node the user has already
forked would violate monogamy
('override_invariants.rs:65-73,127-156'). The confirmation stage detects
this with a new helper 'existing_user_owned_overrider_of(N)' (over the
resolver's inverse lookup) and rejects that candidate with an
explanatory error naming the existing clone. (DECIDED, Jeff: the error
message alone is enough -- the client does NOT auto-open or offer to open
the existing clone, since visiting a view from an id is already easy. So
no client-side jump is built; this is DONE as shipped.) Never mint a
second clone, never auto-delete the first.

** Shallow copy and override resolution
C stores N's child IDs raw. At render each child runs through
'resolve_override', so:
- if a copied child is itself a node the user has forked, C shows that
  child's clone -- desirable, and visible via heralds;
- if N contains itself (or a cycle through the copied IDs), the
  resolver's cycle guard draws the repeat as an indefinitive cycle node
  -- normal cycle display, nothing special.

** One save: ordering and round-trip
Creation + subscribe + override happen in one save (they are additive on
a fresh node, unlike move+merge). The touched-override invariant check
reads the post-save simulated graph, so C and its 'overrides_view_of'
edge must be in 'touched' and in that simulated graph before validation
-- as nodeMerge already simulates its nodes.

The fork is the first code path that ever writes 'overrides_view_of'
from a save, so the 'overridesHere' round-trip (render -> save ->
re-render, keeping the container's stored 'contains' pointed at N, never
rewritten to C) needs dedicated coverage. The inactive-source fallback
(an inactive clone does not substitute; the original is drawn raw) is
already coded; it needs a test, not new logic.

* Collateral-rerender staleness fix (in scope)
A definitive subscriber's 'subscribeeCol' is not refreshed from the graph
during a collateral rerender. 'reconcile_subscribee_col_children'
('update_buffer/reconcile/subscribee_col.rs') computes the fresh goal
list but only applies it under 'if parent_indefinitive ||
source_diffs.is_some()' (line ~74), so a definitive subscriber in a
non-diff collateral view keeps stale children. The guard is correct for
the SAVED view (a definitive subscriber there is the source of truth for
that save) but wrong for a collateral rerender (which should refresh from
the graph). Fix: thread a "this is a collateral rerender" signal and
reconcile children regardless of definitiveness in that case. Forking
creates subscribers, which makes this easy to hit; pre-existing, but in
scope here. Test: open two views of a definitive subscriber, change its
subscriptions in one, save, assert the other refreshes.

** ONGOING ah wait I don't understand this
I need you to explain this to me more simply.
Please provide a glossary of all terms involved,
and then restate the idea in plainer English.
*** Claude : glossary, then plain English
**** Glossary (the terms in that paragraph)
- *view* / *buffer* :: one window onto the graph, shown as an org
  buffer (a tree of headlines). You can have several open at once,
  and two of them can show the same graph node at the same time.
- *subscriber* / *subscribee* :: a node that "subscribes" to another.
  The subscribee's content surfaces under the subscriber as
  "unintegrated subscribed content". (Forking uses this: a clone
  subscribes to its original. So once forks exist, subscribers become
  common.)
- *subscribeeCol* :: an auto-generated scaffold listed under a
  subscriber, holding the nodes it subscribes to. skg GENERATES it
  from the graph; the user does not type its membership. Its
  "children" are those subscribed nodes.
- *definitive* vs *indefinitive* :: definitive = shown editable
  (read-write); indefinitive = shown read-only. The SAME graph node
  can be definitive in one view and indefinitive in another.
- *the saved view* vs *a collateral view* :: when you save a buffer,
  that buffer is "the saved view". Any OTHER open buffer that shares a
  node with it is "collateral". After a save, skg re-renders the saved
  view AND every collateral view, so two windows onto the same node
  agree.
- *reconcile* against a *goal list* :: to refresh a generated
  collection, skg computes the "goal list" (the members it SHOULD have
  per the current graph) and reconciles the on-screen collection
  against it -- adding missing members, dropping stale ones.
- *stale* :: showing what the graph USED to say, not what it says now.
- *the guard* :: the `if parent_indefinitive || source_diffs.is_some()`
  condition that decides whether to actually apply the fresh goal list.
- *source_diffs.is_some()* :: shorthand for "we are in git-diff mode".
**** Plain English
Picture the same subscriber node S open in two windows, A and B.
In window A you change S's subscriptions and save. skg updates the
graph and re-renders A correctly. Window B is collateral -- it should
also refresh so ITS subscribeeCol shows the new subscription list. But
it doesn't: B keeps the OLD list on screen. That stale list is the bug.

Why: the function that refreshes a subscribeeCol
('reconcile_subscribee_col_children') always computes the fresh list
from the graph, but only APPLIES it when the guard holds -- i.e. when
the subscriber is read-only (indefinitive) OR we're in git-diff mode.
If the subscriber is editable (definitive) and we're not in diff mode,
it skips applying and leaves whatever was already drawn.

That skip is RIGHT for the saved view: in the window you just saved,
your buffer text is the authority on the subscriptions (you may have
just typed them), so skg must not overwrite it mid-save with a
graph-derived list. But the skip is WRONG for a collateral view, which
you did NOT edit -- it should always defer to the just-saved graph.
The code can't yet tell those two apart: "definitive + no diff" lumps
"the view you just saved" (don't refresh) together with "a collateral
view of the same node" (do refresh).

The fix: thread one extra boolean down the render path meaning "this
is a COLLATERAL re-render, not the view that was just saved." When it's
true, refresh the subscribeeCol from the graph regardless of
definitiveness. The saved view stays byte-stable; collateral views
catch up. (Forks make this matter because forking mints subscribers,
so you'll often have a subscriber open in two windows -- exactly when
this latent bug shows.)

If that lands cleanly, please mark this DONE.
***** DONE that guard sounds totally wrong
Is there something similar for the other collections?
If so we will have to correct them too.

Whether there are source diffs is irrelevant; rerender should always
correct containers to match what their owning vognode (in this case,
the subscriber) actually shows on disk. Whether the owning vognode
is indefinitive is also (for the children of Col scaffolds,
not for the owning vognode itself) irrelevant -- the collection
says "these things are in this relationship to the ownig vognode",
and it should not lie.
****** Claude : you're right. Removed the gate entirely; details:
- *Other collections?* No. The GENERIC reconciler
  'reconcile_partnerCol_children' -- which serves the OTHER writable col
  ('overriddenCol') and every inbound read-only col (subscriberCol,
  overriderCol, hiderCol, hiddenCol) -- already reconciles
  UNCONDITIONALLY: it never looks at definitiveness or diff mode; it
  only consults 'policy()' to choose member ORDER. So 'subscribeeCol'
  was the ONLY collection with this gate. Nothing else to correct.
- *Why subscribeeCol alone had it:* it has its own reconciler
  ('reconcile_subscribee_col_children', separate because its members are
  subscribees-as-such with special content + a trailing
  HiddenOutsideOfSubscribeeCol), and that reconciler carried a stale
  gate the generic one never had. 'subscribeecol-maybe-todo.org' had
  already flagged it as "suspicious" and asked for a focused test before
  changing it.
- *Saved view too?* Yes -- I dropped the gate completely, not just for
  collateral views. Safe even there: reconciliation runs strictly AFTER
  extraction and the graph update, so by the time it runs the graph
  already holds the subscriptions the user just saved; reconciling
  reproduces them rather than clobbering them (exactly how overriddenCol
  already behaves in the saved view). The whole view/save/override/diff
  test surface (139 tests) passes with the gate gone, including
  idempotence and the save round-trip.
- *The focused test the note asked for:*
  'tests/collateral_subscribee_staleness.rs' -- two views of a
  DEFINITIVE subscriber S; drop a subscription in one and save; assert
  the other view's subscribeeCol refreshes. Verified to FAIL under the
  old gate and pass now.
Marked DONE since it landed cleanly; reopen if you disagree.
* Types and helpers to add
- a 'ForkInstruction' (or 'ForkSpec') type carrying the clone id, the
  original id, and the copied title/body/contains -- so fork extraction
  is typed end-to-end, parallel to 'NodeMerge';
- 'existing_user_owned_overrider_of(id)' -- the monogamy pre-check;
- consolidate the duplicated "source not found" error into one
  'SourceValidationError' ('supplement_from_disk.rs', 'save.rs',
  'multiple_nodes.rs') -- nice-to-have.

There is NO 'EditRequest::Fork': the fork is triggered by the editing
gesture and detected at save, not by a metadata edit request.

* Implementation order
1. Prerequisite display fix (subscribee-as-such subtracts the
   subscriber's contains) + its regression. Independent; land first.
2. Collateral-rerender staleness fix + its regression. Independent.
3. Fork detection: in 'apply_foreign_policy', turn a modified-foreign
   'SaveNode' into a fork candidate instead of an error; resolve C's
   source (nearest owned ancestor).
4. Fork extraction: a 'fork_instructions_from_pairs'-style async function
   that fetches N from disk and builds C's 'SaveNode' (title/body/contains
   copied; subscribes_to=[N]; overrides_view_of=[N]; no hides). Reuse
   'nodeMerge_instructions_from_pairs' where genuinely shared, else note
   the quasi-duplication.
5. Monogamy pre-check ('existing_user_owned_overrider_of') and
   source validation (owned; reject inactive; 'SourceValidationError').
6. The confirmation stage: the fork-confirmation response-type, the
   writeable-for-source buffer, the ID-stack-push affordance, and the
   follow-up request that approves (commit) or declines (abort whole
   save, keep buffer open).
7. One-save ordering + the override-write round-trip, with tests.

* Tests
Follow the established sharing-test patterns ('hidden_from_subscriptions.rs',
'override_substitution.rs', 'collateral_partner_col.rs'; the
'run_with_shared_test_db' harness; multi-source 'skgconfig.toml' fixtures
with an owned source and a 'user_owns_it=false' foreign source).

- prerequisite regression (above) and collateral-staleness regression
  (above), both independent of forks;
- save-instruction assertion: a foreign edit yields a 'SaveNode' for C
  with title/body copied, contains = N's child IDs *shallow* (assert
  descendants are NOT pulled in), subscribes_to=[N], overrides_view_of=[N],
  no hides;
- fixture-file assertion after save: 'C.skg' holds those fields; N's
  '.skg' is byte-unchanged;
- confirmation flow: a foreign edit returns the fork-confirmation buffer;
  approve commits, decline aborts the whole save and leaves the buffer;
- round-trip: save C, close, reopen a view that contained N; assert C is
  drawn in N's place with '(overridesHere N)' and C's subscribeeCol shows
  N with empty unintegrated content; add a child to N on disk + rebuild,
  assert it appears as unintegrated;
- monogamy: forking an already-forked N is rejected with the naming
  error, not a raw invariant crash;
- source: inference from an owned ancestor; the buffer-set path when none
  exists; refusal when the resolved source is inactive;
- source-set: an inactive clone does not substitute and degrades to
  drawing the original;
- integration (elisp), mirroring 'tests/integration/override-menu/':
  drive the edit -> confirm -> fork flow end to end.

* Docs to update (at execution time)
- 'docs/forks.md': rewrite to the subscribeeCol mechanism and the fork
  gesture; drop the "<skg<...>>" branch; replace the provisional examples;
- 'docs/sharing-model.md': document the fork gesture and that a
  subscribee-as-such shows only what the subscriber neither hides nor
  contains;
- 'glossary.md': add "clone" / "fork";
- 'schema.tql': note the fork creates both the subscribes and
  overrides_view_of edges;
- 'TODO/DONE/sharing/vision.org': mark "fork a foreign node by editing
  it" resolved.

* Writeable clone source (build now -- Jeff: "let's do it now")
The one functional gap. Bundles the source-inference fix above (stop at
the nearest vognode ancestor; infer only if owned) with making the
confirmation buffer's clone-source editable so a fork whose source could
not be inferred can still be completed.

** Source inference correction (do first; it produces the unresolved case)
Change 'owned_ancestor_sources_for_foreign_vognodes': at the FIRST Active
vognode ancestor, record its source iff owned, then break unconditionally
(never walk past a foreign ancestor). Regression: owned -> foreign -> N
must yield no inferred source (-> ForkSourceUnresolved without the
user-set path; -> the user-set source with it).

** Default the clone source, so the common case needs no action
When building each ForkSpec, the clone source is, in priority order:
  user-set (from the confirmation buffer)  -- highest priority
  else inferred (the nearest vognode ancestor's source, iff that
       ancestor is owned -- the corrected rule above)
  else a DEFAULT: the user's first owned source in config.
So "no owned ancestor" no longer hard-fails: it defaults to an owned
source, which the user can rotate. For a user with exactly ONE owned
source this is automatic (no choice to make). ForkSourceUnresolved then
only fires if the user has NO owned source at all (cannot fork anywhere).
*** DECIDED (Jeff: "agreed on both"): default-to-first-owned-source (NOT
a '<choose>' placeholder). The confirmation buffer SHOWS each clone's
source so it is visible and rotatable, mitigating the "silently filed in
the wrong owned source" risk.

** How the user sets it: editable confirmation buffer + C-c s s
*** DECIDED (Jeff: "agreed on both"): the editable confirmation buffer
(the plan's design), NOT the simpler minibuffer-prompt alternative.
The confirmation buffer stops being fully read-only: each fork headline
carries the clone's proposed source as its '(source ...)' field (the
default above), and 'skg-set-source' (C-c s s) rotates it among owned
sources. Every OTHER edit is ignored. On approve, the client walks the
buffer's headlines, reads each (id . source), and sends them as a new
'(fork-sources ((N . src) ...))' field on the fork-approved re-save. The
server reads that map and feeds it as the highest-priority clone-source
for each N (validated owned + active, reusing validate_fork_specs).

*** NEW QUESTION: the source field on each confirmation headline names
the CLONE's target, not the original's real source. The buffer is a flat
list with ONE headline per foreign node to be forked; each such headline
reads '(skg (node (id N) (source OWNED-TARGET) indef)) <title>' -- i.e.
it shows the original's id but with the OWNED source the clone will land
in (the original's real source is foreign). This is deliberate: it lets C-c s s (which rotates
the '(source ...)' field among owned sources) set the clone target with
no new command, and the buffer's preamble explains "this source is where
the clone goes". The only oddity is cosmetic (N drawn with an owned
source it does not actually live in; the buffer is never saved as N, so
nothing validates it). Acceptable? Or would you rather keep N's real
(foreign) source and add a separate clone-target indicator + a dedicated
rotate command? I recommend the former (reuse '(source ...)' + C-c s s).

**** TODO
You made me realize it's more complicated.
The user may have edited the title.
It will be confusing to see only one headline standing in for both nodes.
Instead the confirmation buffer should be two levels deep.
The first layer is each fork-to-be,
each of which has children equal to everything it would override
(with appropriate heralds -- the child is independent,
and the child is marked 'pO' IIRCS ("parent overrides)).
That way both can display their proper titles and sources,
and the user can easily see which sources' nodes they are forking.
***** ONGOING Claude : agreed, the two-level buffer is right. Reiterating
so we're aligned, then one sub-question (the herald).

SHAPE -- per fork, two levels:
  * <edited title>   (the FORK-TO-BE = clone C: an OWNED source [editable
                      via C-c s s], the user's EDITED title, and NO id --
                      C does not exist yet)
  ** <original title> (the node C would OVERRIDE = the original N: its
                       REAL id, its REAL (foreign) source, indefinitive,
                       parentIs=independent, + a herald for the override)
The clone-to-be parent carries the one editable thing (its source). The
child shows the original with its true title + source, so you see exactly
which source's node you're forking and what the clone will become. A
clone that overrides several nodes lists several children.

WHY this also fixes the title problem: the edited title lives on the
parent (the clone), the original title on the child (the original) --
two headlines, each honest about the node it is.

ON APPROVE: the client pairs, per fork-to-be, the PARENT's (source) with
the CHILD's (id N) -> '(fork-sources ((N . owned-src) ...))'. The parent
needs no id; the fork is keyed by the child's id.

SUB-QUESTION (the herald): there is no 'pO'/"parent overrides" herald
today. The override heralds are 'overriding' (O), 'overridesParent' (Op,
the INVERSE -- the node overrides its parent), 'grandparentOverrides'
(gO), 'grandparentSubscribes' (gS), and 'overridesHere' (Oh). So to mark
the child "your parent overrides you" I see three options:
  (a) ADD a new herald 'parentOverrides' ("pO") -- matches your two-level
      intent exactly; cost is a viewStats field + a heralds.rs leaf + the
      conformance test + emitting it on the child. Small but real.
  (b) Reuse the existing OVERRIDDEN-AS-SUCH idiom: put the child under an
      'overriddenCol' of the clone-to-be (C -> overriddenCol -> N). N is
      then a normal overridden-as-such and AUTO-gets the 'gS' herald
      (since C also subscribes to N). No new herald, but THREE levels
      (a col scaffold between), not the two you asked for.
  (c) No herald: rely on the two-level shape + the buffer's preamble
      ("each top entry is a clone you'll create; its child is the
      original it forks"). Simplest; least explicit.
I lean (a) -- it is what you described and reads cleanest. Which do you
want? (Then I build: source-inference fix first, then this.)
****** DONE Jeff : (a)
Plus: by the time the confirmation buffer is shown, ancestry inference
has ALREADY run; the default source is only supplied in the buffer when
inference failed. (Supersedes the earlier "Source selection" wording.)
******* Claude : decided -- herald (a), and the inference/default
ordering folded in. Two changes recorded:
- *Herald*: ADD a 'parentOverrides' ("pO", red) viewStats herald, emitted
  on each overridden child in the confirmation buffer; plus its
  heralds.rs leaf and the conformance-test atom. (New build step.)
- *Source ordering* (your clarification -- it BEATS the older "Source
  selection" bullet, which I rewrote to match): inference from the saved
  buffer's ancestry runs FIRST, at save time, before the confirmation
  buffer exists. Each clone-to-be's source shown in the buffer is the
  INFERRED source when inference succeeded, and only FALLS BACK to the
  default (first owned source) when it could not be inferred. So the
  default is a confirmation-buffer-time fill for the un-inferred case,
  never a substitute for a successful inference. The user can still
  rotate either. (Hard-fail ForkSourceUnresolved only if the user owns
  NO source at all, so nothing -- inferred or default -- can be filled.)

** Build steps
1. Source-inference correction + its regression (server only; a clean
   bugfix, safe to land first on its own -- Jeff OK'd this). Stop at the
   nearest vognode ancestor; infer iff owned.
2. Server: clone-source resolution = INFERRED (step 1) else DEFAULT (the
   user's first owned source), applied when the ForkSpec is built so the
   confirmation buffer always carries a concrete owned source; the
   user-set source from the re-issued save then overrides it.
   'fork_sources_from_request' parsing; thread the user-set map through
   buffer_to_validated_saveplan -> validate_and_filter_foreign_instructions
   -> fork_spec_from_buffer_node; validate the final source owned+active.
   ForkSourceUnresolved now only when the user owns no source at all.
3. The 'parentOverrides' ("pO", red) herald (decided (a)): a new
   ViewNodeStats field, its leaf in heralds.rs, the
   herald_rules_cover_the_emittable_vocabulary atom, and emit it on the
   overridden child.
4. build_fork_confirmation_buffer: emit the TWO-LEVEL shape (see the
   reiteration above) -- per fork, a clone-to-be parent (editable owned
   source via C-c s s, edited title, no id) over its overridden
   original(s) (real id/title/source, independent, marked "pO"). Update
   the explanatory text accordingly.
5. Client: make the confirmation buffer editable (not read-only);
   skg-approve-fork pairs, per fork-to-be, the PARENT's source with the
   CHILD's id, and adds the '(fork-sources ((N . src) ...))' field to the
   re-issued save.
   PITFALL to guard: the editable confirmation buffer must NOT be a normal
   save target. Its clone-to-be parents are new (id-less, owned-source)
   nodes; a stray ordinary save of this buffer would CREATE them as bare
   nodes (no subscribe/override edges, no confirmation). So the buffer's
   normal-save path must be refused or redirected to approve (e.g. leave
   skg-view-uri nil so the existing nil-view-uri save guard rejects it,
   or rebind the save key to skg-approve-fork). Approve/decline are the
   only actions on it.
6. Tests: source-inference regression (owned -> foreign -> N); a fork
   with no owned ancestor completes via the default (and via a user-set
   override) source (Rust, driving fork-sources directly); the "pO"
   herald renders on the child; extend the integration test to rotate a
   source in the confirmation buffer before approving (needs a fixture
   with TWO owned sources -- the current one has only one).
