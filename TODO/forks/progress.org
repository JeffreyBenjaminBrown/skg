#+title: Forks: execution progress
#+startup: overview

Companion to 'plan.org'. The plan is left as-is; this file records
what got done, deviations on *how* (not *what*), and bad news. Per
'when-to-stop.org', deviations on how-to-achieve are recorded here, not
escalated. Items needing Jeff's attention are marked TODO.

* Architectural decisions made during execution
These were under-specified in the plan; I picked the cleanest option
and proceeded (all easily reversible, or precedented).

** Confirmation transport: a field on a re-issued save, not a new request
The plan says "a new response-type plus a follow-up request". I
implement the follow-up as the field '(fork-approved . "true")' on a
*re-issued save request*, mirroring the override-menu precedent
('(override-choice . "bypass")' on a re-issued view request), rather
than a dedicated request type or a server-stashed pending save.

WHY: the plan requires the confirmation buffer to stay usable for
search / ID-stack-push while it is open. A persistent TCP connection
processes one request at a time, so a save handler that blocked
mid-pipeline reading the y/n decision would wedge the connection and
forbid search. Instead, the first (unapproved) save that finds fork
candidates returns a 'fork-confirmation' terminal message (after the
mandatory save-lock), commits nothing, and RETURNS -- freeing the
connection. Approval re-issues the same buffer with 'fork-approved'.
Stateless: no token lifecycle, no leak if the user never answers.

** Fork extraction copies the BUFFER node, not a disk-fetched N
Unlike nodeMerge (which fetches its acquiree from disk because the
buffer holds only an ID reference), the fork already has N fully in the
buffer -- the user just edited it. So C copies the *edited* buffer
node's title/body/contains directly; no async disk fetch of N for
content is needed. N's pid (for the subscribes_to / overrides_view_of
edges) is the buffer node's pid. N's .skg is left untouched; the
foreign SaveNode for N is dropped and replaced by C's SaveNode.

** Source inference walks the viewforest, computed before validation
C's source = nearest OWNED vognode ancestor of N in the viewforest
(new walk modeled on 'nearest_truenode_ancestor_source'). The flat
DefineNodes at foreign-policy validation have lost ancestry, so the
owned-ancestor source is computed where the viewforest is live
(buffer_to_validated_saveplan) and threaded in, keyed by foreign pid.
A foreign node can appear at multiple positions; v1 takes the first
owned ancestor found in a DFS (monogamy means one clone anyway).

** SourceValidationError: typed fork errors now; broad string dedup deferred
The plan's "consolidate the duplicated 'source not found' error" is a
nice-to-have. The recon found the duplication is wider than three sites
(~9, across fs/git/diff layers) with carrier-type friction (io::Error
vs String vs BufferValidationError). I add the clean part -- typed
BufferValidationError variants for the fork's own source rules -- and
defer the broad string-literal consolidation. Recorded in
TODO/problems.org as quasi-duplication.

** Collateral-staleness fix: removed the gate, did not thread a flag (Jeff's call)
The plan said "thread a 'this is a collateral rerender' signal and
reconcile children regardless of definitiveness in that case." I first
implemented exactly that (a 'reconcile_definitive_subscribee_cols' bool
through CompletionContext / rerender_view). Jeff pushed back in plan.org
("that guard sounds totally wrong") and asked whether other cols share
the issue.

Investigation: the GENERIC reconcile_partnerCol_children (used by the
other writable col 'overriddenCol' and every inbound read-only col)
reconciles UNCONDITIONALLY -- no definitive/diff gate. subscribee_col
was the ONLY col with the gate. So nothing else needs correcting, and
the gate was anomalous, not load-bearing. Removing it entirely (always
reconcile, like every other col) passed the whole view/save/override/
diff surface (139 tests) plus a new collateral regression
('tests/collateral_subscribee_staleness.rs', verified to fail under the
old gate). So I dropped the threaded-bool machinery and just removed the
gate. Simpler than the plan, and it also fixes the saved-view case the
plan deliberately left gated (safe: reconciliation runs post-extraction/
post-graph-update, so the graph already holds the user's edit).

* Changes to the plan's envisioned order
(For my own benefit.)
- Steps 1 and 2 (the two independent prerequisites) landed first, as the
  plan ordered. Step 2 was then SIMPLIFIED mid-stream (gate removal, not
  the threaded "collateral rerender" bool the plan described) after Jeff's
  pushback; the threaded-bool version was written, then reverted.
- Steps 3-4 (detect + extract) landed together and were COMMITTED WITH
  AN AUTO-APPROVE stub (forks committed unconditionally) so the core
  mechanics + round-trip could be proven in isolation. Step 6a then
  layered the confirmation gating on top (replacing the stub). So the
  commit order is detect/extract/commit -> monogamy/source -> confirm,
  not a single fork commit.
- Step 6 split into 6a (server gating, Rust-tested), 6b (elisp client),
  6c (integration test) -- the plan treated it as one step.
- Docs and the adversarial-review fix came after the feature was green.

* What's done and went well
- *Step 1* (prereq display): content_goal_list subtracts the subscriber's
  own contains. ONE function changed (the de-novo and rerender paths
  share it; the to_org/complete partition's 'visible' output is dead --
  only its 'hidden' feeds the HiddenIn col, so no second site to touch).
  Regression extends the move-into-self test; verified to fail without it.
- *Step 2* (collateral staleness): removed the gate outright (Jeff's
  call), so subscribeeCol reconciles like every other col. New
  collateral regression. See the decision note above.
- *Steps 3-4* (detect + extract + commit): editing a foreign node forks
  it. The clone copies the EDITED buffer node (no disk fetch -- it is
  already disk-supplemented, unlike nodeMerge's acquiree). Source
  inferred from the nearest owned ancestor. Commits via the normal save,
  so the override-invariant simulated graph sees the clone for free.
  tests/fork.rs: save-instruction, fixture-file, override round-trip.
  Quasi-duplication noted: build_fork_clone parallels
  create_acquiree_text_preserver but is simpler (synchronous, no merge);
  not worth a shared abstraction.

** TODO Round-trip test: rendered "unintegrated content" check deferred
The plan's round-trip wanted: clone's subscribeeCol shows N with EMPTY
unintegrated content, then a new child of N appears as unintegrated. A
subscribee-as-such only EXPANDS its content when made definitive (a
viewRequest + save round-trip), so verifying this through the rendered
buffer needs that dance, and the two N-positions (overridden-as-such
and subscribee-as-such) can't both be definitive. The underlying rule
(contains - hides - subscriber.contains) is already pinned by Step 1's
hidden_from_subscriptions test, and fork_round_trip pins the override
half (clone drawn in N's place, P.contains preserved). So I deferred the
rendered unintegrated-content assertion rather than build the fragile
expansion dance. Not bad news; flagging only so it is a conscious gap.

- *Step 5* (monogamy + source validation): existing_user_owned_overrider_of
  (live graph) rejects a re-fork with ForkAlreadyExists naming the clone;
  the source-set check rejects an inactive clone source. Run in
  buffer_to_validated_saveplan. Tests: fork_monogamy, fork_source_inactive.
- *Step 6a* (server confirmation): a save with forks, unapproved, returns
  a 'fork-confirmation' message (new TcpToClient) + commits nothing;
  approval re-issues the save with (fork-approved . "true"). Gated in
  update_from_and_rerender_buffer. Test: fork_confirmation_gates_commit.
- *Step 6b* (Emacs client): skg--fork-confirmation-handler shows the
  read-only confirmation buffer + skg-approve-fork (C-c C-c) /
  skg-decline-fork (C-c C-k); the handler balances the pending-count
  (save-result and fork-confirmation are mutually-exclusive terminals).
  Byte-compiles clean.

** TODO Writeable-source path NOT built -- a foreign node with no owned ancestor cannot be forked yet
The plan's confirmation buffer is "read-only EXCEPT the user may set each
clone's source (C-c s s)", for the case where C's source cannot be
inferred from an owned ancestor. I built the READ-ONLY confirmation (the
common case: an owned ancestor exists, so the source is inferred). The
writeable-source path -- the user rotating C's source in the confirmation
buffer, and that choice riding back on the approve re-save -- is NOT
built.

CONSTRAINT THIS IMPOSES (bad news): forking a foreign node that has NO
owned vognode ancestor in the view (e.g. a foreign node opened as a bare
root) currently fails with ForkSourceUnresolved and cannot be completed
through the buffer. Workaround: place the foreign node under a node you
own (so an owned ancestor exists) before editing it. The
read-only-but-source-writeable buffer + the per-node source transport on
approve is the follow-up to lift this.

- *Step 6c* (integration): tests/integration/fork drives server + Emacs
  through gesture -> confirm -> approve -> substitution; passes.
- *Step 7* (one-save ordering + round-trip): covered. The clone's
  override edge rides the same DefineNodes, so the touched-invariant
  simulated graph sees it (fork_fixture_files commits cleanly past that
  check). Round-trip pinned by fork_round_trip + the integration test.
- *Docs*: forks.md rewritten; sharing-model, glossary, schema, vision
  updated.

** Source-set "inactive clone does not substitute" -- covered transitively
The plan listed a test that an inactive clone degrades to drawing the
original. A fork's override edge is identical to a manual one at the
resolver, and override_substitution::ownership_and_visibility_gate_substitution
already pins exactly "an inactive owned overrider does not substitute ->
original drawn raw". So I did not add a redundant fork-specific render
test (the discussion itself said this fallback is "already coded; needs
a test, not new logic" -- that test exists). fork_source_inactive covers
the other half (you cannot CREATE an inactive clone).

* Adversarial review (multi-agent) and its one confirmed fix
Ran a 4-dimension adversarial review over the whole fork diff (13
agents). 9 findings; 8 dismissed on independent verification; ONE
confirmed and fixed:

** content_goal_list subtracted the subtrahends RAW while the minuend was pid_of-resolved
The Step-1 fix subtracted subscriber.contains (and the pre-existing
worktree_hidden) raw, but content_ids (the minuend) is pid_of-resolved
by the caller (content.rs:221-225, to redirect a now-merged acquiree id
to its acquirer primary). So a child the subscriber integrated under a
MERGED id -- its contains holds the pre-merge acquiree alias M while the
subscribee's contains holds the acquirer primary M' -- would not cancel
(setlike subtraction is exact-id), and M' would double-show as
unintegrated. Fix: resolve both subtrahends through graph.pid_of,
mirroring the minuend. Narrow trigger (needs a merge + subscription).
A dedicated regression test was DEFERRED as disproportionate: it would
need a multi-id (post-merge) fixture plus the subscribee-expansion dance;
the fix is a minimal mirror of the proven content_ids resolution and the
full subscribee/merge suites confirm no regression.

* Final test status
- Rust: 455/455 pass (cargo nextest run).
- Emacs: 234/234 pass (bash/emacs-tests.sh), incl. 4 new fork tests.
- Integration: tests/integration/fork passes end to end.

* Unanticipated future costs/benefits
- BENEFIT: the fork reuses the entire sharing substrate; the net new
  server code is small (from_text/fork.rs + the detection seam + the
  confirmation gating). No new graph relations, no schema change.
- COST (minor): editing ANY foreign node now triggers the confirmation
  stage. A user who makes a foreign node definitive and tweaks it -- even
  by accident -- gets a confirmation buffer rather than a silent reject.
  That is the intended gesture, but it is a behavior change for anyone
  used to foreign edits being rejected outright.

* Open questions
** ONGOING Writeable-source confirmation buffer -- build it? (the one real gap)
The only functional shortfall vs the plan. Forking a foreign node with
no owned ancestor cannot be completed yet (details under "Writeable-
source path NOT built"). Lifting it needs: (a) the confirmation buffer
editable for each clone's source via C-c s s; (b) the chosen sources
riding back on the approve re-save (a new request field or per-node
metadata the server reads). Do you want this now, or is the
"put it under a node you own first" workaround acceptable for v1?
*** ONGOING let's do it now
**** ONGOING Claude : planned it -- see plan.org "Writeable clone source (build now)".

Good timing: it FOLDS IN the source-inference bug you found in plan.org
("I found a problem") -- the nearest-ancestor-foreign case is exactly
what this work must handle, so I corrected the inference rule as step 1
of it.

Net behavior after this lands: a fork's clone source is user-set (from
the confirmation buffer) > inferred (nearest ancestor, iff owned) > a
default of your first owned source. So "no owned ancestor" stops being a
hard failure -- it defaults to an owned source you can rotate (and for a
single-owned-source user it is automatic). It only hard-fails if you own
no source at all.

Two decisions I'd like settled before I build (both in plan.org, both
reversible -- I recommend the first option of each):
1. Default the clone source to your FIRST owned source (visible +
   rotatable in the buffer), vs a '<choose>' placeholder that forces an
   explicit pick. I lean default-to-first.
2. The editable confirmation buffer (the plan's design), vs a simpler
   minibuffer prompt for only the unresolvable forks. I lean to the
   plan's editable buffer, but flag the smaller option per "push back
   against complexity".

Say "go" (with any choices) and I'll execute it -- the source-inference
correction can even land first on its own, since it is a clean bugfix.
***** DONE Jeff: agreed on both
****** ONGOING Claude : plan revised; both decisions folded in. Then you
spotted that one headline can't honestly stand for both the original and
the (re-titled) clone, and proposed a TWO-LEVEL confirmation buffer
(clone-to-be parent over the original(s) it overrides). Agreed -- I
reiterated it in plan.org under your TODO and updated the build steps.
ONE sub-question remains there (the override herald on the child): no
'pO'/"parent overrides" herald exists today, so (a) add it, (b) reuse the
overriddenCol idiom (3 levels, auto 'gS', no new herald), or (c) no
herald (rely on shape + preamble). I lean (a). Answer that and I build:
source-inference bugfix first, then the two-level writeable-source.
** DONE Monogamy reject: error message alone (no client auto-open)
The plan said the client "offers to open" the existing clone when a
re-fork is blocked. I implemented the server side (ForkAlreadyExists
names the existing clone's id in the error), but the Emacs client just
shows the error text; it does not parse out the id and offer to visit
it. Minor UX follow-up.
*** TODO please explain in simpler english with a glossary
**** Claude : glossary, then plain English
***** Glossary
- *fork / clone* :: editing a foreign node N (one you do not own) makes
  an editable copy of it in your own data. That copy is the *clone*; the
  act is *forking*.
- *monogamy* :: you may keep at most ONE clone of any given foreign node.
  (The shared data can be cloned by many people; but YOU get one clone
  of any particular node.)
- *re-fork* :: trying to fork a node you have ALREADY forked.
***** Plain English
When you try to RE-fork (fork something you already forked), skg refuses
-- monogamy says one clone each. The refusal message already names the
clone you made earlier (it prints that clone's id).

The open question is only about CONVENIENCE on that refusal. Right now
the Emacs client just shows you the error text (which contains the id).
It does NOT automatically jump you to that existing clone. So to go edit
the clone you already have, you'd copy/visit its id by hand.

The plan said the client should "offer to open" that existing clone --
i.e. on the refusal, either jump straight to it, or ask "open your
existing clone? (y/n)". I built the server half (it tells you the id);
I did not build the client half (the jump/offer).

So the question is just: when a re-fork is refused, do you want Emacs to
auto-open (or offer to open) the clone you already made? It is a small
elisp addition either way.
****** DONE Jeff: the error message alone is fine
Visiting a view from an ID is already easy.
******* Claude : settled -- no client jump built; plan.org Monogamy
section marked DONE-as-shipped. Nothing more to do here.
** Confirmation UX: keys and text
The confirmation buffer approves on C-c C-c, declines on C-c C-k (and
interactively prompts y/n). The server-built buffer text names those
keys. If you want different keys or wording, it is a small change
(elisp/skg-request-save.el + build_fork_confirmation_buffer).

* 3_plan execution: writeable clone source (the last piece)
This section reports execution of TODO/forks/3_plan.org -- the writeable
clone source and the source-inference fix. The plan is left as-is.

** What's done and went well
All five plan steps landed in order, one (or two) commits each:
- *Step 1* (source-inference fix): 'owned_ancestor_sources_for_foreign_
  vognodes' now stops at N's NEAREST vognode ancestor, recording its
  source iff that ancestor is an owned Active vognode -- never walking
  past a foreign or inactive ancestor. Unit tests in tests/unit/fork.rs
  (owned->foreign->N infers nothing; owned->N infers; scaffold skipped).
- *Step 2* (resolution): clone source = user-set > inferred > default,
  where the default is 'SkgConfig::first_owned_source'. The
  '(fork-sources ((N . X) ...))' field is parsed in save_buffer.rs and
  threaded via a new 'buffer_to_validated_saveplan_with_fork_sources'
  through 'validate_and_filter_foreign_instructions' ->
  'fork_spec_from_buffer_node'. 'ForkSourceUnresolved' now fires only
  when the user owns NO source; 'ForkSourceNotOwned' was added for a
  non-owned chosen source. Tests: a no-owned-ancestor fork completes via
  default, and a user-set source overrides the inferred one
  (fixtures-two-owned).
- *Step 3* (pO herald): 'parentOverrides' field on ViewNodeStats +
  serialization + herald rule "pO" + coverage atom + regenerated
  tests/elisp/herald-rules.sexp.
- *Step 4* (two-level buffer): 'build_fork_confirmation_buffer' emits the
  clone-to-be parent (edited title, editable source via a sourceHerald,
  no id) over the original (real id, foreign source, indef,
  parentIs=independent, pO). To show the original honestly, 'ForkSpec.
  original_title' is now N's DISK title (threaded through ForkCandidate),
  not the edited buffer title. Unit test pins the two-level shape + pO.
- *Step 5* (client): the confirmation buffer is editable for the source
  only; guarded against ordinary saves (nil 'skg-view-uri' + a
  buffer-local C-x C-s refusal). 'skg-approve-fork' walks the buffer and
  sends '(fork-sources ((N . X) ...))' on the approve re-save. elisp
  tests + integration test tests/integration/fork-source (two owned
  sources, rotate to owned2 before approving, assert the clone lands in
  owned2).

** Changes to the plan's envisioned order
- Steps ran in the plan's order (1..5), then docs, then a small test
  fixup commit for two suites that had encoded the OLD reject-on-no-
  source behavior (validate_foreign_nodes, the subscribee-as-such
  extraction test).
- 'buffer_to_validated_saveplan' kept its 4-arg signature as a thin
  wrapper delegating to '..._with_fork_sources', so its ~44 test call
  sites stayed untouched (the same shim idiom test_utils already uses for
  the save path). The threading the plan describes still happens; only
  the entry point is split.

** Unanticipated future potential costs and benefits
- BENEFIT: 'ForkSpec.original_title' is now honestly the disk title; it
  was the edited buffer title before (harmless in the old one-level
  buffer, wrong for two levels).
- BENEFIT (fixed in passing): the confirmation buffer's 'local-set-key'
  calls had been mutating the SHARED 'skg-content-view-mode-map'. Now on
  a buffer-local copy, with a regression test -- otherwise rebinding
  C-x C-s would have broken saving in every content view. See
  TODO/problems.org.
- COST (minor): the confirmation buffer is now fully editable, not just
  its source field. Stray edits other than the source rotation are
  ignored (only each clone-to-be's source and each child's id are
  extracted, and the buffer is never an ordinary save target), so this
  is safe, but it is laxer than a strictly source-only buffer.

** TODO open question: default clone source is alphabetical, not config order
'SkgConfig.sources' is a HashMap, so the TOML's source order is not
preserved. 'first_owned_source' returns the alphabetically-first owned
source; the Emacs client's 'skg--default-source' returns the TOML-first
owned source. They coincide for a single-owned-source user but can
disagree for someone with several owned sources. Impact is low (the
confirmation buffer displays the server's choice and the user can rotate
it), but the two "defaults" are inconsistent. Aligning them would mean
preserving source order server-side (an ordered map or a
Vec<SourceName>), which is out of scope here. Flagged for your call.
