* progress
** WARNING for readers orienting from this file
This log predates the local-instruction-collection and
local-view-update overhauls. In particular, 'parentIs=collector'
(described under stage 6 below) was later removed: 'ParentIs' is now
'{Affected, Independent, Absent}', and collection membership is
marked 'Affected'. Stage 7 was never executed as written; parts of
its territory were covered differently by the local-view-update
overhaul, and the remainder is re-planned in
[[./metaplan_2.org]], which supersedes metaplan 1.
** stage 10 : heralds and the remaining stats
*** planned
Wrote 'TODO/full-schema/10_heralds-and-stats.org', covering the
metaplan_2.org step "add heralds and the remaining stats", as
amended by Jeff's 2026-06-11 directive that a herald's rules must
not be spread across Rust and Emacs.

In order: move the herald-rule table (atoms, labels, colors,
ordering) from 'elisp/heralds-minor-mode.el' into one Rust module,
served to Emacs over a new "herald rules" endpoint, with a Rust
conformance test (every emittable atom has a rule, every rule names
a live atom -- the earlier JSON idea dissolved, since both sides of
the check now live in Rust) and a one-time migration-equivalence
test; then the H graphstat ('GraphNodeStats.hiding', both
computation paths); then the gO/gS view stats
('grandparentOverrides' for subscribees-as-such,
'grandparentSubscribes' for overriddenCol members, computed
position-aware via 'relation_membership_is_real', with the
serialization AND parse arms landing together -- the viewStats
parser errors on unknown atoms, so a serialized-but-unparsed stat
would make buffers unsavable); then the read-only herald audit.
'overridesHere' ("Oh") stays with the override-rendering step.

Ends with open questions: how typed the Rust rule table should be
(lean: typed-lite), how Emacs obtains it (lean: fetch at connect),
the failure mode when unavailable (lean: disable heralds with a
message), whether the lens engine stays in elisp (lean: yes), and
whether the conformance test should also pin presentation (lean:
no).
*** open questions answered; Op stat added (2026-06-11)
Jeff confirmed all five leans; folded into the plan body.
Separately, the metaplan had lost its "define override rendering
and navigation" heading in an earlier edit, which made two of
Jeff's comments look like part of the heralds step; the heading is
restored.  Of those comments: the substitution/source-set
correction is folded into the override step (ownership is
set-independent, visibility gates substitution -- an inactive
overrider does not substitute, the original is drawn); and the
"Op (overrides parent) herald" request moved into plan 10 as the
'overridesParent' view stat, computed everywhere, as a viewstat
rather than a graphstat for the same reason as 'containsParent'
(the fact is org-parent-relative; Jeff confirmed the placement).
Also answered Jeff's awaits comment: the "unintegrated subscribed
content" presentation already exists, so that forks-gap bullet is
withdrawn.
** stage 9 : source-set safety for sharing relations
*** planned
Wrote 'TODO/full-schema/9_source-set-safety.org', covering the
metaplan_2.org step "preserve source-set safety for sharing
relations" and the folded-in 2026-06-10 decision that an inactive
member under a col must not block saving.

In order: thread the active source-set into save extraction
(plumbing); the visible-subset merge at supplementation (one named,
unit-tested function for 'subscribes_to' and 'overrides_view_of');
only then relax 'validate_inactive_node' to accept col parents
(relaxing first would turn today's loud failure into silent data
loss); omit inactive members from rendered cols (goal-list
filtering, detaching parsed placeholders, filtered de novo col
creation); the after-noop-filter guard rejecting any instruction
that writes an inactive source; small api-and-formats.md updates.

Ends with open questions: whether detaching a stale inactive col
member should warn (chosen: silent), guard placement after the noop
filter (chosen: yes, so stale buffers no-op rather than error),
diff-mode behavior until the refusal step lands, whether
'apply_source_set_to_viewforest' still needs to placeholder-ize col
members afterward, and preserving unresolvable-source members in
the merge.
*** replanned (2026-06-11)
Rewrote the plan around two of Jeff's decisions:
- Inactive subscribees-as-such are SHOWN as placeholders and saved
  positionally (subscribee order matters, like content), so
  'subscribes_to' needs no merge; omission now applies only to
  order-meaningless cols, and the visible-subset merge only to
  'overrides_view_of'.
- The abort-style write guard was infeasible (a stale buffer can
  hold a whole now-inactive subtree whose children the save rightly
  reads as belonging to their parents); replaced by
  write-suppression: instructions that would modify an inactive
  node are dropped, with the warning "Inactive nodes present in
  saved buffer remain unchanged in graph."
Remaining open questions: whether overriddenCol order matters
(if yes, the merge dies entirely), whether deleting an inactive
subscribee placeholder unsubscribes (plan assumes yes, parallel to
content), suppression-warning timing (edits-only vs
presence-based), the diff-mode interim window, and
unresolvable-source members in the overrides merge.
*** replanned again (2026-06-11): 9-2 supersedes 9-1
9-1 (renamed from 9_source-set-safety.org) became a dialog; its
decisions are baked into the clean rewrite
'TODO/full-schema/9-2_source-set-safety.org':
- the anchored weave is blessed: inactive children are OMITTED from
  display everywhere (no placeholders), and the save weaves
  invisible members back into 'contains' and 'subscribes_to';
  'overrides_view_of' uses the set-difference merge;
- exception: an inactive node with active view-children stays on
  screen (read-only) in views that were open when the set switched;
- source-set switches now RE-RENDER open views in place instead of
  closing them (new scope; "closing them all was a wart"), via a
  convert-and-prune pass plus completion with col creation enabled;
- the prune removes ALL indefinitive leaf partners, active or
  inactive (completion regenerates current membership);
- invisible relationships are deliberately untouchable.
Still-open questions in 9-2: suppression-warning timing
(edits-only vs presence-based), the silent failure of deleting a
retained inactive node, recursive omission of whole inactive
branches in fresh renders, and the diff-mode interim window.
*** implemented (2026-06-11)
Six commits on branch 'full-schema', executing
'TODO/full-schema/9-2_source-set-safety.org' in its planned order:
1. Plumbing: 'buffer_to_validated_saveplan',
   'extract_nonmergeSavePlan_locally' and
   'build_diskSupplemented_defineNodes' gained
   'active_source_set : Option<&ActiveSourceSet>', normalized once
   ('all' -> None).
2. The merges ('server/from_text/weave.rs'): 'weave' (anchored;
   contains + subscribes_to) and 'set_difference_merge'
   (overrides_view_of), applied at supplementation;
   'member_is_visible' resolves member sources via the in-Rust
   graph snapshot with disk fallback; unresolvable counts as
   invisible, hence preserved.  Fields are replaced only when the
   merge changed them, preserving MSV shape for the noop filter.
3. Validation/extraction: InactiveNodes accepted under cols, under
   gnodes, with children, and as view roots; their own content
   stays read-only.  'subscribeeCol_members' counts buffer-present
   placeholders positionally (mirroring 'content_members');
   'partnerCol_members' (overriddenCol) stays Active-only.
4. Omission: goal lists filter by activity
   ('omit_inactive_members' + 'retained_inactive_children' in
   'reconcile/mod.rs'); content and subscribeeCol retain
   view-present InactiveNodes with children; the reconciler's stale
   rule deadens InactiveNode branches and deletes leaves; de novo
   col creation filters before the non-empty check; inactive
   removed-member diff phantoms are quietly omitted (interim
   diff-mode policy).
5. Inactive-node rewrite suppression
   ('suppress_writes_to_inactive_nodes', validate.rs): after the
   noop filter, drops DefineNodes targeting inactive sources,
   SourceMoves with inactive endpoints, and nodeMerge pairs not
   provably active on both sides; warns
   "Inactive nodes present in saved buffer remain unchanged in
   graph." only when something was suppressed (edits-only timing,
   as decided).
6. Switch-rerender: 'set_active_source_set' re-renders open views
   in place via the rerender-all stream, with the convert-and-prune
   prepass ('server/update_buffer/source_switch.rs') and PartnerCol
   creation enabled; Emacs stops closing buffers and reuses the
   rerender stream handlers.
api-and-formats.md's source-set passages were rewritten (omission,
retention, save preservation, suppression, switch semantics).
*** deviations from the plan as written
- The suppression tests live in their own target
  ('tests/inactive_suppression.rs'): the silent-untouched case
  needs the process-global in-Rust graph (the noop filter reads
  it), and installing that inside the shared-process
  'tests/source_sets.rs' target would couple unrelated tests.
  Added an idempotent 'try_init_global_handle' for tests.
- The plan's "verify whether apply_source_set_to_viewforest still
  needs its col-member case": it remains load-bearing as the final
  safety net (e.g. stale buffers re-rendered after save) and for
  content placeholders parsed from old buffers; it additionally
  learned to DETACH childless inactive diff phantoms (quiet
  omission in diff mode) while converting ones with children.
- 'rerender_view' gained a 'create_partnerCols' flag rather than a
  new entry point; 'stream_rerender_views' gained prepass and
  col-creation parameters shared by rerender-all, diff toggle and
  the switch.
- No Emacs-side integration test drives the switch-rerender end to
  end; the server handler is covered by a direct test
  (scoped-thread, since the handler calls block_on internally) and
  the Emacs side reuses the already-tested rerender stream
  handlers.  Worth an integration test eventually.
- Three tests in 'tests/source_sets.rs' that encoded the
  placeholder-rendering model were rewritten for omission; the
  old no-active-children validation test now asserts the retained
  case passes; the git_diff_view suite needed no changes.
*** unanticipated future costs and benefits
- 'member_is_visible' (save side) and 'omit_inactive_members'
  (render side) resolve sources per member; both are in-memory
  lookups with disk fallback, negligible today.
- The weave runs for every definitive node's contains under a
  restricted set, including unchanged ones; it is O(children) and
  identity-stable, so the noop filter still sees unchanged nodes.
- The switch-rerender normalizes view-local niceties (e.g.
  stage-8's read-only col order), exactly as close-and-reopen used
  to.
- 'NonmergeSavePlan' gained a 'warnings' field; future save stages
  can reuse it.
*** tested
- 'cargo nextest run': 532/532 passed.
- 'bash/emacs-tests.sh': 222/222 passed.
- New coverage: 12 weave/set-difference unit tests; 3 source-switch
  prune unit tests; suppression target (3 scenarios); stale-buffer,
  positional-subscribee and weave-through-the-pipeline tests in
  tests/source_sets.rs; the rewritten omission tests.
- 'bash/all-tests.sh --no-sound': all passed (Emacs tests, Nextest,
  28 integration tests; 87s total).
*** open questions
None for Jeff; the plan's open questions were all closed before
execution, and no stoppers arose (per when-to-stop.org: no
unanticipated architectural decisions, no stumper mysteries).
*** open questions closed (2026-06-11); plan final
All four answered and folded into 9-2's body: suppression warning
fires edits-only; recursive omission of inactive branches in fresh
renders confirmed; diff-mode interim accepts quiet omission; and
the retained-inactive-deletion question mostly dissolved -- Jeff
caught an error in the plan's respawn claim.  Corrected behavior:
deleting a retained inactive node's subtree sticks in the view
(omission keeps it out of the goal lists), while the graph
relationship survives via the weave; view-deletion edits the view,
never the graph.
** stage 8 : read-only set ergonomics
*** planned
Wrote 'TODO/full-schema/8_readonly-set-ergonomics.org', the first
sub-plan drawn from metaplan_2.org ("make read-only sets ergonomic
under save and rerender").  It supersedes the never-executed
7_saving-readonly-cols.org, which now carries a SUPERSEDED header
and stays outside DONE/ (DONE/ holds executed plans).

The plan covers: the RoleCol -> PartnerCol rename; a typed
'ColPolicy' (WritableSet / ReadOnlySet / ReadOnlyFilter, with the
demote-not-discard decision recorded); view-local order
preservation for ReadOnlySet cols; typed repair warnings scoped to
the saved view, delivered via 'SaveResponse.warnings'; a parse-time
warning for edited col headline text; save abortion on IDCol
membership edits; and two small ride-alongs (panic -> Err in the
reconcile helper, the 'MSV' rule comment).
*** implemented
Eight commits on branch 'full-schema', the three ride-alongs first
and separate per Jeff's instruction:
1. Rename RoleCol and its vague aliases to PartnerCol.  Beyond the
   type: 'reconcile_relation_col_children' ->
   'reconcile_partnerCol_children', 'reconcile_sharing_col_children'
   -> 'reconcile_partnerCol_children_against_goal_list',
   'relation_col_members' -> 'partnerCol_members',
   'maybe_add_relation_col_branches' ->
   'maybe_add_partnerCol_branches',
   'member_counts_for_relation_collection' ->
   'member_counts_for_partnerCol',
   'create_relation_cols_for_fresh_nodes' ->
   'create_partnerCols_for_fresh_nodes', the file
   'reconcile/relation_col.rs' -> 'partner_col.rs', the module
   'to_org/complete/sharing/' -> 'partner_col/', and ~36 prose
   comments.  "relation" survives where it means 'NodeRelation'.
   Also fixed the stale api-and-formats.md passage that still
   described 'Scaffold::RoleCol'.
2. Made 'complete_relevant_children's orderkey and create-child
   closures fallible; the panics in 'child_data.rs' (and the
   'unreachable!'s in the AliasCol/IDCol reconcilers, and the
   panic/expect in the content reconciler) became Errs.
3. The 'MSV' comment naming the absent-versus-empty col rule.
4. 'ColPolicy' on 'PartnerCol' ('server/types/viewnode.rs'), with
   the demote-not-discard decision documented at the definition.
   The per-kind methods needing 'RelationRole' stay in
   'partner_col/kind.rs' (the types layer should not import dbs);
   a header comment there explains the split.
5. Order preservation: 'view_order_preserving_goal_list' in
   'reconcile/partner_col.rs', used when 'policy() == ReadOnlySet'.
   WritableSet keeps graph/disk order.
6. Warnings: 'complete_relevant_children' returns a
   'RepairSummary'; new 'server/update_buffer/warnings.rs' defines
   'CompletionWarning'/'RepairKind' and per-(col, owner) batching;
   'CompletionContext.warning_sink' is Some only for the saved view
   ('rerender_view' gained the parameter; collateral, rerender-all
   and de-novo pass None); 'update_views_after_save' renders the
   warnings into 'SaveResponse.warnings'.
7. Parse-time col-headline warning: 'viewnode_from_metadata' returns
   a warning when a QualCol/PartnerCol header carries title text;
   warnings flow through 'org_to_uninterpreted_viewforest' and
   'buffer_to_validated_saveplan' (both signatures widened) into
   'SaveResponse.warnings'.  Scaffold bodies still abort
   ('Body_of_Scaffold').
8. IDCol membership validation: 'idCol_membership_errors'
   ('validate_tree.rs') compares each present idCol's ID-scaffold
   multiset (in diff mode, excluding net-removed entries, via the
   new 'MembershipAxes::net_is_present') against the owner's
   pid + extra_ids; mismatch aborts with the new
   'BufferValidationError::IDCol_Edited'.  Reordering passes;
   absence means no opinion.
*** deviations from the plan as written
- The plan said "warn on duplicate member removed".  The warning
  category exists and is unit-tested, but no integration test
  triggers it through a save: validation
  ('partnerCol_children_have_distinct_ids') rejects duplicate
  read-only col members before reconciliation can see them.  The
  category remains reachable only by non-save completion paths,
  which carry no sink today.  Harmless dead-ish code, kept for
  symmetry.
- "Collateral views produce no warnings" is enforced by
  construction (None is passed at both collateral call sites and
  rerender-all) but not by an integration test: constructing a
  registered second view in the test harness was judged not worth
  the weight.  The de-novo no-warning case is structural (the
  de-novo path has no warnings channel at all).
- The three git_diff_view 'ids::save' tests encoded the old
  silently-respawn IDCol policy; they now assert the abort, and a
  new test asserts reordering still saves.
- tests/move_source.rs, tests/multi_source_errors.rs and several
  tests/new helpers needed mechanical tuple updates for the widened
  parser/saveplan signatures.
*** unanticipated future costs and benefits
- 'buffer_to_validated_saveplan' now returns a triple; any future
  caller must decide what to do with parse warnings.  A struct
  return might read better if a fourth element ever appears.
- The IDCol check does one in-Rust-graph (rustFirst) lookup per
  visible idCol per save; negligible now, and it short-circuits
  disk in the normal case.
- 'RepairSummary' gives any future col policy free repair
  accounting; the visible-subset merge work (next metaplan step)
  can likely reuse it.
- The restored-member warning's accepted false positive (a member
  newly created by the same save being reported as "restored") is
  documented in the plan's open questions; Jeff accepted it
  (2026-06-10), revisit if annoying.
*** problems discovered along the way
- Pre-existing flaky test recorded in TODO/problems.org: the
  git_diff_view 'ids::save' group fails intermittently under plain
  'cargo test' (shared-resource race; passes under nextest and in
  isolation; reproduces at commit 63feeef1, before this work).
*** tested
- 'cargo nextest run': 513/513 passed.
- 'bash/emacs-tests.sh': 222/222 passed.
- Focused targets passed throughout: unit (lib), new, save,
  subscribee_col, subscribee_col_empty_persists,
  hidden_from_subscriptions, content_view, idempotence,
  git_diff_view, initial_view_bfs, move_source, multi_source_errors,
  plus the two new targets partner_col_order and
  partner_col_warnings.
- 'bash/all-tests.sh --no-sound': all passed (Emacs tests, Nextest,
  28 integration tests; 108s total).
** stage 7 : saving read-only RoleCols
*** planned
Wrote 'TODO/full-schema/7_saving-readonly-cols.org',
a focused implementation plan for the metaplan item
"Make read-only sets predictable under save and rerender".

The plan distinguishes ordinary read-only RoleCols from the
hidden-subscription RoleCols, because earlier review settled that
stale generated members under HiddenIn/HiddenOutside should be
discarded rather than preserved as independent branches.
** stage 6 : relation accessors and generated collections
*** planned
Wrote 'TODO/full-schema/6_col-buckets-for-everything.org',
a focused implementation plan for the metaplan item
"Add relation accessors and generated collections".

The plan separates:
- in-Rust graph relation/role accessors,
- metadata for relation collection scaffolds,
- new Col scaffold parsing/rendering/validation,
- 'parentIs=collector',
- writable versus read-only collection save behavior.

Revised the plan after review to:
- use TypeDB's "relation" versus "relationship" terminology,
- use "member" rather than "endpoint",
- reuse the existing relation/role table instead of introducing a
  separate relation-end abstraction.

Revised the plan again to:
- make collection scaffold headlines titleless on the server side and
  uniformly explained by Emacs heralds,
- restrict auto-add to de-novo view creation and optionally definitive
  view expansion,
- require save-triggered completion not to recreate relation
  collection scaffolds deleted by the user.
*** implemented
Added a typed in-Rust relation accessor layer for the ordinary
node-to-node relations:
- 'contains',
- 'textlinks_to',
- 'subscribes',
- 'hides_from_its_subscriptions',
- 'overrides_view_of'.

Added relation collection scaffolds for:
- 'subscriberCol',
- 'overriddenCol',
- 'overriderCol',
- 'hiderCol',
- 'hiddenCol'.

Added 'parentIs=collector' and wired parsing, serialization, graph
stats, local validation, rendering, completion and save extraction to
understand it as generated-collection membership rather than ordinary
containment.

Changed server-side collection scaffold headlines to be titleless.
Updated Emacs heralds so collection metadata supplies the visible
labels, including the new relation collections.

Initial view construction now auto-adds relation collections under
definitive TrueNodes when the corresponding relationship set is
non-empty.  Save-triggered completion reconciles existing relation
collections but does not recreate ones the user deleted.

Save extraction now treats 'subscribeeCol' and 'overriddenCol' as the
writable relation collections.  The new read-only relation collection
children remain display-only and do not become ordinary save
candidates.

Documented and regression-tested the distinction between an absent
writable relation collection and a present-but-empty collection:
absence means no opinion, while an empty collection means the user
explicitly wants the set to be empty.

Review cleanup: made 'HiddenInSubscribeeCol' and
'HiddenOutsideOfSubscribeeCol' use the same 'parentIs=collector'
membership policy as the other generated collection scaffolds.
Completion now discards stale children marked 'parentIs=collector' in those two
read-only generated collections during reconciliation, and local
validation requires their collection members to be TrueNodes marked
'parentIs=collector'.  'HiddenCol' is a simpler relation collection,
and follows the simple relation-collection reconciliation policy
instead.

Review cleanup: clarified language that called a collection member a
"collector child".  Comments and local helper names now say that the
child is marked 'parentIs=collector', so collection membership is not
confused with the child itself being a collector.

Review cleanup: changed 'RelationRole' so its role is represented by
'BinaryRolePosition' rather than a raw TypeQL role-name string.  The
TypeQL role names remain derived from the existing relation/role table,
so role names still have a single source of truth.

Test-runner cleanup: made 'bash/all-tests.sh' sequential and
conservative rather than suite-parallel.  Its defaults now use two
Cargo build jobs, one nextest job, one integration test at a time, and
a low TypeDB operation fanout.  The TypeDB fanout is now runtime
configurable via 'SKG_TYPEDB_CONCURRENT_TRANSACTIONS'.

RoleCol refactor: replaced the eight standalone role-collection
scaffold constructors with 'Scaffold::RoleCol { roleCol: RoleCol }'
and collapsed 'ScaffoldKind' role collection cases the same way.
The external org metadata strings remain unchanged, and
'api-and-formats.md' now documents that split between internal Rust
representation and buffer syntax.
*** tested
Focused checks passed:
- 'cargo check'
- 'cargo test --test dbs -- --nocapture'
- 'cargo test --test new viewnodes_to_instructions -- --nocapture'
- 'cargo test --test save -- --nocapture'
- 'cargo test --test content_view -- --nocapture'
- 'cargo test --test subscribee_col -- --nocapture'
- 'cargo test --test hidden_from_subscriptions -- --nocapture'
- 'cargo test --test git_diff_view -- --nocapture'
- 'bash/emacs-tests.sh'

Full suite passed:
- 'bash/all-tests.sh --no-sound'
** stage 1 : rename override relationship roles
*** planned
Wrote 'TODO/full-schema/1_rename-override-rels.org',
a focused implementation plan for renaming 'overrides_view_of'
roles from 'replacement' / 'replaced' to
'overrider' / 'overridden' across schema, TypeDB queries,
in-Rust inverse-map vocabulary, tests and docs.
*** implemented
Renamed the TypeDB roles for 'overrides_view_of' to
'overrider' and 'overridden'.

Updated the schema, TypeDB relationship helpers, TypeDB search
helpers, graph-stats TypeQL, in-Rust inverse-map vocabulary,
diff-analysis role labels, audit references, override-role tests,
merge override queries, confusing merge fixture IDs, and the old
overview doc sentence.

The on-disk field remains 'overrides_view_of'.
*** verified
Focused checks passed:
- 'cargo check'
- 'cargo test --test typedb'
- 'cargo test --test merge merge_nodes'
- 'cargo test --test save none_overrides'

Full suite passed:
- 'bash/all-tests.sh'
  - passed once after the role rename
  - passed again after renaming the merge override fixture IDs
** stage 2 : explicit warning channel
*** planned
Wrote 'TODO/full-schema/2_explicit-warning-channel.org',
a focused implementation plan for separating protocol warnings from
errors across structured server responses and Emacs handlers.

The plan also includes the small documentation item from the same
metaplan section: defining "read-only set" in 'glossary.md'.
*** implemented
Added an explicit 'warnings' field alongside 'errors' for the
structured response shapes used by:
- save-result
- single-root content view
- rerender-done
- diff-analysis
- search-enrichment

Updated the shared Rust response-formatting helpers so buffer-like
responses emit:
#+begin_example
((content "...") (errors (...)) (warnings (...)))
#+end_example

Updated rerender-done formatting to emit:
#+begin_example
((errors (...)) (warnings (...)))
#+end_example

Moved existing nonfatal audit messages out of the save/content-view
error path and into the warning path.  Existing fatal validation and
generation failures still travel as errors.

Updated the Emacs handlers to parse 'errors' and 'warnings'
separately and to show both, when present, in a single org buffer with
top-level 'errors' and 'warnings' headings.

Updated 'api-and-formats.md' for the new protocol shape and added the
"read-only set" definition to 'glossary.md'.
*** tested
Focused checks passed:
- 'cargo check'
- 'cargo test serve::util --lib'
- 'bash/emacs-tests.sh'

Full suite passed:
- 'bash/all-tests.sh --no-sound'
** stage 3 : TypeDB knows source ownership
*** planned
Wrote 'TODO/full-schema/3_typedb-knows-ownership.org',
a focused implementation plan for representing configured source
ownership in TypeDB.

The first draft kept the existing node 'source' attribute for
compatibility, but that was rejected as insufficiently clean.
Rewrote the plan so TypeDB sources are first-class entities and nodes
belong to sources only through a 'has_source' relationship.

The plan now includes the wider TypeDB query migration, source-move
updates, direct test-helper setup changes, config-change rebuild
guard, and tests proving that TypeDB nodes no longer own the old
string 'source' attribute.
*** implemented
Changed the TypeDB schema so configured sources are first-class
'source' entities with:
- 'source_name',
- 'user_owns_it'.

Removed the node-owned TypeDB string 'source' attribute.  Nodes now
belong to sources through 'has_source', with the node role constrained
to exactly one source:
#+begin_example
plays has_source:node @card(1)
#+end_example

Added TypeDB source helpers for creating source entities, replacing a
node's source relation, querying node source names and ownership, and
counting node-source relations for tests.

Updated TypeDB initialization and direct test setup paths to create
source entities before creating nodes.  Node insertion now creates the
node and its 'has_source' relation in the same transaction.

Rewrote 'pid_and_source_from_id' to recover source names through
'has_source'.  Source moves now update the 'has_source' relation
instead of a node attribute.

Updated the TypeDB/in-Rust graph audit to compare source membership
through 'has_source'.

Added the startup incremental-init guard for config changes: a running
server does not poll 'skgconfig.toml', but on restart a config mtime
newer than '.skg_init_marker' prevents incremental init.

Collapsed the source-move TypeDB helper surface back to one public
function, 'update_node_source'.  The implementation still updates the
'has_source' relation, but the temporary
'replace_node_source_relation' name is gone.
*** tested
Focused checks passed:
- 'cargo check'
- 'cargo test --test typedb -- --nocapture'
- 'cargo test --test move_source -- --nocapture'
- 'cargo nextest run --test collateral_source_move'
- 'cargo test --test multi_source_errors -- --nocapture'
- 'cargo test --test subscribee_col -- --nocapture'
- 'cargo test --test hidden_from_subscriptions -- --nocapture'
- 'cargo test --test source_sets -- --nocapture'

Full suite passed:
- 'bash/all-tests.sh --no-sound'

After the 'update_node_source' helper cleanup:
- 'cargo check' passed
- 'cargo test --test move_source -- --nocapture' compiled, but could
  not run because TypeDB was not accepting TCP connections
- after TypeDB was restarted,
  'cargo test --test move_source -- --nocapture' passed
** stage 4 : validate override invariants
*** planned
Wrote 'TODO/full-schema/4_validate-override-invariants.org',
a focused implementation plan for rejecting ambiguous or recursive
user-owned override states at rebuild/init and save time.

The plan centers the stage on one shared graph-shaped validator,
save-time post-apply simulation before persistence, full-graph
startup validation even after incremental database updates, extra-ID
resolution before comparison, and client-facing validation errors.
*** implemented
Added a shared override-invariant validator for the in-Rust graph.
It rejects:
- more than one user-owned overrider for the same resolved overridden
  PID,
- user-owned override chains of the form X overrides Y overrides Z
  where X and Y are user-owned,
- nodes whose source is absent from the loaded config.

The validator resolves override targets through the graph's primary-ID
mapping before checking collisions.

Factored the in-Rust graph DefineNode application logic so the save
path and validation simulation share one mutation semantics.

Save-time validation now simulates the real post-save graph before any
store is mutated.  The simulation applies the same delete-propagation
cleanup as the save path, then applies merge DefineNodes, and returns
a structured buffer validation error if override invariants would be
violated.

Full TypeDB rebuild/init now validates the full node set before
populating TypeDB.  Incremental startup validates the full graph after
the all-node read and before publishing the server environment.
Manual database rebuild also validates before replacing stores.

Separated graph construction from override-invariant validation in the
init/rebuild paths.  Callers now build an 'InRustGraph' explicitly and
then call 'error_unless_override_invariants_hold', so validate-only
uses no longer look like they are depending on a returned graph.
*** tested
Passed:
- 'cargo check'
- 'cargo test override --lib -- --nocapture'
- 'cargo test --lib -- --nocapture'
- after moving override-invariant validator tests out of the module:
  - 'cargo test --test dbs -- --nocapture'
  - 'cargo test --lib -- --nocapture'
- after moving save-time override-invariant tests out of 'server/save.rs':
  - 'cargo test --test save override_invariants -- --nocapture'
  - 'cargo test --lib -- --nocapture'

'cargo fmt' could not be run because the local cargo installation has
no 'fmt' subcommand.

TypeDB-backed integration test binaries compiled, but the tests could
not run because TypeDB was not accepting TCP connections:
- 'cargo test --test save -- --nocapture'
- 'cargo test --test merge -- --nocapture'
- 'cargo test --test typedb -- --nocapture'
** stage 5 : source mismatch representation
*** planned
Wrote 'TODO/full-schema/5_source-mismatch-representation.org',
a focused implementation plan for replacing the audit's set-shaped
source mismatch adapter with a scalar source mismatch representation.

The plan keeps relationship role-player-set mismatches as sets, adds
an explicit source mismatch variant, and updates audit formatting so
source names are not represented as node IDs.
*** implemented
Replaced the audit's single set-shaped 'Mismatch' struct with a
variant representation:
- relationship mismatches keep relation, role and role-player ID sets,
- source mismatches carry the node PID, the in-Rust source name, and
  the TypeDB source name,
- missing TypeDB nodes are represented as their own mismatch variant.

Updated audit formatting and the ad-hoc audit probe so source
mismatches are printed as node-level source facts rather than as
'relation=has_source role=node' relationship-set mismatches.

Updated scheduled-audit org output and audit-store recording to handle
the new mismatch variants.  The audit store still uses six CSV fields,
but recorded payloads are now strings, so source names are not forced
through 'ID'.

The first implementation briefly added an audit-specific TypeDB source
cardinality helper, but that duplicated the TypeDB schema constraint
that each node plays 'has_source:node' exactly once.  The implementation
now uses the existing scalar source-name lookup and represents the
TypeDB side of source mismatches as a concrete 'SourceName'.  If the
node+source query returns no row, the audit records a distinct missing
TypeDB node mismatch instead.

*** tested
Passed:
- 'cargo check'
- 'cargo test --test dbs -- --nocapture'
- 'cargo test audit --lib -- --nocapture'
- 'cargo test --lib -- --nocapture'
- 'cargo test --test typedb -- --nocapture'
- 'bash/all-tests.sh --no-sound'
- after moving audit-store tests out of the module:
  - 'cargo test --test dbs -- --nocapture'
  - 'cargo test audit --lib -- --nocapture'
- after simplifying missing-source audit states into a missing-TypeDB-node variant:
  - 'cargo test --test dbs -- --nocapture'
  - 'cargo test audit --lib -- --nocapture'
  - 'cargo test --test typedb -- --nocapture'
* problems discovered along the way, open
* problems discovered along the way, solved
** TypeDB-backed verification was blocked
After stage 4 implementation, TypeDB-backed integration tests compiled
but initially failed immediately with:
#+begin_example
tcp connect error: Connection refused (os error 111)
#+end_example

No 'typedb' process or listening TypeDB port was visible from the
workspace when checked with 'pgrep' and 'ss'.

*** resolution
After TypeDB was available again, TypeDB-backed verification passed:
- 'cargo test --test typedb -- --nocapture'
- 'bash/all-tests.sh --no-sound'

** focused TypeDB tests require a running TypeDB server
After the 'update_node_source' helper cleanup, the source-move test
binary compiled, but all tests initially failed immediately with:
#+begin_example
tcp connect error: Connection refused (os error 111)
#+end_example

*** resolution
After TypeDB was restarted, the focused source-move test passed:
- 'cargo test --test move_source -- --nocapture'

** global string replacement would be unsafe
The words "replacement" and "replaced" appear in many unrelated
contexts, including buffer/string replacement and content-link
replacement.  The implementation should classify matches before
editing rather than applying a mechanical global rename.
*** resolution
Stage 1 was implemented with targeted edits only.
Searches for old override role strings confirmed that remaining
"replacement" / "replaced" language is unrelated to the
'overrides_view_of' TypeDB roles.
** fixture IDs may contain stale vocabulary
Some test fixture IDs appear to include old words such as
'overrides-view-of-1'.  These are IDs rather than role labels.
The implementation plan leaves room to keep them if renaming would
create noisy fixture churn.
*** resolution
Renamed the confusing merge fixture IDs:
- '1-overrides-view-of' -> 'overridden-by-1'
- 'overrides-view-of-1' -> 'overrider-of-1'

The affected merge tests and the full test suite pass after the rename.
** content-view handler syntax can be hard to audit after nested response changes
While updating the single-root content-view handler, an unbalanced
parenthesis was introduced in the nested condition-case / if / let*
shape.  The Emacs test loader caught this immediately.
*** resolution
Rewrote the changed branch with explicit 'has-errors',
'has-warnings', and 'has-content' locals, which made the nesting
shallower and easier to read.

'bash/emacs-tests.sh' passes after the fix.
** exact Rust sexp formatting differs from the prose examples
The sexp crate prints symbol-like atoms such as 'content',
'errors', and 'save-result' without quotes, and prints an empty string
atom as an empty atom.  The first focused Rust tests expected the more
documentation-like quoted spelling.
*** resolution
Updated the tests to assert the actual protocol strings emitted by the
existing formatter, while still covering that explicit 'errors' and
'warnings' fields are present and separated.

'cargo test serve::util --lib' passes after the fix.
** TypeQL match-insert can silently insert nothing
When node insertion was rewritten to match a source entity and then
insert the node plus 'has_source' relation, missing source entities did
not produce a TypeDB error.  The match simply had no rows, so the
insert did nothing.  This showed up as source-move tests finding no
nodes in TypeDB.
*** resolution
Node creation now explicitly checks that the source entity exists
before issuing the insert.  Setup paths were also updated to create
source entities before nodes.

'cargo test --test move_source -- --nocapture' passes after the fix.
** direct TypeDB setup helpers are easy to miss
Most TypeDB setup flows now create source entities before creating
nodes, but a direct nextest helper in 'tests/collateral_source_move.rs'
still created nodes immediately after schema load.  The new explicit
source existence check exposed this as:
```
TypeDB source entity 'public' does not exist
```
*** resolution
Updated that helper to call 'create_all_sources' before
'create_all_nodes'.

'cargo nextest run --test collateral_source_move' passes after the
fix, and the full test suite passes.
** TypeDB boolean display is not a bare boolean string
TypeDB row concepts for 'user_owns_it' display as strings like
'Attribute(user_owns_it: true)', not just 'true'.  The first source
entity test expected the bare spelling.
*** resolution
Normalized boolean row values by checking whether the TypeDB concept
string contains 'true'.  The source entity test and full TypeDB target
pass after the fix.
** collector membership initially broke subscribee-as-such semantics
Stage 6 changed generated collection members from implicit container
children to children explicitly marked 'parentIs=collector'.  That exposed old
subscribee-as-such code paths that recognized editable subscribees
only through non-ignored/container parent status.

The symptom was hidden-subscription completion either failing to
prefetch child data or failing to infer hide/unhide edits after a user
moved a generated hidden child into visible subscribee content.
*** resolution
Changed the generated-collection membership tests so collection
members must have 'parentIs=collector'.  The older omitted/default
'parentIs=container' shape is no longer accepted as collection
membership.

The two affected meanings were:
- a TrueNode child of 'subscribeeCol' is still a subscribee
  member only when it is marked 'parentIs=collector',
- a TrueNode child of a definitive subscribee-as-such branch is
  still visible subscribee content if it is either an ordinary
  container child or a generated child marked 'parentIs=collector'.

The change was deliberately split by meaning.  Generated
collection membership is strict: 'subscribeeCol', 'overriddenCol' and
the read-only relation Cols count only children marked 'parentIs=collector'.  Ordinary
content completion and ordinary save extraction still use
'parentIs=container' / non-ignored when deciding whether a node is real
containment content.  Visible content inside an expanded
subscribee-as-such branch can still include a child marked 'parentIs=collector', because
moving a generated hidden child into the visible branch is the existing
gesture for inferred unhide.

Local validation now rejects TrueNode children of
'subscribeeCol' or the relation Cols unless they have
'parentIs=collector'.  Test fixtures that meant collection membership
were updated to say so explicitly; test fixtures that meant "extra
display child, not a collection member" now use 'parentIs=independent'.

Updated the affected rendering and integration fixtures because the
new intended output is different: collection scaffold headlines have
empty titles, and generated collection members now print
'(parentIs collector)' instead of relying on omitted/default
'parentIs=container'.

The hidden-subscription focused suite and the full test suite pass:
- 'cargo test --test hidden_from_subscriptions -- --nocapture'
- 'bash/all-tests.sh --no-sound'
