codex resume 019e64c9-1ff6-75a3-91c5-2c54b644440b
* DONE sent : 1
Welcome, ChatGPT! Please read coding-advice/ORIENTATION.org, and whatever it refers to (recursively), for orientation.

Then: We need to make a big plan. It will be a plan of plans; each element of this big plan is enough to warrant its own dedicated sub-plan. I have sketched a vision, at 'TODO/full-schema/vision.org'. I am sure the spec is incomplete. I need your help completing it. Please read it, and think about what it leaves out, where it contradicts itself, where it is unclear, what is inelegant about it, or anything else you might push back on. Append your feedback to the last (currently empty) section called 'problems'.

Remember that your org-output does not have to be a tree of uniform depth -- it is usually natural to subdivide a big topic into more levels than a small topic.
* DONE feedback, round 1
That was great. Thank you.

I have responded to everything in problems,
hopefully marking each response 'todo'.
(I deleted one or two simple things that are now obvious --
e.g. where I had used the wrong term, I've fixed that,
and deleted your question about it.)

I'm sure we're not done. Please review the document,
and then append any new or still-open problems to
'feedback, round 2' (which really does exist now).
* ONGOING implementation
We're ready to start implementing.

I renamed plan.org to metaplan.org
(specifically TODO/full-schema/metaplan.org).
I'm still manipulating it, so don't make any changes to the metaplan.
(The initial stages of it are fixed now, I believe,
which is why we can start implementing.)

Unless and until I say otherwise, please leave git operations to me.

First step: Please write a plan for the first stage of the metaplan,
at TODO/full-schema/1_rename-override-rels.org,
for the metaplan section labeled
"Rename the roles of 'overrides_view_of' everywhere:"

I've started a new document, TODO/full-schema/progress.org.
Throughout implementation -- both when writing plans and executing them --
pleease record progress under its first headline
(subdividing org-style in whatever way you find natural)
and record problems discovered along the way under the second
(again subdivided org-style).
