* discussion : local BFS instruction collection
This is Claude responding to [[./1_vision.org]].
Structure: first what I believe the current code does (so you can
correct any misreading), then places where the vision contradicts the
code, then the decisions I think remain open (each with a
recommendation you can accept or reject inline), then dangers, then a
concrete sketch to argue about.

I have also appended a few code problems I found along the way to
[[../problems.org]].
* the current pipeline, as I read it
Please correct anything wrong here; everything later depends on it.
** the part of the save path this vision would replace
'buffer_to_validated_saveplan' in 'server/from_text.rs' runs, in order:
parse, enrich, validate, place, and then the part at issue:

- 'SaveAuthority::from_viewforest' does two whole-tree passes:
  - 'viewforest_with_saveroles' (in 'classify.rs') clones the entire
    viewforest into a 'Tree<ViewNode_in_Role>', computing each node's
    'SaveRole' by looking *up* at its parent, grandparent, and
    sometimes great-grandparent.
  - 'collect_savenode_candidates' walks that tree to find positions
    allowed to own instructions ('Ordinary', 'Subscribee',
    'Overridden').
- 'extract_save_intents' then runs five more passes, each iterating
  over the candidate list against the whole tree:
  'collect_node_edit_basics' (title/body/delete),
  'collect_contains_by_node_id',
  'collect_aliases_by_node_id',
  'collect_subscribees_by_node_id',
  'collect_overridden_by_node_id',
  plus 'subscribee_hiderel_intents_from_candidates'.
- 'assemble_node_edit_intents' zips those five maps into
  one 'NodeIntent' per candidate.
- 'reconcile_nodeIntents' groups intents by PID and errors on
  save+save or save+delete collisions for one PID.
- 'apply_hiderels_from_intents' converts the subscribee-as-such
  visibility signals into hides/unhides deltas on subscriber intents.
  This consults disk, and consults the subscriber's own post-save
  'contains' from the same intent map.
- 'build_diskSupplemented_defineNodes' fills 'MSV::Unspecified'
  fields from disk, canonicalizes IDs, detects source moves.
- 'filter_wouldbe_noop_defineNodes' drops saves identical to the
  in-Rust graph.

Separately, 'instructiontriples_from_merge_candidates' (merge
extraction) and 'validate_no_title_or_body_edit_in_subscribeeAsSuch'
also consume the same 'SaveAuthority'.
** how little of the role classification is actually consumed
Evidence for your instinct that this machinery is overgrown:

- 'SaveRole::AliasDisplay', 'IdDisplay', 'HiddenInSubscribeeCol', and
  'HiddenOutsideOfSubscribeeCol' are computed (the latter two with
  nontrivial grandparent/great-grandparent walks that can error) and
  then never matched on anywhere. Only 'Ordinary', 'Subscribee', and
  'Overridden'-vs-rest are ever consumed.
- 'DefinenodeCandidateKind::Overridden' is collected and then filtered
  out by every consumer: naive intents take only 'Ordinary',
  hiderel intents take only 'Subscribee', merge extraction takes only
  'Ordinary'. It is dead weight.
- 'viewforest_with_saveroles' clones every 'ViewNode' in the buffer
  just to attach a role, once per save.

So the replacement is well motivated. The role data that is actually
load-bearing is small and fits naturally in a top-down traversal
context (see the sketch below).
* places where the vision contradicts the code
** SubscribeeCol does generate an instruction today
The vision says

  "If it recurses into a SubscribeeCol, the col itself doesn't
   generate instructions"

but in the current code the *membership* of a SubscribeeCol is exactly
what becomes the parent's 'subscribes_to' field
('collect_subscribees'). The col carries MSV semantics: absent col =
'Unspecified' (no opinion, disk value survives); present-but-empty col
= 'Specified(vec![])' (explicitly unsubscribe from everything).

So SubscribeeCol generates two distinct kinds of information:
- its member list -> the parent's 'subscribes_to' (a field value);
- each definitive subscribee-as-such child's visible content -> a
  hides/unhides signal for the subscriber (not a field value; it
  needs disk data and the subscriber's own post-save contains to
  resolve).

I assume the vision means only that the col generates no instructions
*about itself* (it has no gnode), which is true of AliasCol and
OverriddenCol too. But the spec should state explicitly that
SubscribeeCol emits the 'subscribes_to' field into its parent's map
entry, exactly as AliasCol emits 'aliases' and OverriddenCol emits
'overrides_view_of'. Do you agree?
*** DONE right!
** the full inventory of instruction-generating positions
You asked whether AliasCol and OverriddenCol are the only
instruction-generating collections. Per the current code the full
inventory is:

| position                                  | emits                                            | into whose map entry  |
|-------------------------------------------+--------------------------------------------------+-----------------------|
| definitive Active vognode, ordinary       | title, body, contains, or Delete                 | its own               |
| definitive Active vognode, ordinary       | Merge edit request (separate path today)         | its own (see below)   |
| AliasCol (under eligible writer)          | aliases (MSV)                                    | parent's              |
| SubscribeeCol (under eligible writer)     | subscribes_to (MSV)                              | parent's              |
| OverriddenCol (under eligible writer)     | overrides_view_of (MSV)                          | parent's              |
| definitive subscribee-as-such             | visible-content signal -> hides/unhides          | ancestor subscriber's |

Notably *not* on the list:
- IDCol / ID: display only. 'extra_ids' is never written from the
  buffer; it changes only via merge. (Matches your TODO note that IDs
  should not be writeable in the metadata view.)
- The six read-only RoleCols (Subscriber, Overrider, Hider, Hidden,
  HiddenInSubscribee, HiddenOutsideOfSubscribee): no instructions,
  and today no recursion into them either (see the matrix below).
- 'hides_from_its_subscriptions' is never emitted directly from any
  buffer position; it changes only via the subscribee-as-such signal
  path (and via merge).
** "reads the vnode and its vnode children, and that's all" is one level too shallow
A vognode's aliases live at grandchild depth (vognode -> AliasCol ->
Alias), as do subscribees and overriddens. So a per-node rule that
reads only the node and its children cannot collect those fields.

Two consistent ways to make it local; I believe you intend the
second, but the vision text mixes them:

- Each vognode reads two levels down (children for contains, and
  through any QualCol/PartnerCol children for the rest). The cols
  then emit nothing.
- Each vognode reads one level (itself + children -> title, body,
  contains, delete). Each *col*, when the traversal visits it, emits
  its field into its parent's map entry. This is the "merged into the
  map value keyed by their parent's ID" sentence in the vision.

I recommend the second: it keeps every emission strictly local (one
node and its immediate children), and the traversal context (below)
gives the col its parent's identity for free. The cost is that the
spec must define which parents are eligible (a col under an
indefinitive or subscribee-as-such or read-only-positioned vognode
must not emit; today those cols are simply never consulted because
field collection happens only at 'Ordinary' candidates).
*** DONE right, the second
* decisions remaining
** where does the procedure start and end?
QUESTION: Does 'local BFS instruction collection' replace only the
extraction passes (classify + candidates + the five collectors + the
hiderel-intent collector + same-ID reconciliation), keeping
'apply_hiderels_from_intents', disk supplementation, and the noop
filter as downstream stages over its output map?

My recommendation: yes, exactly that boundary. The downstream stages
are not tree problems; they are per-ID map problems plus disk I/O,
and they are already fairly simple. The output type of the new
procedure would feed 'apply_hiderels_from_intents' nearly unchanged.
Trying to fold disk supplementation into the traversal would
reintroduce async into a procedure that is otherwise pure, and purity
here is worth a lot for testing.
*** DONE
- apply_hiderels_from_intents feels like it should be rewritten
to be part of local BFS instruction collection.

- Disk supplementation should be separate.

- When you say 'noop fitler' do you mean the bit that discards
save instructions that would do nothing?
If so, yes, that should be downstream too.
**** DONE (Claude) on rewriting 'apply_hiderels_from_intents'
Agreed it should be rewritten; one constraint to flag. The
*emission* half is already inside collection in this design:
visiting a definitive subscribee-as-such emits a
'SubscribeeVisibility' intent into the subscriber's map entry. But
the *resolution* half -- turning that signal into hides/unhides --
cannot run during the traversal, because it needs (a) disk (the
subscribee's contains, the subscriber's hides) and (b) the
subscriber's own post-save contains, i.e. a *finished* entry
elsewhere in the same map. Running it mid-traversal would make
collection async and visit-order-dependent, undoing the main
simplification.
So I propose: delete 'apply_hiderels_from_intents'; write a new
resolution stage that consumes the 'SubscribeeVisibility' intents
from the completed map. New code, new types, same module family --
"part of local instruction collection" in that sense. OK?
***** TODO agreed
**** DONE (Claude) yes, that is what I meant by noop filter
'filter_wouldbe_noop_defineNodes', which discards Save instructions
identical to the pre-save in-Rust graph. It stays downstream, and
must in fact stay *after* disk supplementation, since unspecified
fields have to be restored before the comparison makes sense.
** what is an "instruction"? (granularity)
The vision says the map's values are *sets* of instructions. Today's
'NodeIntent' is a monolith: one struct carrying every field. Sets of
monoliths don't merge well. I see three options:

- per-field instructions (my recommendation) ::
  An enum roughly like:
    SetTitleAndBody { title, body },
    SetContains (Vec<ID>),
    SetAliases (Vec<String>),
    SetSubscribesTo (Vec<ID>),
    SetOverrides (Vec<ID>),
    SubscribeeVisibility { subscribee : ID, visible : Vec<ID> },
    Delete,
    (optionally Merge { acquiree : ID } -- see the merge section)
  Each buffer position emits zero or more of these. Merging into the
  map is trivial; conflict rules are statable per variant (below).
  Lowering map entry -> 'NodeIntent' (with MSV::Unspecified for
  absent variants) is mechanical, so everything downstream survives.

- whole-node intents, merged ::
  Keep 'NodeSaveIntent' but make every field MSV-ish, and define a
  merge operation (Unspecified yields to Specified; two Specified
  that disagree = error). Workable but the merge function is the
  complexity, and 'title' would need a fake MSV wrapping.

- whole-node intents, no merging ::
  Closest to today: the vognode's visit must gather everything,
  which forces the two-levels-down reads rejected above.

QUESTION: which granularity do you want? Everything below assumes
per-field.
*** DONE yes! per-field
** merge/conflict semantics inside the map
If values are sets of per-field instructions, the rules I propose:

- Two instructions of different variants for one ID: both kept.
- Two identical instructions: collapse silently (a set).
- Two same-variant instructions with different payloads for one ID:
  buffer validation error. (Today this is mostly precluded upstream:
  'validate_definitive_view_requests' and friends ensure at most one
  definitive instance per ID, and only definitive instances emit. So
  hitting this would indicate a bug, but the check is cheap and I'd
  keep it as an error rather than an unreachable!().)
- 'Delete' plus any Set* for one ID: error, matching today's
  "Cannot have both Delete and Save for same ID".
- 'Delete' plus 'Delete': fine, collapse.
- 'SubscribeeVisibility' signals: multiple allowed for on
  subscriber (one per subscribee branch). The existing
  per-(subscriber, content) conflict check
  ('validate_no_overlapping_subscribee_hiderel_conflicts') stays
  downstream, since it needs disk contains lists.
- 'SubscribeeVisibility' aimed at a subscriber whose map entry holds
  'Delete': resolution is a no-op, as today
  ('apply_hiderel_delta' ignores Delete intents).

QUESTION: any of these you'd decide differently?
*** ONGOING
Agree on everything except:

- "same-variant instructions with different payloads for one ID"
  This feels like it should be okay.
  For instance, multiple subscribees-as-such
  should be able to influence the subscriber's hide relationships.
  Maybe that's the only variant that should be mergeable; I'm not sure.

  Oh wait, maybe you covered merging the 'hides' change instructions
  from multiple subscribees under 'SubscribeeVisibility'.
**** ONGOING (Claude) yes, that case was covered
'SubscribeeVisibility' is deliberately the one same-variant exception
that combines freely: a subscriber's map entry may hold several, one
per subscribee branch. They never conflict at insertion time, because
each names a different subscribee. Genuinely contradictory opinions
about one (subscriber, content) pair are caught downstream by
'validate_no_overlapping_subscribee_hiderel_conflicts', which needs
disk contains lists and so cannot run during collection.
For the Set* variants, a payload conflict can only arise from two
definitive instances of one ID, which upstream validation already
forbids -- so keeping it an error costs nothing and makes collection
catch upstream regressions instead of silently picking a winner.
***** TODO would it be awkward
to use an enum that distinguishes 'combineable' instructions from
'exclusive' instructions? So far this would be the only combineable one,
but others might come along -- and even if they don't,
I like to keep as much logic as possible in types rather than functions,
because they're easier to follow.
****** TODO (Claude) not awkward, and there is an even more typed encoding
Two ways to put combineability in types:

- wrapper enum :: 'enum Intent { Exclusive (ExclusiveIntent),
  Combineable (CombineableIntent) }', map value =
  'Vec<Intent>'. Insertion matches the wrapper: Exclusive errors if
  an intent of the same variant is already present (a runtime
  discriminant comparison); Combineable just pushes.

- slot struct (my recommendation) :: the map *value* is a struct
  with one 'Option' field per exclusive intent kind and one 'Vec'
  field per combineable kind. Then "at most one of each exclusive
  kind" is not a rule a function checks against a Vec; it is the
  shape of the type. Inserting into an occupied slot is the only
  error case, and combineability is literally visible as Vec vs
  Option. It also echoes the MSV idiom: an empty slot lowers to
  'MSV::Unspecified'.

The slot struct leaves exactly one cross-slot rule that the type
cannot express: Delete excludes the Set* slots. (It could be typed
too -- an enum over { Delete, Edit (slots) } at the top of the value
-- but insertion across that boundary still needs a check, so the
enum buys little; the plan pins one choice.) Per-position *emission*
stays a small enum either way: a visit returns a list of
(target ID, intent) pairs, and insertion into the accumulator
enforces the slot discipline. Details in 3_plan.org; veto there if
the slot struct is not what you wanted.
*** ONGOING language
We need to be careful to keep a clear distinction
between merging nodes and merging instructions.
**** ONGOING (Claude) proposed vocabulary
Reserve 'merge' exclusively for node-merge (the 'EditRequest::Merge'
/ 'Merge' triple machinery). For putting instructions into the map,
the plan and the code will say 'combine' (e.g. a function named
something like 'combine_instruction_into_map'), never 'merge'. I will
state this in the plan's terminology section.
***** TODO no, that's dangerous
That effectively asks the user to remember that 'combine' means
'combine instructions' and 'merge' means 'merge nodes'.
Just say 'merge nodes' or 'merge instructions'.
And where we are talking about a noun phrase, 'nodeMerge' or 'instructionMerge'.
(This means updating a lot of function names. That's fine.
I care more about readable code than a readable diff.)
****** TODO (Claude) adopted; one scope question
3_plan.org carries a rename inventory ('Merge' -> 'NodeMerge',
'merge_instructions' -> 'nodeMerge_instructions',
'EditRequest::Merge' -> 'EditRequest::NodeMerge', the 'merge/'
module names, etc.). The scope question: I plan to rename Rust
identifiers only, leaving client-facing wire/metadata strings
unchanged -- '(merge ID)' in headline metadata, the "merge" the
user types. In user-land only nodes can merge, so the short word is
unambiguous there, and renaming it would touch the elisp client and
saved-buffer syntax. Flag in the plan if you want the wire strings
renamed too.
** ordering
A bare 'HashMap<ID, HashSet<Instruction>>' loses buffer order.
Today's 'SameIdReconciledNodeIntents' deliberately keeps first-
appearance order, and downstream disk supplementation and persistence
consume an ordered Vec. Even if nothing strictly requires the order,
nondeterministic instruction order would make logs, tests, and
debugging worse.

Recommendation: keep what 'SameIdReconciledNodeIntents' already does
-- a map plus a first-appearance order vector. (Or an IndexMap-style
type, but the two-field struct is already idiomatic here.)
*** DONE sure
** BFS, preorder, and what must flow down
The vision says "a simple BFS preorder procedure". BFS and preorder
are different traversals; preorder normally means DFS. Since
emissions merge into a map keyed by ID, *visit order cannot matter at
all* -- the only real requirement is that ancestor context flows
downward. So:

- Recommendation: plain recursive DFS preorder, like the rest of the
  codebase (e.g. 'collect_savenode_candidates' itself,
  'update_buffer/complete.rs'). A queue-based BFS buys nothing here
  and the codebase has little precedent for it.
  TODO (Jeff speaking): Agreed.
- Whatever you choose, may I suggest the project term be
  'local instruction collection', dropping BFS from the name unless
  you want literal BFS for some reason I'm missing? The vision asks
  for absolute specificity of terminology, and "BFS preorder" is the
  one phrase in it that doesn't denote a specific thing.
  TODO (Jeff speaking): Agreed.

The context that must flow down (this is what *replaces*
'classify.rs', which derived the same facts by walking up):

- the nearest vognode ancestor's ID, and whether that ancestor is an
  eligible writer (definitive + Active + ordinary position). Cols
  need this to emit, or to stay silent.
- whether the current position is inside a SubscribeeCol whose parent
  is an eligible writer, and if so the subscriber's ID. This is the
  subscribee-as-such case, including the at-most-one-writer-per-ID
  guard currently in 'subscriber_instance_is_definitive' (the
  comment in 'subscribee_hiderel_intents.rs' explains why losing it
  would let one subscriber receive contradictory hide edits).
- whether the current position is inside a read-only col (emit
  nothing, and today: don't even recurse).

That's a three-or-four-variant context enum passed as a function
argument. No tree cloning, no upward walks, no roles computed that
nobody reads.
*** ONGOING I would add
"the nearest vognode ancestor's ID" is not enough;
we need to be sure the path to that ancestor makes sense.
Somewhere in the code there is a table describing permissible ancestries,
and there already exist functions to check parts of a viewtree against it.
**** ONGOING (Claude) found them; here is how I propose to use them
The table is 'required_ancestry' in 'server/update_buffer/ancestry.rs'
(checker: 'col_is_generalized_orphan'), from the
propagate-death-leafward work -- but that lives on the *update* path.
On the *save* path, the corresponding checks already run before
extraction: 'validate_local_structure' in
'server/from_text/buffer_to_viewnodes/local.rs' (per-node shape
checks -- alias under AliasCol, col parent kinds, etc.), called by
'find_buffer_errors_for_saving'.

Proposal: collection does not re-verify ancestries. It assumes
'find_buffer_errors_for_saving' passed (stated in its header
comment, as agreed under "losing the upstream guarantees silently"),
and the downward context carries the only path facts it needs
(eligible-writer parent; enclosing subscriber). The shape checks we
agreed to move out of the collectors land in
'validate_local_structure', where their siblings already live. If
you want belt and suspenders, collection can also debug_assert the
ancestry of any col it emits from.
***** TODO yes, that's good
and no suspenders needed
** the emission/recursion matrix
Drawn from 'collect_savenode_candidates' and the collectors. Each row
says what the current code does; please confirm or amend each "keep?"
cell. "emit" means: may contribute instructions; "recurse" means: its
descendants are still searched for instruction owners.

| kind at this position                         | emits?                              | recurses?                   | keep?     |
|-----------------------------------------------+-------------------------------------+-----------------------------+-----------|
| Active vognode, definitive, ordinary          | yes: title/body/contains, or Delete | yes                         |           |
| Active vognode, indefinitive, ordinary        | no                                  | yes                         |           |
| Active vognode, definitive subscribee-as-such | yes: visibility signal only         | yes (children are ordinary) |           |
| Active vognode under read-only col            | no                                  | NO                          | see below |
| Inactive vognode                              | no (but parent's contains keeps it) | no                          |           |
| Phantom::Diff                                 | no                                  | NO                          |           |
| Phantom::Deleted                              | no                                  | yes                         |           |
| Phantom::Unknown                              | no                                  | yes                         |           |
| DeadScaffold                                  | no                                  | yes                         |           |
| AliasCol                                      | yes: aliases for parent             | (children are leaves)       |           |
| IDCol / Qual::ID / Qual::Alias                | no                                  | no                          |           |
| SubscribeeCol                                 | yes: subscribes_to for parent       | yes                         |           |
| OverriddenCol                                 | yes: overrides for parent           | yes?                        | see below |
| Subscriber/Overrider/Hider/Hidden cols        | no                                  | NO                          |           |
| HiddenInSubscribeeCol, HiddenOutsideOf...     | no                                  | NO                          |           |
| BufferRoot                                    | no                                  | yes                         |           |

Three rows deserve attention:

- Active vognode under a read-only col: today its whole subtree is
  invisible to extraction. If a user somehow obtains a definitive
  node there and edits it, the edit is silently discarded. Your
  existing entry in 'TODO/problems.org' ("Save validation and
  extraction use different edit-request surfaces") is exactly this
  family of problem. The rewrite is the natural moment to decide:
  silently ignore (status quo), or emit a buffer warning, or a
  validation error. I'd vote warning at minimum.
- OverriddenCol members: today they're classified ('Overridden' role,
  candidates collected) and then *everywhere discarded* -- no
  recursion value, no instructions. So effectively OverriddenCol's
  membership is read (for the parent's 'overrides_view_of'), but its
  members' subtrees are dead ends. If that's intended, the new
  procedure gets simpler (treat members as inert); if not, this is a
  latent bug to record.
- Phantom::Diff: no recursion today, so a definitive node nested
  under a diff phantom is also silently ignored. Probably correct
  (diff mode is read-only-ish) but worth a deliberate yes/no.
*** DONE Active vognode under read-only col
It emits if it is definitive, for the gnode it represents,
just like any other vognode.
But it emits nothing at ancestor that owns the Col that owns it.
**** DONE (Claude) agreed; noting it is a behavior change
Today such a subtree is invisible to extraction entirely, so its
definitive members' self-edits are silently discarded. The new rule
(emit for self, never for the col's owner) goes on the
expected-differences allowlist for the differential tests.
*** DONE Inactive vognode
Do recurse into it.
It never spawned children, so none of its children should have ParentIs=Affected.
But thee user might have put some definitive children there to recurse into.
*** DONE actually just recurse into everything
This is for the same reason -- the user can attach independent nodes anywhere.
**** DONE (Claude) agreed -- recursion becomes unconditional
This kills the matrix's recurse column entirely; only emission stays
conditional on (kind, context), which is a real simplification.
One consequence to confirm: taking "everything" literally includes
'Phantom::Diff' and 'Phantom::Deleted' subtrees, so a definitive
vognode found beneath a phantom now emits for its own gnode (it
still cannot affect the phantom). My default is the literal rule;
flag it if diff mode deserves an exception.
*** DONE OverriddenCol members
The Col that owns them does emit override instructions for its parent, right?
**** DONE (Claude) yes
'collect_members_from_child_relation_col (.., RoleCol::Overridden, ..)'
turns the col's membership into the parent's 'overrides_view_of',
with the usual absent-vs-empty MSV semantics. That is preserved. The
members themselves were dead ends only as instruction *owners*; with
recurse-into-everything, a definitive member now also emits for its
own gnode.
** rules under a subscribee-as-such
The locality rule needs care here, because a subscribee-as-such is a
definitive Active vognode whose *own* fields must mostly not be
written:

- title/body edits there are forbidden
  ('validate_no_title_or_body_edit_in_subscribeeAsSuch', which
  compares against disk). QUESTION: keep this as a pre-pass (it is
  async, needs disk), or have collection emit a "claimed title/body"
  fact that a downstream validation checks against disk? I lean to
  the latter; then the validation stops needing the role forest, and
  'SaveAuthority' can die.
- a QualCol/PartnerCol child of a subscribee-as-such (say the user
  pastes an AliasCol under one): today ignored, because field
  collection only happens at 'Ordinary' candidates. Under the
  col-emits-into-parent rule, a naive implementation would write the
  subscribee's aliases! The eligibility context must prevent this.
  Worth an explicit test.
- ordinary definitive *children* of a subscribee-as-such do emit
  their own instructions today (their role is Ordinary). Keep.
*** ONGOING yes to all, minus one correction
You wrote
```
a subscribee-as-such is a
definitive Active vognode
```
but it's not necessarily definitive. I hope. I believe when those are created,
they are created indefinitive.
**** ONGOING (Claude) right -- correction accepted, with one refinement
They are indeed created indefinitive (the local-view-update vision
says of SubscribeeCol: "children it creates are indefinitive").
The refinement: only a *definitive* subscribee-as-such emits the
visibility signal. That is today's behavior too --
'collect_savenode_candidates' skips indefinitive ones -- so an
indefinitive subscribee-as-such emits nothing, and deleting children
beneath it changes no hide relationships. My matrix row described
only the emitting case; the plan will carry both rows
(definitive: emits the signal; indefinitive: silent; both recurse).
I assume keeping the definitiveness requirement is what you want,
since it parallels how an indefinitive ordinary vognode ignores
edits to itself and its collections. Confirm?
***** TODO agreed
** should Merge join the same map?
Today merges are extracted by a second traversal over the same
candidates ('instructiontriples_from_merge_candidates'). Your
TODO.org already contains "move 'merge' into the type containing
'save' and 'delete'". The new instruction enum is the obvious home:
'Merge { acquiree : ID }' emitted by the same visit that emits
title/body. The downstream expansion into the
preserver/acquirer/delete triple (which needs disk) would stay where
it is, just fed from the map instead of from a tree walk.

QUESTION: in scope for this change, or a follow-up? It would let us
delete 'SaveAuthority' entirely (its only remaining consumers are
merge extraction and the subscribee title/body validation), which is
when the old machinery actually disappears. My vote: in scope, since
otherwise classify+candidates must survive in parallel just for
merges, and the simplification mostly fails to materialize.
*** DONE love it, yes
** what happens to the existing collector-embedded validations
The collectors currently throw errors like "AliasCol has non-Alias
child" and "SubscribeeCol has unexpected non-vognode child". The
local procedure can keep these (each visit still sees its children),
but a cleaner separation might move structural-shape checks into
'find_buffer_errors_for_saving', leaving collection total (never
erroring on shape). I weakly prefer moving them, since
"validation happens before extraction" is already the architecture's
stated posture, and a total collection function is easier to reason
about. But it does mean the same shapes get pattern-matched in two
places. Your call.
*** DONE yes, let's separate them
The performance cost seems minimal, and despite the redundant shape-checking,
the cognitive burden seems lower that way.
* dangers
** behavior drift in the membership predicates
The highest-risk part of this rewrite is not the traversal; it is the
small predicates deciding membership, which encode a lot of
hard-won policy:

- 'member_counts_for_relation_collection'
  (parentIs == Affected, not a would-be diff phantom, not toDelete);
- the contains filter in 'collect_contents_to_save_from_children'
  (same three conditions, plus role Ordinary, plus Inactive vognodes
  included unless 'inactiveNode_is_phantom');
- 'dedup_vector' applied to aliases/subscribees/overriddens but NOT
  to contains (contains may legitimately repeat? -- QUESTION: is
  that deliberate? If contains is deduped elsewhere I haven't found
  it, and 'reconcile' in update_buffer suggests repeats are legal);
- MSV absent-vs-empty semantics per col.

Recommendation: the new code should *call these same predicates*, not
re-derive them. Move them somewhere both old and new code can reach
during the transition, and port the tests that pin them down.
*** DONE also make sure the tests for those
are complete -- that they test enough to pin the behavior.
**** DONE (Claude) will do
The plan will include an audit step: for each predicate, enumerate
the conditions it encodes and check a test exists per condition,
adding any missing ones before the rewrite starts.
*** ONGOING (Claude) one question above went unanswered: contains dedup
From the parent section: 'dedup_vector' is applied to aliases,
subscribees, and overriddens, but not to contains. Is that
deliberate? My default for the plan: preserve the asymmetry exactly
(repeated content under one parent stays legal, since repeated nodes
are a supported if dangerous feature), and document it at the
predicate. Say the word if you'd rather dedup contains too.
**** ONGOING I think that's a model violation caught at validation.
If I'm right, then wherever we would have deduped,
let's insert a comment explaining why it's not necessary.
(If I'm wrong let me know how it actually works!)
***** ONGOING (Claude) verified: right for contains and the relation cols; one gap: aliases
How it actually works, per
'server/from_text/buffer_to_viewnodes/local.rs':

- contains: 'nonignored_children_have_distinct_ids' runs for every
  TrueNode and Phantom (via 'validate_gnode_identity_and_structure')
  and rejects two Affected content children sharing an ID. It skips
  parentIs!=Affected children and diff-phantom members -- exactly
  the children collection also skips -- so collection sees a
  duplicate-free list. You are right; the missing 'dedup_vector' was
  never needed, and the new code gets the explanatory comment.
- SubscribeeCol, OverriddenCol (and the read-only cols):
  'relation_col_children_have_distinct_ids' runs for each via its
  validator, so their 'dedup_vector' calls are redundant today.
  Comment instead of dedup, as you said.
- AliasCol: 'validate_aliascol' checks child kind and
  col-uniqueness-among-siblings but NOT distinctness of alias texts.
  So today two identical alias lines pass validation and
  'dedup_vector' silently collapses them. To make the no-dedup rule
  uniform, the plan adds an alias-text distinctness check to
  'validate_aliascol', making duplicate aliases a validation error
  like duplicate subscribees. Note this is a small user-visible
  behavior change: duplicate alias lines currently save fine
  (silently collapsed); they would now bounce the save. Veto in the
  plan if you would rather keep the silent collapse for aliases.
****** TODO disagree
Aliases should be silently dedup'd, because aliases are unordered.
I don't want to harsh the user's vibe
by complaining that they repeated themselves.

Similarly, any other defining col (I think only OverriddenCol qualifies)
should have repeats silently dedup'd, rather than squawking.
******* TODO (Claude) adopted; note this removes two existing checks
The plan now says: defining cols silently dedup, never squawk.
Three things to know about what that means in the code:

- There are three defining cols, not two: AliasCol, OverriddenCol,
  and SubscribeeCol ('subscribes_to' is writeable through it). You
  named only OverriddenCol; I have included SubscribeeCol as well.
- For SubscribeeCol and OverriddenCol this is a behavior *change*,
  not a preservation: both currently bounce the save on duplicate
  members ('relation_col_children_have_distinct_ids', called from
  'validate_subscribeecol' and 'validate_relation_col'). Honoring
  the vibe principle means deleting those calls for the defining
  cols. Only aliases keep exactly today's behavior (silent
  collapse; no validator existed).
- Dedup happens at the col's emission, via the existing
  'dedup_vector', which preserves first-occurrence order -- so
  SubscribeeCol's display order survives deduplication.

Unchanged, deliberately: duplicate *contains* members remain a
validation error ('nonignored_children_have_distinct_ids'), per
your earlier model-violation answer. Also unchanged: the six
read-only cols keep their duplicate-member checks. Their members
are inert on save, so those checks have no save semantics; but by
the same vibe principle a user who duplicates a line in a read-only
col gets their save bounced today. Quieting them too would be
consistent; I left them alone as out of scope. Flag if you want
them quieted.
** equivalence testing before deleting the old path
'extract_nonmergeSavePlan' (the legacy wrapper) and the old pipeline
already exist and are tested. The new procedure can be built
alongside and compared: for every viewforest the test suite
constructs, assert old and new produce the same 'NonmergeSavePlan'
(and same 'Vec<Merge>' if merge is in scope). Only then delete the
old path. This is cheap insurance against the predicate-drift risk
above, and the vision's own claim ("in principle it could be a
simple BFS preorder procedure") becomes a checked theorem instead of
a hope.
*** DONE They actually might not produce the same thing
because there were, if your analysis was correct,
places we did not recurse during save collection that this new plan will do.
But we can still make similar tests, and verify that the two procedures
only differ in the ways that they should.
**** DONE (Claude) agreed
The differential test carries an explicit allowlist of expected
differences -- exactly the new-recursion-surface emissions (members
of read-only cols, descendants of inactive vognodes and phantoms)
-- and any difference outside the allowlist fails the test.
** mixed map values: field values vs unresolved signals
The map will contain both real field assignments and
'SubscribeeVisibility' signals that *cannot* be resolved during
collection even in principle: resolution reads the subscribee's disk
contains, the subscriber's disk hides, and the subscriber's own
post-save contains (i.e. another entry of the same map). So the
"instructions" in the map are really intents at two maturities.
That's fine -- the vision already says signals merge into the map --
but the type should be honest about it (e.g. name the enum
'InstructionIntent' or keep 'intent' in its name), and the lowering
stage must process Set* variants before resolving visibility
signals. If you'd rather keep the map values uniform, the
alternative is returning the signals beside the map (what
'SaveExtraction' does today); slightly less elegant, slightly more
typed.
*** DONE no, I agree
The types should be honest.
More types are in general better than more procedures.
(I mean there's always a tradeoff but
procedures get hairy much faster than types.)
** losing the upstream guarantees silently
Several invariants the collection relies on are enforced upstream
('find_buffer_errors_for_saving', 'add_missing_info_to_viewforest'):
every node has a PID and source; at most one definitive instance per
ID; at most one definitive-view-request per ID. The new procedure
should state its assumptions in its header comment the way
'find_buffer_errors_for_saving' does, because a map-merge that
"just works" can mask upstream regressions (two definitive instances
would merge instead of erroring, unless the same-variant-conflict
rule above is kept as an error).
*** DONE absolutely, it should
* a concrete sketch, to argue about
Pure function, sync, no disk. Names are provisional.

#+begin_src text
enum LocalContext {            // what flows down
  TopLevel,                                    // children of BufferRoot
  UnderWriter        { parent : ID },          // parent is an eligible writer
  UnderNonwriter,                              // parent exists but cannot be written
  InSubscribeeCol    { subscriber : ID,        // direct children are subscribees-as-such
                       subscriber_is_definitive : bool },
}

enum Instruction {             // map values; see granularity question
  SetTitleAndBody { .. }, SetContains ( .. ), SetAliases ( .. ),
  SetSubscribesTo ( .. ), SetOverrides ( .. ),
  SubscribeeVisibility { .. }, Delete, Merge { .. } }

struct CollectedInstructions {  // the output
  order  : Vec<ID>,             // first appearance in buffer
  by_pid : HashMap<ID, Vec<Instruction>>, } // Vec, checked for conflicts on insert

fn collect_instructions_locally (
  forest : &ViewForest,
) -> Result<CollectedInstructions, String>
// DFS preorder. At each node, match (kind, context):
//   emit into the map per the matrix above,
//   compute each child's LocalContext,
//   recurse or not per the matrix.
#+end_src

Then downstream, unchanged in spirit:
'apply_hiderels_from_intents' (consumes the visibility signals),
disk supplementation, noop filter, foreign-instruction validation.
'classify.rs', 'DefinenodeCandidate', 'SaveAuthority', and the five
per-field collectors are deleted; merge extraction reads 'Merge'
instructions from the map.
* minor
** DONE The vision filename term: you ask that we always say
  'local BFS instruction collection'. Happy to, though see the
  BFS-vs-preorder note; if you accept DFS preorder, perhaps the
  canonical term becomes 'local instruction collection'.
*** (Jeff speaking) Yes, I agreed earlier (above) to this.
** DONE Typo in the vision: "OverridddenCol"; also the asterisk quoting in
  "and *that's all"." is unbalanced. Only worth fixing because the
  file is the canonical spec.
*** (Jeff speaking) Fixed.
