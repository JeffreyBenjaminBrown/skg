If any of these are questions rather than requests for code,
please answer inline,
marking the headline of your answer 'TODO'.

If any of these were already done,
please mark them 'DONE' in TODO.org
(and here). Don't otherwise modify what I've written here,
but feel free to add subheadlines anywhere, at any depth,
somehow marking them for your voice.
* DONE BUG : aliases should be merged, not added
Aliases are being added rather than merged to the view.
(Happens e.g. when saving a buffer with git diff mode on
that already has aliases shown.)

Code for merging collections is already part of
the view update exeecution path.
It might be called correction, or completion.
** Claude : fixed
Your suspicion was right that the merging code exists -- the alias
reconciler ('reconcile_alias_col_children') dedups fine. The bug was
upstream: in diff mode, 'process_activeNode_diff'
(server/to_org/render/diff.rs) prepended an EMPTY AliasCol whenever
the alias list changed in git, without checking whether the node
already carried one (a saved diff-mode buffer always does). Both
cols then each reconciled to the full alias list, so every alias
appeared twice. Now the existing col is reused; same guard added for
the IDCol, which had the identical latent bug. Regression test:
tests/git_diff_view/aliases/view.rs,
'test_saving_a_diff_view_with_aliases_shown_does_not_duplicate_them'
(verified to fail without the fix).
* goto id should work from a raw .skg file
If there's only one ID on that line,
it should go to it even if it's not on the ID.
If all IDs on that line are left of point we should visit the closest;
if all are on the right, ditto.
(If it's between two, that's ambiguous, so just say so and visit nothing.)
* The skg diff report should report what vanished nodes used to be.
The kind of node that would give
"Parent references unknown node."
should be investigated in the git history of all repos.
If it's not there, say that;
otherwise say at what commit it vanished,
and what it was connected to (in every possible way) then.
* Why does this search fail?
** Searched "rt"-style for
"dens.* AND (mean.* OR info.* OR know.*)"
** got this error
Error searching index: An invalid argument was passed: 'RegexQueryError: regex parse error:
    (mean.*
    ^
error: unclosed group'
** TODO Claude : answer -- regex mode has no operator-level grouping
In regex+operators mode ('build_regex_query',
server/dbs/tantivy/search.rs), the query is split on WHITESPACE; the
keywords AND / OR / NOT and the +/- prefixes are consumed as
operators, and EVERY OTHER piece must be a valid regex on its own.
Your "(mean.*" is such a piece: the "(" opens a regex group that
never closes, hence Tantivy's RegexQueryError. Parentheses as
operator-level grouping exist only on the non-regex QueryParser path
(plain "t"-style with operators); the regex path never got a grouping
layer.

You don't need one for this query, though: regexes have alternation
natively, so grouping can live INSIDE a single piece. This does what
you meant:

  dens.* AND (mean|info|know).*

(one piece per required token; both pieces adjacent to AND become
MUST). If you want operator-level parens in regex mode anyway --
e.g. for mixing MUST_NOT into a group -- say so; it needs a small
boolean-expression parser over the pieces in
'regex_pieces_with_operators', where today's grammar is flat.
* C-c g i should work from anywhere.
Regardless of buffer type.
* If indefinitive, add a B to that symbol where body is hidden.
* when staging new files (C-c t [a|A]), explain if git diff mode off
rather than acting like the user did it right
and stating that 0 files were added
* BUG :: make-content cleanup -> collateral view not found
** the bug
Change a link relationship into an ordinary content relationship. This orphans the linking node. Visit the linking node. If it is an island (ignoring its out-link), delete it. The save works, but a (warning?) buffer complains that some collateral buffer does not exist.
** TODO Claude : could not reproduce; need the verbatim message
I mirrored the shape (P contains linking node L, whose title is one
big textlink to X) and tried deleting L via a save while OTHER
registered views still held L three ways: as a child of an open view
of P, as the ROOT of a second content view, and as the root of a
search view. All complete with no errors or warnings; the collateral
rerenders succeed. That attempt is kept as a regression test
(tests/collateral_delete.rs).

Two things would pin it down:
1. The VERBATIM complaint. Candidates in the code are "Collateral
   view <uri>: no viewforest found" and "View <uri>: no viewforest
   found" (save vs rerender-all paths) -- but by construction both
   look unreachable (the uri list and the forest map are the same
   structure, read back-to-back) -- versus "Collateral view <uri>:
   <some inner error>", which would mean a collateral RERENDER failed
   and the interesting part is the inner error.
2. Whether git diff mode was on at the time (my attempts ran with it
   off), and whether the complaining buffer appeared at the save
   itself or at a later rerender-all (diff-mode toggle / source-set
   switch).
If you can paste the exact message (or reproduce with
data/logs/server.jsonl timing on), I can finish this.
* Warn the user when they make dead links.
* Show the herald it is responsible for next to each field in the metadata.
That is, when the user uses C-c v m,
each value that produces a herald should contain that herald,
colored per herald rules, in (uncolored) parentheses,
after the rest of the title.
