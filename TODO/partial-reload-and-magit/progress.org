* partial reload and magit -- progress

Execution log for =3_plan.org=. Per =when-to-stop.org=: the plan is left
as-is; deviations in *how* goals are met are recorded here. Categories:
what's done and went well; changes to the plan's envisioned order;
what's imperfect about what's done; choices determining the future.

* execution of '5_plan.org'
** integration and baseline
- Rewrote '5_plan.org' as a unitary declaration and committed it as
  'd78cfdf1'.
- Merged main through '971e17b5' as '311a5c36'.  The only textual conflict,
  in 'server/save.rs', was resolved by retaining both the reload no-write
  seam and main's hoist-approval boundary.
- Focused Rust reload/scalar/telescope suites, all 308 Emacs tests and the
  Neovim suite passed after the merge.  The two TypeDB-backed reload parity
  tests passed once the standard development servers were running.

** authoritative source inventory and pre-parse selection (complete)
- Replaced the independently mutable source 'HashMap' and 'source_order'
  with 'SourceCatalog': one ordered lookup catalog which retains each raw
  configured path and resolved physical-directory identity.  Mixed named
  and unnamed sources now retain declaration/privacy order.
- Config load now creates owned directories, validates foreign directories,
  canonicalizes source identity and rejects two entries which alias the same
  physical directory, naming both entries in the error.
- Added one exact direct-child classifier.  Reload no longer treats an
  arbitrary descendant or a path-prefix sibling as a source file.
- Added the shared path-selection boundary in 'source_files.rs'.  Bulk load
  and one-pid load now choose owned same-filename candidates before reading
  YAML, so a malformed ignored foreign collision is opaque.  The warning
  carries winner and loser paths internally, while the existing aggregate
  telescope report still receives its source-level form.
- Diff snapshot enumeration now ignores nested '.skg' files and applies the
  same path selection before parsing full HEAD, index and worktree snapshots.
  Focused source, reload-path, malformed-collision and 28 diff-analysis tests
  pass.
- The connection handshake now carries the server's ordered, normalized source
  inventory.  Emacs and Neovim cache that inventory and use it in preference to
  reparsing TOML; their TOML fallback also handles unnamed sources correctly.
- Ignored owned/foreign collisions survive initialization and rebuild as
  structured telescope warnings.  The handshake makes them prominent and
  persistent in a dedicated Emacs or Neovim warning buffer, including the
  winning and ignored paths.
- Validation after this slice: all 176 Rust library tests under nextest, all
  312 Emacs tests, and the complete Neovim suite pass; 'git diff --check' is
  clean.  (The four source/privacy tests which race through shared global graph
  state when run in one process pass under the repository's required nextest
  process isolation.)

** selected-store and index generations (complete)
- Replaced the graph-only 'ArcSwap' payload with one immutable
  'SelectedStoreState': graph snapshot, monotonic graph generation, BLAKE3
  selected-path manifest, per-path index state, cyclic-root slot and derived
  store health now publish as one value.  Existing graph readers retain a
  cheap 'Arc<InRustGraph>' from that publication.
- Initialization and rebuild build the manifest from the exact retained bytes
  they parsed.  Save and node merge update it from their prepared serialized
  bytes; ignored foreign files are not falsely called selected.  The
  connection handshake now exposes graph generation, path outcomes and store
  health, and both clients cache that authoritative baseline.
- The Tantivy background writer now assigns monotonic generations and retains
  'Pending', 'Committed', 'Reconstructed(reason)' or 'Failed(reason)' for each.  Exact and captured-
  through waits are available; enqueue failure and worker panic become failed
  generations.  A transition publishes its awaiting-index paths before the
  task can run, and the worker promotes precisely those paths by atomic CAS.
- Save/reload no longer acknowledges before the exact Tantivy generation.
  Node merge drains older queued work before its direct write.  Whole-corpus
  whitespace normalization now takes the graph writer lock before scanning,
  drains the captured older index generation and publishes graph plus manifest
  together.
- TypeDB recovery now reconstructs from the immutable candidate graph rather
  than rereading possibly bad disk.  Reload additionally attempts restoration
  of the prior graph projection and marks TypeDB poisoned if neither complete
  reconstruction succeeds.
- A failed incremental Tantivy write now reconstructs the existing index in
  place from the exact immutable graph attached to that queued generation,
  including its complete cached context labels.  Only a failed reconstruction
  poisons search; later queued deltas remain FIFO after the repair.  Reload
  acknowledgement reports that fallback prominently.

** stable reload transaction (complete)
- Path parsing before the writer lock resolves identifiers only.  Under the
  lock, reload captures every possible section path as exact bytes or absence,
  applies owned/foreign selection, parses/folds, and rereads every captured
  path before the first store mutation.  A mismatch changes no store.
- Each legal telescope updates the selected manifest from its captured bytes;
  fatal telescopes keep both their prior graph node and selected digests.  The
  final graph undergoes pure distinct-ID-claim and override validation.
- An extra-ID change invokes the complete normalized-corpus fallback, validates
  it, rechecks its complete selected manifest, and derives graph definitions by
  comparing old and new graphs.  A test proves that claiming an untouched
  node's primary ID rejects the transaction without advancing graph or
  manifest.
- Validation after these changes: 180/180 Rust library tests, the combined
  135 save/source integration tests, all 312 Emacs tests and the complete
  Neovim suite pass.

** request-scoped response protocol (complete)
- Every foreground operation now receives a fresh connection-local request
  ID.  The server echoes it on every frame, names the frame kind independently
  of its payload type, and marks exactly one terminal frame.  Search's snapshot
  response is the sole same-ID continuation; authorization retries are new
  requests.
- Both clients retain handlers in per-request records and serialize complete
  operations through a FIFO coordinator.  A stale or unknown response cannot
  reach a newer like-typed handler.  Terminal cleanup advances the queue once,
  including when the selected handler itself errors; parser state is reset
  only for a new or abandoned connection.
- Rerender privacy challenges are terminal rather than beginning a synthetic
  empty rerender stream.  The client unlocks immediately and schedules any
  approved retry as a new request.
- The wire contract and its search continuation are recorded in
  =api-and-formats.md=.  Validation after this slice: 182/182 Rust library
  tests in one serial process, all 314 Emacs tests and the complete Neovim
  suite pass; =git diff --check= is clean.

** durable incident identity and explicit ID selection (complete)
- Request records can now carry a durable incident UUID.  Both clients copy
  it into the request, the server echoes it on every frame and clients reject
  a frame whose incident identity differs.  Search continuations retain it;
  retries may use a new request ID without losing the episode identity.
- =skg-reload-from-id-stack= opens a transient copy of the canonical stack
  with a buffer-local, single-keyword Org TODO sequence.  Standard S-left and
  S-right mark =TO-RELOAD=; C-c C-c submits deduplicated IDs, while C-x C-s
  explains that this buffer cannot save or alter the real stack.
- Reload requests now accept paths and primary or extra IDs together.  IDs
  resolve against the selected graph before the writer transaction and name
  their complete possible cross-source telescope.  The terminal response
  reports each requested ID, primary PID, considered paths and acknowledged
  or rejected status.  The selector clears only acknowledged marks and keeps
  rejected marks with transient reason overlays.
- Expected reload failures now terminate as a failed =reload-paths= result
  rather than bypassing the request's handler through the generic error type.
  Validation after this slice: 184/184 serial Rust library tests, all 318
  Emacs tests and the complete Neovim suite pass.

** candidate observation and reload-batch brackets (complete)
- Replaced stamp-based discovery with one nonrecursive Emacs file-notify watch
  per normalized source.  Watch callbacks and raw-file saves enqueue path
  names only.  A sequence-tagged path map coalesces events, preserves a newer
  event which lands during an in-flight predecessor and groups the dispatched
  snapshot under one incident.
- Connection, watcher-recovery, Magit and manual scans request a server-side
  full sweep.  It compares exact BLAKE3 selected manifests, including explicit
  additions and deletions; a test proves a same-size rewrite is detected.
- Added process-wide, overlapping reload-batch tokens.  A token is owned by
  the connection which opened it and is released only by its matching end or
  EOF.  While any token is active, reload returns visible =deferred=true=
  data and selects no bytes.  Automatic observations retry with the same
  incident; explicit ID selections retain their marks and retry likewise.
- =data/bash/pull-all.sh= now keeps a small direct TCP control connection open
  across its serial pulls and closes the token from its EXIT trap.  Server
  unavailability remains nonfatal.  A live smoke test opened and closed a
  token on port 1731 and separately verified the deferred incident response.
- Validation: =bash -n= is clean, all 186 serial Rust library tests and
  321 Emacs tests pass, and the complete Neovim suite passes.

** dirty-view safety and scalar-gated reload presentation (complete)
- Added a conservative client worktree gate. A raw direct-source =.skg= save
  or a Magit command which may write a repository containing a configured
  source is refused while any Skg view is dirty, naming every such view.
  Read-only and proven index/ref-only commands (including stage, unstage and
  commit) remain available; unknown Git commands fail closed. Both central
  synchronous and asynchronous Magit process seams are advised.
- Reload now locks views before dispatch and reports the exact dirty URI
  snapshot. Rust partitions dirty affected views before publication, stages
  all incoming renderings in memory, and applies main's scalar-release
  decision before and after completion. No view registry mutation or textual
  frame precedes authorization.
- A scalar challenge retains the already-committed store transition under
  its incident in connection-thread state. An approved new request with the
  same incident performs prepare/gate/present only, avoiding a second graph
  mutation. The client keeps the same incident across that prompt.
- Clean affected views update normally. Dirty affected views retain both
  their local text and old server forest; the authorized incoming rendering
  is returned as conflict data, never automatically installed. The client
  stores last-rendered base/local/incoming state, blocks ordinary save, opens
  the agreed =*SKG Disk-Client Conflicts*= Org report and offers a three-way
  Ediff review. A prefix invocation of the resolution command submits the
  user's reconciled text normally and clears the marker only on save success.
- Factored sound playback out of =main.rs=. A committed disk/client conflict
  plays =/home/sound/beep-harsh.wav= once; server readiness alone retains the
  glorious sound. Missing audio is nonfatal.
- A final client-side race defense refuses any streamed view update which
  nevertheless reaches a newly modified buffer. Focused validation: 187
  serial Rust library tests, 329 Emacs tests, the complete Neovim suite, and
  compilation of every Rust integration-test target pass.

** local context repair and cyclic-root terminology (complete)
- Renamed the search-rank type and stored index label from =CycleMember= to
  =CyclicRoot=, including its multiplier, both clients, command references,
  fixtures and glossary.  Full startup/rebuild passes now write an explicit
  empty label for ordinary nodes, so the old spelling is cleared rather than
  accepted as a permanent parser alias.
- The selected graph state now carries the last authoritative cyclic-root
  set.  Startup and rebuild replace it after the full context pass; ordinary
  writes preserve it.  Local writes compare the closed one-hop contains and
  text-link neighborhood in the old and new graphs, including inverse and
  canonical extra-ID edges, and put changed context-only documents in the
  same Tantivy generation as the graph write.
- Added =skg-recompute-cyclicroots= and =:SkgRecomputeCyclicRoots=.  The
  server computes against an immutable graph generation without its writer
  lock, retries if that generation advances, waits through prior Tantivy
  work, rewrites only labels which differ and publishes the new cache only
  after a successful commit.  Failures keep the prior cache and give the
  client a rebuild instruction.
- The code comment at the algorithm records the precise meaning of cyclic
  root, the two rejected automatic approaches and their measured costs, and
  the limited rank/top-N consequence of deliberate staleness.
- Validation: 16 focused context tests pass; all 329 Emacs tests and the
  complete Neovim suite pass; the Rust library compiles.  Still owed in the
  later presentation scheduler: rerun live search views after an explicit
  repair commits, without clobbering edited search buffers.

** low-priority collateral scheduler (complete)
- Added immutable render cancellation tickets backed by a monotonic epoch.
  View completion checks between BFS viewnodes and around its expensive async
  seams; the finishing pass checks before and after ancestry/graph-stat
  TypeDB work and before text rendering.  A cancelled job may finish its one
  already-running query but starts no later expensive stage.  Foreground and
  de-novo renders carry no ticket and retain their prior behavior.

* completion pass

** low-priority collateral scheduler and Git presentation
- Every graph transition replaces one connection-local queue containing every
  non-saved open view.  One cancellable worker renders at a time, ordered by
  most-recent visit; foreground requests preempt it.  Offers are server-owned
  operations naming graph/presentation generations and a base view revision.
  The client applies only to a still-clean matching buffer and ACKs that exact
  offer before Rust advances its forest.
- Reload presents dirty affected views synchronously as conflicts and queues
  every clean view. Save updates its authored buffer in the foreground and
  queues every other view. A second transition discards obsolete remaining
  work. The code and comments now use 'collateral' only for any non-saved view.
- A deterministic HEAD/index signature over configured direct '.skg' paths
  advances a separate presentation generation. Stage/unstage therefore
  refreshes diff-mode views without pretending the worktree or graph changed.
- Explicit cyclic-root repair now reruns every clean live Emacs search from its
  recorded terms and axes after Tantivy commits; modified search buffers remain
  untouched and receive a warning.

** fatal incident preservation and recovery
- Fatal reloads capture and final-reread the complete participating '.skg'
  overlay, including base-only tombstones, unrelated dirty/untracked bytes,
  malformed and non-UTF-8 contents, all touched possible paths, repository
  bases/states and ownership. Nested repositories are not traversed.
- Save and recovery share one graph-to-telescope serializer. While the graph
  writer still protects the newly selected generation, recovery records become
  process-owned and an atomic user-only journal is written outside every source
  repository. Startup reloads unresolved journals; both client handshakes show
  them persistently.
- Confirmed Emacs recovery revalidates bytes, chooses one common unused N,
  constructs P/L/A blobs, trees and commits everywhere before refs, and then
  publishes the documented sibling topology without checkout or index writes.
  Existing dirty '.skg' overlays are preserved. Owned non-Git/unborn sources
  receive a root P and symbolic branch; detached, sequencer and foreign-only
  repositories are warning-only. Only owned fatal paths are restored from L.
- Ref publication rolls back only refs created by the failed attempt. The
  client report names every root/ref/commit/restored path. Explicit dismissal
  deletes only the private journal after explaining that automatic recovery is
  thereafter impossible. Neovim shows unresolved recovery prominently but has
  an explicit unsupported confirmation action rather than recovering silently.

** observation and external batches
- Nonrecursive file notifications, raw-file saves, Magit refreshes, manual full
  sweeps and the transient ID-stack TO-RELOAD selector feed one sequence-tagged
  coalescing map. Digests and absence are compared by the server; metadata is
  never authoritative.
- 'pull-all.sh' holds a process-wide token on a persistent control connection.
  The last matching end or EOF advances a reconciliation generation. The
  server pushes that generation only to a verified interactive client; Emacs
  takes its current dirty-buffer census and submits one exact full sweep. A
  later connection performs the sweep at handshake, so the shell never needs
  editor-local state.

** replacement-aware extra-ID fallback
- A legal extra-ID change still uses a complete-corpus fold because it may
  redirect anchors and relationships outside the touched telescope. If the
  same incident also contains a malformed telescope, that fatal PID is now
  supplied exactly from G0 while every readable telescope folds under the
  tentative resolver. The selected manifest likewise retains G0 for the
  fatal PID, while a separate exact-byte manifest protects the final stability
  check. A TypeDB-backed integration test proves that the legal change commits
  and the malformed telescope remains last-good.

* final verification state
- Focused reload/recovery tests prove exact snapshot bytes and tombstones,
  nested-repository exclusion, owned/foreign warning visibility, changed-byte
  refusal, P/L/A topology, non-Git initialization, preservation of HEAD/index,
  and fatal-only restoration.
- All Rust library targets and integration targets compile. The serial Rust
  library suite passes (200/200), the full TypeDB-backed nextest suite passes
  (603/603), the Emacs suite passes (331/331), and the complete Neovim suite
  passes.
- The repository's polite end-to-end harness passes in full: build, both
  client suites, nextest, doctests and all 68 live server/client integration
  runs (Emacs and Neovim). Its Neovim bootstrap now uses the real initialization
  path, including normalized config paths and server-push handlers, and the
  collateral integrations wait for the documented background application
  boundary instead of assuming it precedes the foreground save response.
- Protocol and glossary documentation now cover request/incident/operation
  identity, reload and batch endpoints, fatal recovery, manifests, tombstones,
  generations and the scalar-release boundary.

* known limit left explicit rather than hidden
- Neovim does not yet originate partial reload or confirm recovery; it displays
  batch/recovery warnings and never mutates automatically. This is the planned
  fail-closed interim behavior.
- If disk changes after a fatal incident, recovery refuses and preserves the
  original journal and any already-published evidence refs. Its structured
  failure makes Emacs assign a fresh incident and queue an exact full sweep,
  which classifies and presents the changed bytes as the successor.

* execution of '7_plan.org'
** what's done and went well
- The pre-execution baseline passes: =cargo check --lib=, all 334 Emacs tests,
  and the complete Neovim suite.
- Added the configured maintenance archive root and made config loading create
  it privately, retain both its host-resolvable configured spelling and its
  canonical server identity, and reject parent traversal or overlap with a
  source in either direction.  Focused default, permission, overlap, traversal,
  and symlink-aware config tests pass.
- Added manifest revision as an identity distinct from graph generation,
  including the semantic-no-op transition which advances exact byte authority
  without pretending the graph or derived stores changed.
- Added the typed maintenance vocabulary and a pure coordinator with explicit
  origins, phases, pending reasons, archive states, dispositions and per-state
  edit/save/query policy.  Its focused happy-path, stale-candidate, pending,
  and disconnect model tests pass.
- Added versioned, private, checksummed, atomic maintenance journals under the
  config-keyed state root.  Malformed and incomplete publications are retained
  and reported.  Focused round-trip, compaction-refusal and corruption tests
  pass.
- Added a generation-gate implementation with RAII query/selection leases and
  the process runtime foundations: immutable selected-runtime publications,
  retained interactive session, retained coordinator, journal and an exclusive
  single-interactive connection slot.
- Wired the process runtime through the listener and request dispatch.  The
  sole interactive session, open forests, collateral scheduler, maintenance
  coordinator, candidates and observer now survive sequential replacement
  connections; role verification refuses a concurrent interactive socket and
  requires a census before ordinary requests.
- Made foreground request teardown total in both clients, made response writes
  report failure, and added the two-step descriptor/text census.  A reconnect
  reattaches an exact retained URI/revision/token or reconstructs a missing
  same-generation forest only from checksummed last-fetched text; mismatches
  preserve editor text but remove live save authority.
- Moved direct source observation into a process-owned nonrecursive Rust
  watcher and cancellable worker.  It builds immutable full-corpus candidates,
  distinguishes byte equality, semantic equality, valid semantic changes,
  invalid input and unstable reads, and advances only manifest revision for a
  revalidated semantic no-op.
- Ordinary Skg saves now prepare and fence the union of every replacement,
  creation and tombstone immediately before their first write, using exact
  selected BLAKE3 bytes/absence rather than watcher timing.  The same boundary
  catches same-length non-ASCII rewrites and unexpected creation/deletion, and
  pending valid or invalid disk work rejects a save before parsing its plan.
- Added explicit view kind/recipe and graph, presentation, revision, client
  buffer and application-token authority to retained forests and both editor
  registries.  View creation and successful saves carry that authority on the
  wire.  Background collateral offers now name the exact base record and
  required next token; clients refuse stale or dirty installation and Rust
  advances its forest only after the exact next token is acknowledged.
- Pinned search enrichment to one generation lease, staged its complete
  immutable enrichment payload before waiting for editor text, and made its
  eventual application use the same exact buffer/revision/token authority and
  acknowledgement boundary as collateral presentation.
- Added native recovery-history adapters for both editors: Emacs delegates to
  undo-fu-session's public serializer (and refuses active Undo Tree mode),
  while Neovim uses its native =:wundo= format.  Both fail closed when a dirty
  buffer has history that cannot be archived; the explicit exact-buffer waiver
  is a separate server-journaled confirmation.
- Added matching portable, private, checksummed archive writers in Emacs and
  Neovim.  They stage exact buffer text, last-fetched text, unified diffs,
  recipes/workflow metadata and opaque native undo sidecars, then publish the
  incident by one directory rename without overwriting an existing incident.
- Extended the durable coordinator's archive contract to freeze the owning
  editor/session, source set, exact G0, complete registered/dirty/undo census,
  server-allocated timestamped directory, and exact undo waivers.  Disconnect
  preserves the precise suspended phase and reconnect restores it.
- Added a strict server-side archive verifier and the begin/archive-ready/
  failure/waiver/cancel/status protocol.  The verifier walks through held
  no-follow directory descriptors on Unix, enforces private modes and a closed
  file inventory, hashes every artifact, and checks the client manifest against
  the frozen server contract before durably accepting =ARCHIVE-READY=.
- Froze the complete typed client buffer census into the durable incident,
  including presentation generation and exact last-fetched/current hashes, and
  made server-side archive acceptance bind every archived buffer and byte hash
  to that census rather than trusting the client manifest in isolation.
- Added durable per-buffer view-settlement records and conservative incident
  classification.  The coordinator now refuses an incomplete inventory and
  cannot enter archive finalization until every registered buffer has supplied
  the exact retirement, release, application, or close acknowledgement required
  by its disposition.  Dirty impacted views are always interrupted, never
  silently refreshed; clean impacted views are assigned exact reapplication.
- The focused Rust maintenance suite passes (23/23), as does the complete
  Neovim suite after the census-schema extension.
- Extended the length-prefixed transport with one typed artifact-bundle frame:
  its exact total and UTF-8 descriptor lengths are in the header, request and
  incident identity live in the descriptor, and the remaining bytes stay
  opaque in both clients.  Rust, Emacs and Neovim tests cover arbitrary chunk
  splits, multibyte descriptors, invalid descriptor boundaries and non-UTF-8
  artifacts.  All 343 Emacs tests, the complete Neovim suite and
  =cargo check --tests= pass.
- Made process-owned candidate evidence directly replayable into the portable
  final archive.  Changed nodes now receive deterministic opaque-safe paths
  under =new-nodes/=, =deleted-nodes/= or =modified-nodes/=, human README and
  source-outcome records, semantic before/after/diff files, and exact available
  raw-after bytes.  The replay builder revalidates each durable artifact and
  emits ordered offsets, lengths and SHA-256s over one opaque byte stream.
- While exercising nested evidence paths, found and fixed an umask-dependent
  permission hole: every newly created intermediate evidence directory is now
  mode 0700 from first creation.  The focused maintenance suite passes (24/24).
- Added the exact, idempotent maintenance-evidence endpoint.  It serves only
  the active archive owner after coherent store selection, requires the
  already-journaled server bundle checksum, journals the deterministic transfer
  manifest and opaque-stream checksums before sending, and emits every safe
  path/offset/length/SHA-256 in the framed descriptor.  A replacement request
  can therefore receive the same transfer without repeating selection.  The
  focused Rust maintenance/protocol suite passes (25/25).
- Added Emacs's replay-safe final archive writer.  It rejects noncontiguous,
  overlapping, unsafe, changed or non-opaque artifact bundles before writing;
  installs each missing evidence category by same-filesystem rename; verifies
  an already-installed category on retry; atomically rewrites =incident.org=
  and the interrupted-buffer index; writes a checksummed final manifest; and
  creates =FINALIZED= last.  Exact non-UTF-8 evidence and a second idempotent
  finalization pass are covered.  All 345 Emacs tests pass (two optional
  undo-fu-session tests remain skipped when that package is absent).
- Added the equivalent Neovim final archive writer and the same portable final
  manifest/report/index schema.  It validates safe evidence paths, contiguous
  offsets, each artifact and the complete opaque stream before staging; installs
  whole evidence categories by rename; verifies already-installed categories
  on replay; and publishes =FINALIZED= only after every durable final artifact.
  Exact non-UTF-8 evidence, idempotent replay and changed-stream refusal are
  covered, and the complete Neovim suite passes.
- Exposed the journaled per-view settlement gate as an exact protocol ACK.
  Every ACK must repeat the incident, epoch, buffer, required action, URI,
  server revision and application token frozen in the settlement plan.  The
  last ACK advances to archive finalization; an exact replay there succeeds,
  while any changed authority is still refused.  The focused Rust maintenance
  suite passes (25/25).
- Added the exact final-archive ACK boundary.  The server now refuses to record
  finalization until the complete registered-view settlement inventory is
  acknowledged and the client repeats the journaled transfer-manifest and
  opaque-stream checksums alongside a valid final-manifest checksum.  Exact
  ACK replay succeeds; a changed transfer or final manifest is rejected, and
  terminal completion additionally requires this durable evidence ACK.  The
  focused Rust maintenance suite passes (25/25).
- Added a durable terminal-delivery handshake instead of dropping a completed
  incident straight to idle.  Completion now retains the exact incident,
  epoch, owner, final-manifest checksum, selected store and unlock census until
  the client acknowledges the terminal instruction; a lost completion reply
  is exactly replayable, and only the final ACK compacts the active journal.
  Safe pre-mutation terminals can unlock, while failure after mutation remains
  locked.  The focused Rust maintenance suite passes (26/26), and
  =cargo check --tests= passes.
- Made non-rendered view settlement ACKs perform their exact server-side
  lifecycle effect.  Retirement and close unregister only the frozen matching
  forest; release carries that forest forward to the selected graph while
  marking presentation stale and, for searches, membership/ranking stale.
  Advanced revision/token/generation authority refuses the ACK, exact replay
  is a no-op, and application ACKs remain blocked until a staged rendering is
  present.  The focused Rust maintenance suite passes (27/27).
- Added the missing durable scalar-authorization phase for maintenance
  presentation.  A text-free challenge records the exact operation, sorted
  protected PIDs and prompt before leaving =presenting=; only an exact PID-set
  approval returns it there, and approval replay is idempotent.  A different
  challenge or partial/broadened approval is rejected without releasing text.
  The focused Rust maintenance suite passes (28/28), and
  =cargo check --tests= passes.
- Staged clean impacted maintenance views as durable exact application offers.
  Rendering now runs from the retained forest under a pinned selected-G1
  lease, without holding the interactive-session lock or mutating the live
  forest.  Each offer journals the rendered bytes and SHA-256 together with
  resulting graph/presentation generations, server revision, application
  token and warnings.  Restricted-source rendering sends only a durable PID
  challenge until the exact scalar approval is journaled; retry rechecks the
  same gate, and status/reconnect can replay either the challenge or the exact
  already-recorded offer.  The focused Rust maintenance suite passes (30/30),
  and =cargo check --tests= passes.
- Completed the server half of exact maintenance application acknowledgement.
  An application ACK now repeats the offered content checksum and resulting
  graph/presentation generations, server revision and client token in addition
  to the frozen base authority.  The coordinator rejects any changed or
  incomplete tuple, including on replay.  Before journaling the ACK, the server
  rechecks the offer's own checksum and one-step counters and reconstructs the
  rendered forest; after the durable transition it installs that forest and
  authority without rerendering.  Applied searches remain explicitly
  membership/ranking-stale.  The focused Rust maintenance suite passes (31/31),
  and =cargo check --tests= passes.
- Closed a generation-authority omission found while implementing the first
  client consumer.  Every settlement now journals and transmits its frozen
  base graph and presentation generations as well as revision/token, and every
  ACK must echo all four exact base counters before its action can settle.
  Older journal records deserialize fail-closed with zero generation authority.
  The focused Rust maintenance suite remains 31/31 and
  =cargo check --tests= passes.
- Tightened the reconnect metadata before teaching clients to resume the
  settlement handshake.  Maintenance status now marks each exact view action
  as acknowledged or pending, while the ordinary connection handshake exposes
  only a concise coordinator-state label instead of Debug-serializing durable
  incident details (which could include staged clean view text).  Focused wire
  and handshake tests and =cargo check --tests= pass.
- Replaced the remaining Rust =Debug= spellings in maintenance status and
  observation-problem frames with explicit stable kebab-case labels for every
  coordinator state, maintenance phase and pending reason.  The focused Rust
  maintenance suite remains 32/32.
- Completed the Emacs client side of a selected-candidate incident.  It now
  validates and applies every exact release, retirement, close or rendered-view
  settlement; preserves point/window position and native undo where required;
  ACKs all frozen and resulting authority; downloads and finalizes the opaque
  evidence archive; and unlocks only after validating the journaled terminal
  census and selected generations.  A reconnect census preserves locally
  applied presentation debt, status filters durable ACK bits, and lost view,
  evidence, final-archive, completion and terminal replies retry without
  repeating local actions.  Hot code reload now refreshes this machinery while
  preserving its live incident state.  All 356 Emacs tests pass (the two
  optional undo-fu-session tests remain skipped when that package is absent).
- Completed the matching Neovim selected-candidate path.  Its buffer registry
  now validates the complete frozen authority before release, retirement,
  close or exact rendered application; preserves authored text, native undo,
  window positions and the prior modifiable state; and records the resulting
  graph, presentation, revision and application-token authority without
  clearing stale search membership.
- Neovim now drives the complete durable handshake: stable settlement replay,
  exact ACK retries without repeating local edits, opaque evidence transfer,
  replay-safe final archive publication, final-archive ACK, completion,
  terminal-census validation and the final terminal ACK.  Its reconnect census
  locks before reporting editor state and resumes the retained phase only after
  census completion.  Deliberate Lua reload preserves incident/archive
  authority and runs every in-flight request finalizer before reconnecting.
  Focused tests exercise the full evidence-to-terminal chain and the complete
  Neovim suite passes.
- Added one durable explicit-partial-reload target contract.  The maintenance
  journal now freezes normalized path and ID requests, and the terminal record
  retains exact per-ID resolution outcomes so clients clear only IDs whose
  incident actually completed.  Primary and extra IDs resolve against G0,
  unknown IDs remain rejected with a reason, and an all-unknown request still
  reaches a durable semantic no-op terminal rather than disappearing.
- Added exact targeted candidate construction and a server-owned origin worker.
  Every resolved PID observes all possible direct cross-source telescope paths;
  extra-ID changes conservatively fall back to the complete corpus, exact byte
  fences are rechecked before selection, and unrelated worktree changes are not
  smuggled into the authorized target set.  The asynchronous result carries the
  incident, epoch and candidate identity and is replay-safe across an ordinary
  socket reconnect.
- Routed Emacs's existing ID-stack selector through the common maintenance
  archive, candidate, settlement, evidence and terminal handshake.  It asks the
  single dirty-work question before beginning, preserves selector marks until
  terminal acknowledgement, and reschedules the idempotent server origin after
  reconnect.  The complete Emacs suite passes: 358 tests, with the two optional
  undo-fu-session tests skipped when that package is absent.
- Added matching Neovim path/ID APIs and =:SkgReloadIds=/=:SkgReloadPaths=
  commands.  Local absolute paths beneath the client config root become stable
  config-relative paths before crossing a host/container boundary; dirty views
  receive the same one-time archive warning; server-push candidate selection,
  failure and terminal outcomes drive the existing exact handshake.  The full
  Neovim suite passes.
- Generalized Neovim's maintenance origins to the same adapter boundary as
  Emacs, including durable opaque origin context and reconnect dispatch for
  archive-ready, running-external-mutation, and final-observation phases.  The
  complete Neovim suite passes.
- Routed pull through maintenance in both clients.  Each client derives and
  proves the same logical-repository mapping from its local source inventory,
  refuses modified raw files, archives before mutation, runs exact argv Git
  pulls serially in incident-qualified interactive process buffers, and reports
  even a lost child as an indeterminate possible mutation before exact final
  observation.  Full Emacs and Neovim unit suites pass.
- Replaced the stale maintenance-entry census with the agreed epoch-first
  barrier.  The server now durably allocates an incident and epoch before any
  archive work, both clients apply that epoch to every existing buffer and to
  later buffer constructors, and an incident-qualified descriptor/text census
  must complete before the exact live inventory is frozen and acknowledged.
  Archive publication cannot begin in the new =awaiting-locked-census= phase.
  Reconnect status can replay either side of the barrier.  The focused Rust
  maintenance suite passes 47/47, all 376 Emacs tests pass (two optional
  undo-fu-session cases skip when the package is absent), and the complete
  Neovim suite passes.
- Added a two-client, real-server pull integration harness with a local bare Git
  remote.  Both editors now pass the entire path: open a live pre-pull view,
  install and report the locked census, publish a zero-dirty initial archive,
  perform a real local Git pull, observe and select exact final disk, update the
  same live view, append exact server evidence, publish =FINALIZED=, acknowledge
  terminal authority, return idle/unlocked, leave the Git worktree clean, and
  retain incident-qualified process diagnostics.
- The real Neovim run exposed and fixed three wire/lifecycle gaps which mocked
  unit fixtures had hidden: external-result list members must be sibling atoms
  rather than one nested list; the Lua reader's singleton representation of a
  bare =nil= scalar must normalize back to false in final-archive authority;
  and Neovim's terminal job renames its buffer to =term://=, so the stable
  =skg://pull/INCIDENT/...= diagnostic name is restored after process exit.
  Each has a focused regression test.
- Routed explicit full rebuild through the same epoch-first locked census,
  private initial archive, complete post-archive observation, durable evidence,
  exact view settlement, final archive and terminal acknowledgement path as
  partial reload and pull.  The raw =rebuild dbs= request no longer mutates
  stores, and the import helper no longer sends it.
- Full rebuild now validates and retains one immutable complete candidate before
  taking the exclusive generation gate.  Under that gate it reconstructs
  TypeDB, Tantivy and context labels from the candidate, publishes the exact
  graph/manifest/config/index/warning tuple together, and attempts complete G0
  reconstruction on every destructive failure boundary.
- A replacement config is loaded from the same governing path and fenced by
  its exact BLAKE3 bytes.  A changed source catalog is checked against both old
  and new archive-root relationships; all replacement watches are prepared
  before the old set is dropped.  The old active source-set is retained by
  exact name or falls back to =all=, never to a similar name.
- Candidate selection and status replay now refresh both clients' normalized
  source inventory, selected source-set and archive-root identities.  Each
  later maintenance offer also carries its own archive-root authority, so a
  post-replacement incident cannot publish into the retired root.
- The real-server rebuild harness now passes for both Emacs and Neovim.  It
  replaces =main/notes= with =replacement/replacement-notes= at
  =ARCHIVE-READY=, reconciles the same live view, proves the =all= fallback,
  advances the graph generation, finalizes the old-root archive, then proves a
  second incident uses the replacement archive root.  Its malformed-config
  preflight remains locked before exclusivity while a fresh G0 content query
  succeeds.
- Live replacement rejects process-startup-only settings (listener, tracing,
  audit-daemon, readiness-sound and shutdown-handler configuration) with an
  explicit restart instruction.  Runtime source, database/index, archive and
  render-limit settings remain replaceable.  The focused maintenance suite is
  55/55; focused Emacs maintenance/archive tests, the complete Neovim suite and
  both real-editor rebuild integrations pass.
- Made ordinary disk observation wholly server-owned.  The legacy =reload
  paths= wire spelling is now a nonmutating observer hint: paths queue exact
  observation, full-sweep queues a complete comparison, and direct IDs are
  refused with a maintained-command instruction.  Closing the final external
  reload-batch token, including control-socket EOF, directly queues a server
  sweep; the client push is progress only.  The observer refuses ordinary
  publication while a batch is open, so the exact close sweep is authoritative.
- Removed Emacs's file-notify watches, coalescing timers, client observation
  incidents and raw-save notification hook.  =skg-reload-changed= and Magit
  refresh now only request process-owned observation, while public path/ID
  reload enters =explicit-partial-reload= maintenance.  Neovim's batch-close
  notice likewise reports the sweep already queued by Rust.  The focused Rust
  maintenance/parser tests, all 376 Emacs tests (two optional undo sidecars
  skipped), and the complete Neovim suite pass.
- Rewrote the protocol documentation around the sole retained session,
  process-owned observation, G0/Gdisk/G1, maintained pull/partial/rebuild
  transitions, exact census and ACK boundaries, opaque artifact framing,
  archive publication, staleness and detached recovery.  The glossary now
  fixes that vocabulary; both command references describe their actual common
  maintained surface and failure actions; setup documents the archive root,
  POSIX privacy, incomplete staging and =undo-fu-session= 0.8; and the
  architecture page draws the server/client ownership boundary.
- Removed the retired direct-reload presentation and client conflict path after
  its maintained replacements passed.  The server's old reload handler is now
  only an observation hint, and the editor no longer carries dead incoming/base
  conflict state which could suggest a second graph-selection route.
- Retired the remaining shell/control bypasses.  The old pull helper now
  delegates to the maintained editor command, and raw rebuild/control requests
  cannot select disk around the coordinator.
- Added strict offline retained-incident inspection in both clients.  Listing
  verifies publication markers, manifest checksum chains, private regular-file
  artifacts and final dispositions without a server, while retaining corrupt
  strict-named siblings as visible invalid incidents.
- Integrated the native undo sidecars into detached recovery.  Opening a
  recovery buffer restores Emacs undo-fu-session or Neovim =:rundo= history
  without modifying the archive; active Undo Tree mode still fails closed, and
  missing, corrupt, incompatible or foreign sidecars fall back to exact text.
- Added equivalent detached-recovery command surfaces in both clients: list,
  inspect, independently open exact interrupted text, navigate verified
  artifacts, open a fresh current-generation view/search, and safely delete a
  terminal incident.  Recovery buffers carry no live URI/application authority
  and refuse every write path.
- Added real-file cross-client fallback tests.  Each suite opens a completely
  published and finalized archive whose signed client identity names the other
  editor, verifies the full checksum chain, preserves exact text, and declines
  only the incompatible native undo sidecar.
- Made fresh recovery views genuinely side-by-side.  Content recovery bypasses
  existing-root redirection, while search recovery allocates a distinct
  =search:recovery:UUID= and carries it through phase one, snapshot and enriched
  application authority so an older same-terms search cannot be overwritten.
- Made maintenance state persistent at the point of use: Emacs's mode line and
  both clients' buffer-entry notifications repeat pending-disk, epoch-lock,
  preserved-presentation and stale-search warnings.  The public recovery
  commands are documented and included in Emacs hot re-evaluation order.
- Completed the compact buffer census rather than treating a URI as the whole
  identity of a view.  Both clients now send stable buffer ID, explicit kind,
  lifecycle/disposable policy, continuation, normalized recipe and roots,
  source set, complete graph/presentation/revision/application authority,
  exact last/current text digests, dirty and stale state, herald presence and
  maintenance epoch.  The server freezes and validates the same descriptor,
  requests exact texts only through the incident-qualified artifact stream,
  and binds the archive and every settlement ACK to that complete census.
- Added exact attached-workflow parentage.  Metadata editors, fork
  confirmations, relationship menus and the remaining late-stream Ediff
  recovery bundle name their origin buffer ID, URI, application token and
  normalized location.  Their live presence makes the origin logically dirty;
  server classification couples each child disposition to its verified parent,
  and client teardown recomputes the parent's unfinished-workflow count.
- Routed every product buffer constructor in Emacs and Neovim through an
  explicit registry policy.  The registry rejects omitted lifecycle or
  disposability, source-scanning tests enumerate every remaining direct editor
  buffer allocation, and configured direct =.skg= file buffers enroll as
  =raw-skg-file=/=ordinary-file= without becoming graph views.  Regenerable
  derived reports are explicitly disposable; ID-stack edits, workflows,
  durable diagnostics and detached recovery are not inferred from conventional
  names.
- Conventional-name reuse is now limited to a registered, clean, explicitly
  disposable buffer with no continuation or active maintenance epoch.  Dirty
  search views and ID stacks remain byte-for-byte untouched, while warning,
  recovery and declined-fork reports get distinct names instead of erasing an
  earlier account.  Maintenance locks apply only to graph views, attached
  workflows, maintenance controls and raw files, and Neovim restores the exact
  prior =modifiable= state.
- Tightened server cleanup to the same explicit rule.  A clean buffer closes
  automatically only when it is disposable and continuation-free (apart from
  the relationship menu's typed cancellation); reload selectors, durable or
  edited derived state, and unfinished workflows receive a release or
  conservative interruption instead.  The legacy Emacs late-stream conflict
  path registers both Ediff variants under one continuation and terminates the
  control/variant bundle explicitly after a successful reconciliation.
- Validation after the constructor/workflow slice: the focused Rust view-impact
  tests pass, all 400 Emacs tests pass with three optional undo-sidecar tests
  skipped, and the complete Neovim suite passes.  The slice was committed as
  separate server, Emacs and Neovim changes.
- Completed the ordinary-save fence envisioned by the plan.  The Rust save
  path prepares the complete create/replace/move/tombstone path union, binds
  every target to selected BLAKE3 bytes or absence, validates the complete
  union under graph-writer serialization immediately before mutation, and
  queues exact observation on a typed mismatch.  Tests now additionally prove
  refusal of semantically equal byte changes, atomic path replacement and one
  drifted member of a multi-path batch without changing any other member.
- Added independent raw-file save authority to both clients.  A configured
  direct =.skg= buffer remembers exact SHA-256 bytes or absence from its actual
  read, including a read which predates verified source inventory; every save
  refuses a changed, deleted, created, symlinked or otherwise unsafe path and
  queues process-owned observation.  Successful saves alone advance the
  baseline, while an explicit revert establishes a new one.
- Raw files now remain unconditionally unsaveable during a maintenance epoch.
  Unlock compares each clean raw buffer with its pre-maintenance read fact,
  retains an externally-stale warning until revert/save reconciliation, and
  never replaces editor text.  The common maintenance entry function in each
  client refuses and names every modified raw buffer before connecting or
  allocating an incident, so partial reload and pending reconciliation have
  the same preflight as pull and full rebuild.
- Validation after the ordinary/raw save slice: all five focused Rust fence
  tests pass; all 403 of 406 runnable Emacs tests pass with the three expected
  optional undo-sidecar skips; and the complete Neovim suite passes.
- Completed replacement-editor and server-restart reconciliation for retained
  incidents.  A complete census now journals =census-absent= for a frozen
  buffer which is genuinely absent and =census-applied= only when a live clean
  descriptor proves every resulting counter, token, epoch, recipe, root set,
  stale bit and exact staged-content checksum of an otherwise unacknowledged
  application.  Neither case rewrites the original planned disposition or
  claims an ordinary client ACK, so the final archive retains how the debt was
  actually discharged.
- Separated immutable archive provenance from transferable incident control.
  The original producer editor/session and initial manifest checksum remain
  signed facts, while an attached replacement session may adopt control only
  after the initial archive is verified and published.  Before selection, a
  restarted server reconstructs its verified-archive cache from that immutable
  checksum; after selection, the journaled store and evidence records remain
  the point-of-no-return authority.
- Both clients now inspect the retained archive before resuming an adopted
  incident, reconstruct active or terminal state from the exact journaled
  checksums, and expose producer, controller, initial/final manifest, transfer
  and artifact checksum facts in retained-incident inspection.  Focused Rust
  coordinator/census/recovery tests, focused Emacs maintenance/archive tests
  and the complete Neovim suite pass.  One full parallel Rust run reproduced
  four preexisting save-leveling failures caused by shared process state; the
  complete affected 15-test module passes serially, while all reconnect,
  coordinator and recovery tests pass.
- Removed the last foreground per-view rerender mutation path.  Explicit
  rerender, diff-mode toggle and source-set switch now only commit/queue their
  retained-session work and terminate; one server-push offer at a time owns
  complete base/result graph, presentation, revision, client-token, buffer and
  source-set identity, and the server forest advances only after its exact ACK.
  Already-offered work survives a replacement batch, dirty or obsolete client
  buffers reject without being overwritten, unchanged rejected/failed views
  become presentation-stale, and search snapshot continuations fence scheduler
  pumping so the application protocols cannot compete.
- Made background scalar release part of that same exact operation.  A
  protected view sends no candidate text; approval requeues only its named PIDs
  and URI, decline preserves the old view as stale, and render failures remain
  visible ACK obligations.  Removed the obsolete command-retry authorization
  surface, whose semantics could have toggled diff mode twice.
- Validation after the retained rerender slice: 48/48 focused Rust handler
  tests pass; focused diff/source-set/override database suites pass; all 405 of
  408 runnable Emacs tests pass with the same three optional undo-sidecar skips;
  and the complete Neovim suite passes.  The protocol and both command
  references now describe queued foreground completion and exact collateral
  offers rather than the retired =rerender-view= stream.
- Made Git presentation observation process-owned.  A distinct watcher covers
  every resolved per-worktree Git directory, common Git directory and recursive
  refs tree, including linked worktrees; filesystem events coalesce through the
  observation worker and only the exact HEAD/index/configured-path signature
  advances presentation generation.  Source-catalog replacement prepares both
  replacement watcher sets before publication, and the existing Magit refresh
  hook remains a latency hint rather than a correctness boundary.
- Every collateral queue replacement now immediately marks its exact server
  views presentation-stale and emits a text-free =refresh-queued= server push
  naming reason, graph/presentation generation and ordered URIs.  Both clients
  expose that debt without touching text or save authority; failed delivery is
  retained, and census replay independently propagates missed stale URIs after
  reconnect.  Exact application ACKs retain the stale bit when newer work for
  the same URI remains queued.
- Validation after the automatic-presentation/status slice: focused Rust
  watcher, scheduler, census and rerender tests pass; the exact isolated tests
  behind two shared-global parallel-run flakes pass; all 406 runnable Emacs
  tests pass with the three expected optional undo-sidecar skips; and a clean
  complete Neovim run passes after isolating one concurrent pending-count
  flake.  =git diff --check= is clean; this environment does not have the
  =cargo fmt= subcommand installed.
- Closed the maintenance presentation fence explicitly.  Each selected G1 now
  journals one typed record pairing its candidate observation sequence with
  the exact normalized Git-presentation BLAKE3 and presentation generation;
  maintenance rendering exact-checks before and after, retries an advancing
  signature, and terminal completion closes delayed watcher gaps once more.
  Later Git changes remain ordinary queued presentation work.  The socket idle
  loop no longer pumps ordinary collateral offers while maintenance is locked.
- Retained source watcher work across the same serialized interval.  An active
  watcher event now advances the durable sequence and records successor debt
  without running a result the coordinator must reject.  Preselection drift
  clears the obsolete candidate/evidence pointer and invokes the incident's
  exact targeted or complete observer; already-published attempted evidence is
  renamed to a unique retained sibling before replacement.  Terminal unlock
  always queues a complete exact sweep for changes after G1.
- Focused coordinator, evidence, observation, selection-adjacent protocol,
  connection-loop and journal tests pass.  The full Rust library run passed
  286/290 and reproduced the same four shared-global save-leveling races seen
  earlier; all 15 tests in that affected module pass together serially.
- Added an exact =retry maintenance= endpoint for the two durable blocked
  phases.  It preserves incident, epoch and archive ownership, never repeats a
  client pull, and always starts from fresh observation.  A still-healthy exact
  G0 store tuple keeps the incident's targeted/complete scope; a poisoned,
  unavailable or changed tuple forces a complete candidate and exclusive full
  reconstruction before release.
- Exposed that recovery as =skg-retry-maintenance= and
  =:SkgRetryMaintenance=.  Both clients retain the exact blocking reason and
  server phase, send the incident-qualified epoch, and await the ordinary
  asynchronous status continuation.  The existing fatal reload-recovery and
  dismissal commands in both clients already covered retained archive
  incidents, so that audit required no additional product surface.
- Closed the invalid-post-pull dirty-work gap.  Once an external operation may
  have written disk and final bytes are invalid, the server journals a separate
  preselection retirement for every dirty member of the frozen census.  Both
  clients retire and exactly ACK those views before retry is permitted;
  replacement census absence is equally durable.  A later valid selection
  retains those acknowledged retirements instead of trying to release text
  whose orthogonality could never be proved.
- The focused Rust maintenance suite passes 69/69 after blocked recovery and
  preselection retirement.  The complete Neovim suite passes; the relevant
  Emacs retry/retirement tests and the broader maintenance selection pass.
- Closed the execution-discovered late-view gap with two durable identities.
  Every successful maintenance-time query now journals its server-owned
  =ViewUri= before the response, and an incident-qualified priority census
  binds it to the editor-owned stable buffer ID.  The immutable initial archive
  census stays separate from the monotonically growing presentation census;
  settlement, final evidence and terminal unlock use the latter.
- Selection now checks that enrollment barrier only after all pinned G0 query
  leases have drained.  If a G0 response or a newly selected G1 response still
  awaits census binding, the coordinator durably defers selection or view
  classification and status resumes it after census.  Exact scalar approval is
  reused across that deferral.  Emacs and Neovim both pass the expanded rebuild
  integration: a view born during an invalid-config block remains epoch-locked,
  joins the repaired incident's settlement, and unlocks only in its terminal
  inventory.
- Validation after late enrollment: 58/58 focused Rust maintenance tests, all
  411 of 414 runnable Emacs tests with the three expected optional undo-sidecar
  skips, the complete Neovim suite, and both real-client full-rebuild/retry
  integrations pass.  =cargo check --lib= and =git diff --check= are clean.
- Completed the post-enrollment compatibility audit.  Successful saves now
  advance each client's process-local graph baseline monotonically, so a view
  constructed after a save inherits the selected generation rather than the
  handshake generation.  Emacs normalizes every atom-shaped identity in both
  ordinary collateral and search-enrichment offers before registry lookup and
  exact ACK construction, and a fresh =skg-connection-verify= correctly defers
  to the mandatory handshake instead of attempting a non-local
  =cl-return-from=.
- Preserved client-created new-empty authority through its first connection.
  Both clients publish the complete typed record before herald setup can cause
  nested network work, initialize only a genuinely unbound revision-0/token-1
  new-empty record from its first handshake, and retain an already bound older
  generation as stale.  Census recognizes the matching never-saved authority
  without inventing a server forest; the ordinary save endpoint remains the
  sole place which materializes its text.  The formerly failing empty-buffer
  save passes in both clients.
- Converted the remaining hand-built integration views to explicit
  =new-empty-content-view= records and made multi-phase fixtures preserve the
  post-save revision/token rather than re-registering a buffer.  Fork tests
  now await the approval re-save before closing their origin, and locate
  durable declined confirmation buffers by registered kind and contents
  instead of a conventional name.  Diff/Magit tests await the actual queued
  application predicate, including both serialized views, rather than
  confusing the command ACK with presentation completion.
- Final repository-wide validation passed without retries in 220 seconds:
  all 417 runnable Emacs unit tests passed with the three expected optional
  undo-sidecar skips; the complete Neovim unit suite passed; all 702 Rust
  nextest cases and all doctests passed; and all 72 Emacs/Neovim integration
  runs passed.  =git diff --check= is clean.

** Changes to the plan's envisioned order.
- Archive-root validation was implemented alongside the first shared identity
  types, before the client archive writer.  Every later archive and handshake
  path now has one validated config fact to consume.
- The first real pull integration was added before the full-rebuild entry path.
  That run exposed the stale-census race, so the epoch-first locked-census
  barrier and its reconnect phase were completed before proceeding to rebuild.
- The obsolete reload and shell bypasses were removed before the fuller
  workflow-census pass.  Their maintained pull/reload/rebuild replacements and
  end-to-end tests already made the deletion safe, and removing them reduced
  the number of constructors and mutation routes the remaining audit must cover.
- The client constructor inventory and close-policy matrix were completed
  immediately after full census/archive parentage, before the later ordinary
  save fence.  That made the census closed over the actual live buffer set and
  exposed conventional-name overwrite risks while the policy was still local.
- The retained failing full-rebuild retry was extended before the final broad
  validation.  It exposed that the original one-time census could not name a
  view born in a blocked incident, so the approved URI-to-buffer enrollment
  barrier was implemented and tested before returning to the verification
  matrix.
- The final broad run was intentionally used as a compatibility audit after
  late enrollment rather than merely as a pass/fail gate.  It exposed old
  fixtures which relied on pre-authority constructors or synchronous rerender
  completion; those tests were migrated to the explicit record and queued
  presentation contracts before the harness was rerun.

** what's imperfect about what's done
- Both clients can finish an already-selected disk candidate, explicit reload,
  client-owned pull and full rebuild through the common post-archive path, and
  can resume either durable blocked phase without repeating external work.
- No known recovery action remains absent from the public Emacs or Neovim
  command surfaces.  The automated final broad validation and end-to-end audit
  are complete.  The plan's final real-corpus dogfood/performance exercise
  remains an operational check requiring Jeff's actual large corpus and editor
  session rather than a repository test fixture.

** choices determining the future
- No new architectural choice arose from the locked-census or end-to-end pull
  slice.  The Undo Tree discovery remains resolved by the agreed fail-closed
  active-mode check; merely having the package installed has no effect.
- No choice remains from late-view enrollment; its two-stage identity and
  monotonic presentation-census rule are the user-approved design.

* post-plan history context and code isolation

** commit context
- Rewrote the messages of all 147 commits after
  =547cfaca628dc2a2a84d59adc962f08fb747ad02=.  The first now begins with
  =TODO/partial-reload-and-magit/6_bugs.org |=; the other 146 begin with
  =TODO/partial-reload-and-magit/7_plan.org |=.  Keeping each former subject
  after the separator preserves useful one-line history as well as plan
  provenance.
- Preserved the old tip at
  =backup/partial-reload-and-magit-before-plan-prefixes=.  An ordinal audit
  compared all 147 old/new pairs: every tree and every non-parent commit header
  is identical, and removing the new prefix recovers each former message.
  The two tips have no content diff.

** server code isolation
- Moved maintenance observation, candidate/archive retention, durable
  coordinator transitions and presentation observation from
  =server/runtime/mod.rs= into =server/runtime/maintenance.rs=.  The general
  runtime module fell from 486 to 221 lines and remains the small owner of
  query leases, selected-store publication and connection exclusivity.
- Moved maintenance-time connection gating, idle server pushes, batch
  reconciliation and disconnect cleanup from =server/serve.rs= into
  =server/serve/maintenance_connection.rs=.  Moved verification/census
  handshake construction into the already feature-specific
  =server/serve/handlers/client_census.rs=, and maintenance endpoint dispatch
  plus save-refusal rendering into =maintenance_protocol.rs=.
- As a result, 15,190 of the branch's 17,534 added server lines (86.6%) live in
  explicitly skippable maintenance, census, observation or collateral modules.
  The central =server/serve.rs= is now only 97 net lines larger than at the
  pushed base.  The remaining additions in preexisting general modules own
  genuinely general contracts: save fencing/publication, retained view state,
  source configuration and request framing.
- =cargo check --all-targets= and the moved focused tests pass.  With the
  required TypeDB process running, =bash bash/all-tests.sh --no-sound= passed
  all Emacs and Neovim unit suites, all 702 nextest cases, doctests and all 72
  live integrations in 236 seconds.  The temporary TypeDB process was then
  stopped.

* filesystem observers no longer trigger themselves by reading

** live diagnosis
- The runaway process was the real-config development server beneath
  =bash/start-servers.sh= and =cargo-watch=, not a surviving per-test Skg
  process.  The test run had caused that development stack to be started for
  its shared TypeDB, so the timing was related even though the hot work was in
  the persistent real server.
- PID 532438 averaged 99.9% CPU and retained about 564 MB.  Per-thread CPU
  sampling isolated all of the current load to =skg-observer=: it consumed
  208 scheduler ticks in two seconds while every other Skg thread consumed
  zero.  The main thread was asleep in =accept= and the notify, gRPC and
  Tantivy threads were also asleep.
- The observer made about 1.5 million read syscalls before shutdown, and its
  durable real-config journal had reached cumulative observation sequence
  22,126.  There was no corresponding source mutation or logged maintenance
  work.  After retaining the live evidence, I terminated process group 532190,
  which contained that Skg, its cargo watcher and the temporary TypeDB.

** cause and repair
- =notify= 8.2's Linux backend installs =IN_OPEN= as part of every default
  watch.  Both observation callbacks treated every successful notification as
  a mutation.  An exact corpus observation therefore opened a watched =.skg=
  file, the open queued another complete observation, and that observation
  opened the files again.  The Git-presentation callback had the same latent
  feedback path when its observation opened watched Git metadata.
- Both callbacks now discard only events which cannot describe mutation:
  open, read, and close-after-read.  Create, remove, modify,
  close-after-write, and conservative unknown kinds remain observable.  This
  retains cross-platform write coverage while breaking the read feedback
  cycle.
- A synthetic classification regression protects that boundary, and a Linux
  watcher regression proves that reading a watched source queues nothing while
  writing the same source still queues its path.

** validation
- All seven =maintenance::observation= unit tests pass, including the two new
  regressions.  =cargo check --all-targets= and =git diff --check= are clean.
- In a fresh real-corpus run, the startup sweep advanced the durable sequence
  exactly once, from 22,126 to 22,127.  It then remained fixed throughout a
  one-minute sample.  =skg-observer= used about one CPU-second for that sweep
  and slept in =futex_do_wait= afterward instead of consuming a core.  The
  verification development stack and TypeDB were then shut down cleanly.

* first search no longer waits behind hidden quadratic startup work

** reproduction and cause
- Jeff's exact workflow explained why =skg-client-init= appeared to finish
  quickly while the first search stalled.  On the captured run, context/index
  initialization ended and Rust logged =Server ready= at 17:52:31.726, but it
  did not accept the queued interactive connection until 17:53:40.583: a
  hidden 68.9-second gap.  Once accepted, verification, census, three already
  queued herald requests and the search reached Rust together.  The search
  itself produced phase-one results in about 15 ms and enrichment in another
  42 ms.
- Main retired the initialization busy-signal and logged readiness before
  =serve= constructed =ServerRuntime=.  TCP connection establishment could
  therefore succeed into the listener backlog even though no request loop was
  accepting.  Emacs' bounded herald wait returned a prompt confirmation, and
  the first subsequent request simply remained queued until runtime setup
  finished.
- Runtime setup seeded the exact Git presentation signature.  The largest
  configured source is itself a repository with 29,972 index entries.  The
  signature code first collected those paths, then rescanned the complete Git
  index separately for every path to recover its stages: almost 900 million
  entry comparisons.  This quadratic pass accounts for the otherwise silent
  CPU-bound interval.  Later searches were fast because it runs only while
  seeding the process-retained signature.

** repair
- Git index entries are now grouped by scoped path in one pass and removed
  from that map as the stable path-order traversal hashes them.  HEAD/index
  identity and multi-stage ordering are unchanged, but the work is
  =O(n log n)= rather than =O(n^2)=.
- Runtime construction, recovery-journal loading, Git signature seeding,
  watcher installation and background-service startup now all occur before
  main retires the busy-signal and announces readiness.  =serve= receives the
  prepared process runtime and only owns the accept loop.  Thus a future slow
  or failed setup step cannot again produce a false ready/connected interval.

** validation
- On the same real corpus, the complete post-context runtime setup now took
  about 0.234 seconds before the truthful =Server ready= log, versus the old
  hidden 68.9 seconds.
- A headless Emacs probe reproduced the documented setup workflow against the
  real server: =skg-client-init= returned in 0.344 seconds and the first search
  buffer appeared 0.073 seconds after submission.
- The Git presentation-signature regression passes, both maintained
  first-search/title-visit integrations pass under Emacs and Neovim, and
  =cargo check --all-targets= plus =git diff --check= are clean.

* duplicate telescope warnings now say where the duplicates are

** diagnosis
- The warning for =33d5cd4e-8a0c-48f0-85c2-467db86e1862= did not mean that
  the node had two section files.  Its sole section file, in =jeff-public=,
  contains the alias =shows-because (relationship)= twice.
- Alias, ordered-relation and unordered-relation folding all reused a
  =DuplicateMember= warning whose text unconditionally claimed that the two
  occurrences came from different sources.  The deduplication itself was
  correct: it retained the first alias and warned about the second.  Only the
  explanation was false.

** repair and validation
- =DuplicateMember= now records both the source of the retained occurrence and
  the source of the rejected occurrence.  A repetition within one source says
  that the first occurrence won; a real cross-source collision names both
  sources and says which more-public occurrence won.
- A focused regression covers both messages and their source identities.  All
  12 telescope tests pass, as does =cargo check --all-targets=.
- The live real-corpus server regenerated =data/telescope-warnings.org= with
  the accurate message: =member 'shows-because (relationship)' appeared more
  than once in source 'jeff-public'; the first occurrence won=.  The source
  data was deliberately left untouched, so the useful warning remains until
  its repeated alias is repaired.

* reload selectors are not dirty work, and native-undo refusals explain themselves

** transient selector repair
- =*skg-reload-from-id-stack*= had the correct explicit =reload-selector= /
  =maintenance-control= identity, but Org's =TO-RELOAD= changes still set the
  ordinary buffer-modified bit.  The generic census therefore treated the
  transient command input as unsaved work, demanded native-undo archival, and
  could refuse the very reload the selector was created to request.
- Reload selectors are now excluded by the registry's definition of dirty work
  that maintenance must preserve.  This policy fixes already-open selectors as
  soon as the Elisp is hot-reloaded.  Newly constructed selectors additionally
  clear their ordinary modified bit after every change, so their visible Emacs
  state agrees with their recovery policy.
- The ordinary editable =*skg-id-stack*= is unchanged: unsaved edits there
  remain dirty client-local state.  Authored content/search views and attached
  workflows likewise retain all existing recovery protection.

** native undo explanation
- The Emacs handshake formerly encoded a missing =undo-fu-session= package as
  two opaque placeholder strings, producing =client advertised unavailable
  unavailable=.  It now advertises the documented adapter kind =none= and the
  explicit state =not-installed=.  If an unsupported package version is found,
  it advertises =undo-fu-session= plus that actual version instead.
- Server refusals translate both the new representation and the legacy
  =unavailable unavailable= pair.  Missing-package errors now say that exactly
  =undo-fu-session= 0.8 must be installed and that Skg must reconnect afterward;
  wrong-version and Neovim adapter/version mismatches name both the observed and
  required capabilities.
- Installing =undo-fu-session= is no longer relevant to the transient selector,
  but remains required when genuinely dirty Emacs buffers with undo history
  must be archived.

** validation
- Regressions prove that both a newly edited selector and an already-modified
  pre-fix selector report =dirty=nil= and =undo-required=nil= in the census.
  They also cover missing and wrong-version handshake/error messages, including
  compatibility with an old client's placeholder pair.
- All 34 tests in the reload and structured-warning Emacs suites pass, both
  focused Rust capability tests pass, and =cargo check --all-targets= plus
  =git diff --check= are clean.

* the Elisp reload bootstrap resolves its own modules

** diagnosis
- =skg-state.el= acquired a dependency on the project-local =skg-log.el= during
  this branch, but =skg-reload.el= neither loaded that stateful module nor put
  its own directory on =load-path= while resolving =require= forms.
- An already initialized Emacs could mask the omission because =skg-log= and
  other transitive dependencies were already present in =features=.  Starting
  from only the documented =load-file= bootstrap exposed it immediately as
  =Cannot open load file ... skg-log=.

** repair and validation
- =skg-log.el= is now explicitly re-evaluated before =skg-state.el=.  This
  refreshes the logging functions while preserving its live =defvar=
  configuration, just like the other stateful client modules.
- =skg--reload-modules= dynamically prepends its own =elisp-dir= to
  =load-path= for the whole unload/reload transaction.  Every project-local
  transitive =require= can therefore resolve without asking the user's Emacs
  configuration to have installed that path first; the outer =load-path=
  remains unchanged afterward.
- A clean batch Emacs launched from =docs/= successfully completed the same
  =load-file ../elisp/skg-reload.el= then =skg-reload= bootstrap with no
  project directory preinstalled.  Regressions protect both the logging-before-
  state order and the dynamically self-contained require path.  All 26
  reload-focused Emacs tests pass and =git diff --check= is clean.

* incomplete client initialization is no longer reported as a connection

** diagnosis
- A fresh Emacs connecting while the server is already down takes the existing
  immediate-refusal path and reports that it could not connect.  The misleading
  message could instead occur with a retained TCP process which Emacs still
  considered live after the server stopped: the bounded herald-rule requests
  timed out, and initialization mistook their nil result for a successful
  connection with an empty server response.
- Nil from =skg-herald-rules-ensure= means only that no attempt installed a
  valid rule table.  It does not establish that the server returned an empty
  table; the connection may have closed, the handshake may not have completed,
  or the server may have been silent.  The old =skg connected= wording was
  therefore both misleading and too confident about the cause.

** repair and validation
- Initialization now raises a =user-error= and says explicitly that the client
  is not ready whenever no valid rule table is installed.  It distinguishes a
  connection which closed during startup, a live-looking connection whose
  handshake or replies did not arrive, and a verified handshake followed by
  invalid or absent rules.  Each form points to both the user-facing server log
  and the cargo-watch log.
- Herald self-healing and request documentation now use the same accurate
  =no valid rule table arrived= terminology rather than asserting that the
  server sent an empty table.
- Regressions cover all three failure states and prove that the success message
  is reserved for initialization which actually installed rules.  All 428
  Emacs tests pass; three native-undo tests are skipped because
  =undo-fu-session= is not installed.  =git diff --check= is clean.

* replacement startup recovers safely from unfinished pre-archive maintenance

** diagnosis
- The client which requested incident
  =40f56a96-1f6e-407d-b1f6-55255cc5512e= disconnected after beginning an
  explicit partial reload but before supplying an acceptable locked census.
  The durable journal therefore correctly retained the incident in
  =awaiting-locked-census= with =archive_status: preparing=, but recorded no
  frozen buffers, archive, selected store, or external mutation.
- The locked-census refusal (the missing native-undo capability) took the
  generic request-error path.  Emacs displayed the error but never cancelled
  this still-safe incident, so the next server process restored it.
- A new Emacs process has a different client-session ID.  The server correctly
  refused to let it /adopt/ an incident before =archive-ready=, but offered no
  safe way past the abandoned transaction.  Meanwhile =skg-client-init=
  waited only for herald rules, not for the mandatory verification and census
  barrier.  It queued an ordinary herald request after the rejected census,
  elicited =client census must complete before ordinary requests=, lost the
  connection, and repeated the sequence through its herald retries.

** repair
- =skg-client-init= now waits for the complete verification-and-census
  handshake before requesting herald rules.  Verification and ordinary census
  requests retain the server's exact terminal error, stop the initialization
  attempt, and report it once.  A closed or genuinely stalled handshake still
  gets the corresponding transport/timeout explanation and log paths.
- A locked maintenance-census refusal now warns with its exact reason and
  schedules cancellation of that exact incident and epoch.  This prevents a
  future pre-archive capability refusal from stranding another journal entry.
- On reconnect, the owning editor session still resumes its transaction.  A
  /different/ editor session abandons, rather than adopts, an incident only in
  the three phases before archive publication; if the incident carried a disk
  candidate, that candidate returns to pending and is offered afresh.  Anything
  at =archive-ready= or later remains protected by the existing archive-verified
  adoption protocol.
- The verification response names any safely abandoned incident and origin.
  Emacs emits a persistent warning that no disk or server-store mutation had
  begun and that the original command must be repeated if it is still wanted.
  The wire contract and ordering guarantee are recorded in
  =api-and-formats.md=.

** live recovery and validation
- Re-running the documented clean-Emacs setup against the real retained journal
  safely abandoned the old pre-archive incident and changed the journal to
  =idle=.  The original partial reload never performed its requested mutation;
  it must be requested again if still wanted.
- That real initialization completed in about 0.4 seconds, issued exactly one
  verification, one census and one herald request, and reported
  =skg ready on port 1731 -- 24 herald rules loaded=.  The warning named the
  abandoned incident rather than flashing the two secondary protocol errors or
  retrying connections.
- All 433 Emacs tests have 430 expected passes and three expected skips for the
  absent =undo-fu-session=.  All 308 Rust library tests pass serially, and each
  touched maintenance test passes in the ordinary runner.  Three unrelated
  save-leveling tests can interfere through shared process state in a parallel
  full-suite run; each passes alone and the entire suite passes with one test
  thread.  =cargo check --all-targets= and =git diff --check= are clean.

* the reported remote-peer failure coincided with the development restart

** diagnosis
- The implementation commit above changed watched server files.  Cargo-watch
  consequently stopped the old server at =20:03:49= and did not report the
  rebuilt server ready until =20:04:02=.  The reported Emacs error is the
  sentinel event for a TCP connection severed by that shutdown; the server log
  contains no rejected handshake or census from the attempt.
- Once the replacement process was stable, the exact documented clean-Emacs
  initialization completed in 0.4 seconds with 24 herald rules and no error.
  This was a deployment-window failure while the server was going down, not
  another retained-incident or startup-protocol defect.

** restart proof
- A stronger live test kept one Emacs process and client-session ID alive.  It
  connected successfully, forced cargo-watch to perform a real server restart,
  waited for the new =Server ready= event, then evaluated the same
  =skg-reload= / =skg-client-init= path again.
- The second connection used the same client-session ID and issued exactly one
  verification, one census and one herald request.  It completed with
  =skg ready on port 1731 -- 24 herald rules loaded= and no warning or protocol
  error.  The maintenance journal remained idle.  No code change was needed.

* early connections once again report that the server is initializing

** diagnosis and repair
- The server's exceptional =busy-initializing= signal and useful status text
  had remained intact.  Its Emacs handler, however, reset handshake state to
  nil.  The new synchronous initialization barrier consequently kept waiting;
  when the server later closed its temporary initialization socket, the TCP
  sentinel replaced the friendly status with a generic long-form connection
  failure.
- Busy initialization is now an explicit handshake outcome with its own saved
  server message.  =skg-client-init= returns that short message without
  signaling an error or attempting the ordinary herald request.  Closure of
  the temporary socket preserves the outcome, and the next user attempt
  discards that socket and opens a fresh one.
- The TCP sentinel now also ignores events from an obsolete socket after a
  replacement connection has begun.  Thus a late close notification cannot
  erase the new handshake.  The documented busy-signal contract records the
  restored Emacs behavior.

** live validation
- Against a deliberately restarted real server, an attempt made immediately
  after its listener opened returned
  =Server is initializing, please wait. See .../server-to-user.log for progress.=
  promptly and without an error/backtrace.  After =Server ready=, the next
  attempt completed normally with 24 herald rules.
- A second live test kept the same Emacs process alive across both calls.  Its
  early call returned the wait message and its later retry connected cleanly,
  proving that busy-socket retirement and same-session retry compose correctly.
  All 438 Emacs tests have 435 expected passes and three expected skips for the
  absent =undo-fu-session=; =git diff --check= is clean.

* explicit targeted reload can overtake an incidental watcher candidate

** diagnosis
- Editing =056a7de8-d7e5-4fb4-9a42-7e0f088ab898.skg= caused the process-owned
  watcher to publish valid complete-disk candidate
  =4534b931-21fe-49aa-9164-654776d72ac0=.  The ID-stack command then correctly
  requested a new =explicit-partial-reload= for that ID with
  =candidate-id none=.
- The coordinator applied its pending-candidate identity check to every origin
  except full rebuild.  That check is essential when the user accepts
  =pending-reconciliation=, but wrong for an explicit reload: naming the
  watcher's complete candidate would silently expand a targeted command to any
  unrelated changes that candidate happened to contain.  Rejecting the command
  instead made the watcher/selector timing visible as =pending candidate ...
  must be named explicitly=.

** repair and validation
- An explicit path/ID reload now deliberately supersedes either an incidental
  pending watcher candidate or an ordinary observation still in progress.  It
  enters maintenance with no candidate and constructs the exact targeted
  candidate promised by the plan.  The mandatory complete sweep after terminal
  acknowledgement will rediscover and offer any changes outside that target.
- Pending reconciliation still requires the exact candidate ID, and full
  rebuild retains its existing supersession behavior.  Regressions cover all
  four boundaries.  All 311 Rust library tests pass serially,
  =cargo check --all-targets= passes, and =git diff --check= is clean.
- Recompiling this server change restarted the live server.  Its incremental
  initializer found exactly one modified =.skg= file and loaded it, so the
  reported title edit is already selected and the live maintenance journal is
  idle.  A later edit followed by ID-stack reload will exercise the repaired
  no-restart path.

* post-restart search waits for reconnect census instead of escaping it

** diagnosis
- This was not the earlier slow-first-query problem.  The server reported
  =Server ready= at =18:55:34.075=.  The Emacs session connected at
  =18:55:39.167=, sent =verify connection=, then sent the ordinary
  =text search= for =science= only 11 ms later.  It never sent =client census=,
  the server never entered =Finding matches=, and the pre-census protocol gate
  rejected the search and closed the socket.  Waiting another 30 seconds could
  not produce a result because no search had run.
- The request coordinator prioritized a census once the verification callback
  successfully constructed one, but treated completion of the verification
  request itself as authority to dispatch the next ordinary request.  If that
  callback failed before submitting census, its terminal cleanup therefore
  leaked the queued search onto the unverified connection.  The callback error
  was written only to the normally disabled client log, so the visible symptom
  was just an empty wait.
- The affected long-lived Emacs also had evidence of obsolete maintenance
  state: two earlier =begin maintenance= requests reached the server without a
  following client census.  A newly allocated server incident could hit an old
  =skg--maintenance-client-incident= in the callback and fail before replacing
  it.  On reconnect, an idle server likewise left the obsolete local record
  and buffer locks intact.

** repair
- Queue entries now distinguish connection/maintenance barriers from ordinary
  work.  Verification and census barriers may run before connection authority;
  every ordinary request remains queued until the complete client census sets
  the handshake state to =verified=.  A verification callback which fails can
  no longer leak search—or any other ordinary command—past that gate.
- A response-handler failure during verification or census now marks the live
  socket failed, records and displays the exact callback error, and clears its
  queued operations.  The next command replaces that unusable socket rather
  than waiting forever for a census which can no longer complete.
- An idle, observing, or pending handshake is authoritative proof that a
  client-only maintenance record is obsolete.  The client now releases only
  that record's exact-epoch overlays and clears the record while preserving all
  buffer text and undo history.  A successful new incident allocation provides
  the same proof and retires an older client record before installing and
  censusing the new epoch.  This prevents the callback failure which could
  strand another pre-archive incident.

** live state and validation
- A diagnostic connection from a different client session caused the server to
  apply its existing safe rule to the already stranded pre-archive incident:
  it abandoned that incident before any archive, disk mutation, or server-store
  mutation.  The maintenance journal is now idle; any still-wanted reload must
  be requested again.
- Against the running real server, a batch Emacs initialized normally, opened
  the production ID-stack interface, deliberately lost its socket, and issued
  =science= immediately.  The wire order was verification, client census, then
  text search; the result buffer arrived in 0.133 seconds with verified state
  and an empty queue.  A second live run with another registered client-local
  buffer completed in 0.306 seconds, both well below the four-second bound.
- Regressions cover ordinary-request gating, priority census passage, callback
  failure without leakage, failed-socket retirement, idle-handshake lock
  release, and replacement of an obsolete record on new allocation.  All 444
  Emacs tests have 441 expected passes and the three expected skips for absent
  =undo-fu-session=.  =git diff --check= is clean.

* Simplify/cohere execution: baseline, 2026-09-09
** what's done and went well
Starting revision: '06f0aaf5343c96883016af099474dfdbe2fff52d'.
Read the orientation, coding guidance, API, glossary, schema, and the settled
contract in document 11; recursive reference inventory is in progress.
The requested implementation includes ordinary-save crash recovery and removes
TypeDB entirely. Earlier contrary descriptions are superseded by document 11.

Preserved the initial worktree status, patch and existing aggregate test results
in [[file:../../temp/simplify-cohere-baseline/][temp/simplify-cohere-baseline/]]. Existing changes include 'TODO.org',
'bash/docker.sh' and the untracked files recorded there. No Git state changed.

Available commands: cargo, rustc, cargo-nextest, emacs, nvim, typedb, python3.
Baseline 'cargo check --all-targets --jobs 2' is running in the isolated copy;
unchanged editor suites are also being recorded before harness changes.
** Changes to the plan's envisioned order.
The checkout has a live cargo-watch process observing 'server/' and running
'data/skgconfig.toml'. Implementation and verification therefore use the plain
source copy '/tmp/skg-simplify-cohere', with separate build products and
fixtures. Validated changes will be transferred to the working tree together.
This avoids deploying intermediate recovery/protocol changes to the corpus.
** what's imperfect about what's done
No product changes or acceptance claim yet. Baseline results are pending.
** choices determining the future
No new architectural question. Proceed with the model, exclusive owner and
coherent graph/search publication before enabling independent buffer release.

* Simplify/cohere checkpoint: executable model and acceptance accounting
** what's done and went well
The six registered model tests pass under nextest. They exercise both worker
completion orders, obsolete operation/base rejection, constructor draining
before a fixed census, independent buffer settlement, a newer save before old
report completion, stale ACKs during a successor incident, crash before live
publication, fresh session identity and operation-based no-change query waits.
Discovery output is retained in 'temp/simplify-cohere-baseline/model-discovery.log'.
These are specification tests; real filesystem, restart and editor fault tests
remain required and are not claimed by this result.

The unchanged baseline passed 'cargo check --all-targets --jobs 2'. Emacs:
444 tests, 441 expected passes and three undo-package skips. Neovim: all 30
spec files passed. Logs are in the preserved baseline directory.

The aggregate runner now keeps a failed first attempt as acceptance failure
even if its diagnostic retry passes. A disposable fake-tool probe verified
that exit status, and shell syntax checks passed. The two Rust/aggregate
runners no longer invoke the obsolete TypeDB cleanup tool.
** Changes to the plan's envisioned order.
Jeff authorized stopping the test cargo-watch/server and requested regular
commits on the existing 'partial-reload-and-magit' branch. Those processes are
stopped. Work continues directly in the checkout; the temporary linked worktree
was removed. The plain source copy remains disposable baseline material.
User authorization to commit supersedes the older no-Git-change guidance.
** what's imperfect about what's done
The baseline's three native-undo skips must be resolved for the required undo
acceptance coverage. The model is an abstract contract, not adapter evidence.
** choices determining the future
No architectural question. Continue with exclusive state ownership and pinned
read publication; independent release stays gated on durable save recovery.

* Simplify/cohere checkpoint: exclusive coordinator publication
** what's done and went well
The process owner is the sole live coordinator publisher. Callers prepare
copies and submit proposals with an exact publication revision; the owner
rejects obsolete proposals. One ordered worker performs journal I/O while the
owner retains readable committed state. Publication failure withholds further
authority because an error after rename cannot be undone by reverting memory.
Observer and disconnect paths now use the same transition boundary. Terminal
ACK cleanup keeps the compact idle record instead of independently deleting a
possibly newer journal. Startup rejects ambiguous active records and preserves
unused temporary files when a valid committed record proves authority.

Observation has a one-second maximum batch age as well as quiet debounce.
Tests cover sustained notifications, delayed publisher completion, obsolete
proposals, publisher failure, malformed authority and interrupted replacement.
All-target checking passes; all 319 non-ignored library tests pass under
nextest with one test job. Fourteen focused owner/journal/observer tests passed.
Logs are in 'temp/simplify-cohere-baseline/owner-*.log'.

Completed findings moved from 'TODO/problems.org' to 'TODO/DONE/problems.org'.
The recursive orientation synthesis is retained in
'/tmp/skg-recursive-orientation-notes.org'; missing/stale historical links do
not override the current contract.
** Changes to the plan's envisioned order.
The bounded debounce repair was made alongside ownership because it is
independent. A temporary proposal/admission adapter preserves current save
exclusion while graph publication and buffer authority move to owner messages.
** what's imperfect about what's done
The owner currently governs coordinator publication. Selected graph/search and
interactive buffer ownership still use the old adapters; this is an intermediate
checkpoint and does not enable independent buffer release.
** choices determining the future
No new architectural choice. Next pin the actual Searcher, remove ordinary
fallback reads, and converge graph mutation and buffer authority on the owner.

* Simplify/cohere checkpoint: client rebuilding display and prompt refusal
** what's done and went well
Both clients now consume explicit '(rebuilding true|nil)' response metadata.
Their modelines show exactly one orange 'rebuilding' token; explicit false
clears it independently of report/epoch bookkeeping. Known rebuilding,
maintenance and pending-reconciliation restrictions refuse a save before its
transient lock or buffer-marker edits. A raced Neovim save cleanup preserves
an overlapping maintenance restriction and restores prior writability after
both restrictions end. Emacs already keeps the two lock owners separate.

The full Emacs suite passes 444 tests, with the same three missing native-undo
package skips (447 discovered). The complete Neovim unit suite passes.
Evidence: 'temp/simplify-cohere-baseline/rebuilding-ui-*.log'.
** Changes to the plan's envisioned order.
The independent client display/lock helper work ran alongside graph snapshot
migration. It is committed separately for review; server lifecycle metadata
and independent buffer release are still being integrated.
** what's imperfect about what's done
This client checkpoint alone does not establish the server's rebuilding
interval. Server transitions must send explicit status; absent metadata
preserves the last status instead of guessing from broad incident phases.
The baseline native-undo package skips remain unverified coverage.
** choices determining the future
No architectural question. Continue the pinned graph/search publication and
ordinary-save recovery prerequisites before changing release authority.

* Simplify/cohere checkpoint: explicit selected reads and completed search pairs
** what's done and went well
Selected runtime environments now retain the actual Tantivy Searcher.
Search hits, privacy preflight, ID/title reads, negative lookups and enrichment
use that captured graph/search pair. Rendering, completion, save preparation,
extra-ID lookup, source inheritance, fork/merge validation and retained-view
root indexing receive explicit graph inputs instead of recapturing globals
or falling back to newer source files. Ordinary export uses selected content.
Readable Git IDs explicitly request Git evidence; ordinary missing-title
queries do not silently consult changed worktrees.

The Tantivy worker returns completion for its named batch without mutating
selected graph authority. Save publication waits for that exact completion;
a failed index preparation retains the earlier graph/searcher and marks
writes as requiring recovery. Merge repair reconstructs the index without
removing live readers' directory and updates affected ranking neighbors.
Unreachable deleted-node rendering machinery was preserved under
'_burying/not_using/obsolete/typedb/'.

A focused privacy regression found private Contains edges affecting public
HiddenOutside membership. The current-graph path now gates those edges before
classifying membership. The historical-diff variant is separately recorded
as open in 'TODO/problems.org'.

Validation: all-target checking passes; the recorded full library run passed
325 tests with one optional real-corpus skip. Twenty-seven search/title/model
tests pass, including an old Searcher retained across index updates and
ordinary missing-title requests while new disk files exist. Eight focused
render/lookup/privacy tests passed. The source-enrichment test now uses its
actual in-memory fixture nodes and passes. Its first build failure after the
fixture edit is retained separately from the fixed run.
Logs: 'temp/simplify-cohere-baseline/pinned-*.log' and
'/tmp/cohere-focused-nextest2.log'.
** Changes to the plan's envisioned order.
The broad signature change is one checkpoint because removing implicit graph
inputs requires updating every caller together. Pure TypeDB replacements and
ordinary-save journal primitives were developed independently; their active
integration follows this checkpoint.
** what's imperfect about what's done
The runtime still uses temporary selected-publication and mutation adapters.
TypeDB and its remaining global query wrappers have not yet been removed;
this checkpoint does not claim the final single-owner architecture. Git
presentation evidence still needs the later identity/revalidation audit.
Native-undo package skips and full process/editor acceptance remain open.
** choices determining the future
No new architectural question. Switch the verified graph query replacements,
remove TypeDB, and integrate durable source effects and owner reservations
before enabling independent release.

* Simplify/cohere checkpoint: explicit graph query primitives
** what's done and went well
The new 'dbs/graph_queries' modules provide synchronous relation, identity,
ancestry, path, subscription and statistics queries over an explicit graph.
Visibility gates apply to the origin, relationship provenance and destination.
Nine focused regressions pass, including private containment not changing
public link classification. Evidence: '/tmp/skg-pure-graph-queries-test-final.log'.
** Changes to the plan's envisioned order.
The replacement query primitives are committed before active caller migration,
so the behavioral replacement and mechanical adapter removal remain reviewable.
** what's imperfect about what's done
No new defect identified in this checkpoint.
** choices determining the future
No architectural choice. Active callers and fixtures now migrate to these APIs.

* Simplify/cohere checkpoint: recoverable ordinary-save filesystem primitive
** what's done and went well
The standalone save journal retains checksummed before/after bytes and config
interpretation evidence before authorizing any source mutation. Prepared and
authorized batches are distinct; replacements use restrictive same-filesystem
staging, synced files/directories and recoverable per-path progress. Whole-batch
conflicts fail before further effects. Committed operation identities retain
the exact result and never replay files after a newer save, even with a lost ACK.

Thirteen focused tests pass, including six actual SIGKILL/restart boundaries,
partial staging/outcome publication, malformed records, evidence corruption,
private permissions, deletion and exact versus conflicting duplicates. The
full library suite passes 347 tests with one optional corpus skip.
** Changes to the plan's envisioned order.
Filesystem durability was developed as a separate primitive alongside read
migration. Runtime startup, owner ordering and editor integration follow;
independent buffer release remains gated on those real paths.
** what's imperfect about what's done
The primitive loads retained operation records for validation. Later completion
compaction must preserve duplicate rejection while avoiding unbounded retained
response work on ordinary requests. No automatic archive expiry is introduced.
** choices determining the future
No architectural question. Integrate this journal with one combined save batch,
startup recovery and durable client operation identities before early release.

* Simplify/cohere checkpoint: durable nonmutating save outcomes
** what's done and went well
The save journal can now retire a staged or prepared request with a durable
refusal/confirmation outcome, without authorizing its file mutations. Once
effects are authorized, this shortcut is rejected: recovery or commit remains
required. Duplicate authorization/application of a refused operation leaves
source bytes unchanged, and startup recognizes its completed obligation.

The current TypeDB-free library run passes 353 tests with one optional corpus
skip, including the new refusal regression and owner reservation cases.
Evidence: 'temp/simplify-cohere-baseline/typedb-free-library-fixed.log'. The first
lib-test build's obsolete fixture signatures and missing test import are
retained in 'typedb-free-library-first.log'.
** Changes to the plan's envisioned order.
This journal extension is committed separately while the larger TypeDB fixture
migration continues, preserving regular reviewable durability checkpoints.
** what's imperfect about what's done
No new defect identified in this checkpoint.
** choices determining the future
No architectural question. Runtime response integration will distinguish a
known refusal from a blocked request with unresolved authorized effects.

* Simplify/cohere checkpoint: retire the active TypeDB runtime
** what's done and went well
The production build, startup, query, render, merge, and save paths now use
the explicit Rust graph and Tantivy. The driver dependency, obsolete service
configuration fields, global graph lookup handles, scheduled audits and
TypeDB-only binaries are removed. Historical implementations and schema are
retained under '_burying/not_using/obsolete/typedb/'.

The fixture migration is being validated target by target. The current
integrated library passed 359 tests with one optional corpus skip; this
includes owner and durable-save changes which will be committed separately.
'grouped_unit', 'grouped_views' and 'grouped_overrides' compile with the
new explicit graph fixture helpers. External save/source targets and final
acceptance remain in progress.
** Changes to the plan's envisioned order.
The TypeDB production migration is checkpointed separately from the external
fixture migration, server owner integration and editor protocol work. Saved
production checkpoint bytes keep these concerns reviewable while preserving
all later worktree edits. Editor durable operation records were independently
committed as 'ae2c7f77'.
** what's imperfect about what's done
The production migration does not yet establish TypeDB removal from every
setup document and runner. Full server/socket verification found two adapter
issues: routing fields are appended after the logical save fingerprint, and
compatible observer journal publication could spuriously refuse a save. Both
are being corrected with retained first-failure logs.
** choices determining the future
No new architectural question. Continue fixture migration, socket/process
crash coverage and fresh-session authority before completing the maintenance
lifecycle and its acceptance tests.

* Simplify/cohere checkpoint: mutation reservations and owner admission
** what's done and went well
The decision owner now holds the selected graph/search pair and opaque
operation reservations. It admits saves, metadata-only manifest updates and
maintenance selection against both current and pending journal decisions.
Compatible observation/status publication does not randomly refuse saves;
a competing barrier or stale graph/manifest base does. Authorized work cannot
release its reservation by dropping a worker or reporting an error.

Blocked-operation recovery uses an exact status comparison and rotates the
reservation token, invalidating abandoned worker controls. Metadata-only
publication preserves the graph and actual Searcher identity. Selection
publication can finish after disconnect only under its previously authorized
incident and selection scope. Twenty-one focused owner tests pass in
'/tmp/skg-owner-admission-tests3.log'.
** Changes to the plan's envisioned order.
Owner primitives are committed independently from their production save and
selection adapters. The adapters already pass the developing full-server
socket suite and are the next checkpoint.
** what's imperfect about what's done
The owner still has temporary adapter boundaries around interactive state
and workers. This checkpoint does not claim complete UI responsiveness or
independent post-publication incident settlement.
** choices determining the future
No new architectural question. Wire and verify the durable adapters, then
move committed incident settlement off the graph-changing state.

* Simplify/cohere checkpoint: native undo coverage executes
** what's done and went well
Installed the existing local undo-fu-session 0.8 package in this environment.
Its normal byte-compilation exposed an adapter bug: the version detector read
the compiled file for a source header and reported the supported package as
absent. The detector now checks its packaged source. All five focused native
undo tests pass with zero skips, including save/restore and a compiled-package
regression. Evidence: 'temp/simplify-cohere-baseline/native-undo-required-fixed.log'.

The batch unit runner initializes installed Emacs packages and explicitly
requires the supported undo version before accepting the suite. Integration
launchers receive the same package initialization with the harness checkpoint.
** Changes to the plan's envisioned order.
Resolved the baseline native-undo skips while production socket tests and
fixture migration were running independently.
** what's imperfect about what's done
The initial focused dependency run exited successfully but retained three
skips; its log is preserved as 'native-undo-required.log'. Only the fixed run
establishes undo coverage. Full editor acceptance will run after protocol work.
** choices determining the future
No architectural question. Continue the server/editor lifecycle work.

* Simplify/cohere checkpoint: durable server saves and selection adapters
** what's done and went well
Ordinary interactive saves retain exact request interpretation, before/after
bytes and the combined ordinary/merge batch before effect authorization.
Owner reservations span preparation, authorized filesystem effects, matching
graph/search publication and durable terminal outcome. The server sends a
successful save result only after the outcome is durable. Generated merge
preservation nodes and acquiree deletion belong to the same batch.

Startup resolves retained source effects before initializing the graph/index,
records recovered outcomes before runtime admission, and gives the new process
a fresh session ID. Runtime construction itself rejects unresolved save
records, preventing alternate entry points from bypassing that ordering.
Protocol version 2 is mandatory before interactive attachment. Census cannot
recreate old authority merely from equal counters and client-supplied text.
Retained duplicate outcomes keep their original issuing session.

All three maintenance selection adapters now reserve through the owner;
reader draining is gone. Failed index restoration retains the old Searcher
for queries and keeps writes blocked. Proved restoration can release the
reservation. Refused observation proposals requeue their exact-check hint.

Hoist classification, repair text and interactive filesystem preparation use
the selected graph. A new regression changes disk after selection and proves
that preflight keeps the selected provenance and repair text. Offline file
writers still explicitly inspect disk. Five Hoist tests and three selection
restoration tests pass.

Real TCP tests execute the actual runtime/request loop in a disposable child
process, SIGKILL it after staging, authorization, each of two path effects,
complete application, pair publication and durable commit, then start a fresh
process against the same source/private-state directories. Each recovered
operation permits a newer save, and the old duplicate never rewrites those
newer bytes. Protocol mismatch is also exercised over TCP. The final focused
run passes five tests including all seven crash schedules in 4.65 seconds:
'durable-runtime-focused-final.log'.
** Changes to the plan's envisioned order.
Fresh server-session authority is integrated with durable save delivery
because an old response cannot safely grant a new process's authority. Both
client adapters are being checkpointed independently. Socket tests accompany
the server integration; maintenance crash/lifecycle schedules follow it.
** what's imperfect about what's done
Initial socket runs exposed a test response-decoder mistake, logical versus
transport save-envelope binding, and an overly strict owner refusal during
compatible observer publication. Their first logs remain preserved. A full
library run found three restoration fixtures using an obsolete synthetic
reservation spelling; the corrected focused fixtures pass.

The socket tests exercise fresh-process server recovery, but do not yet
establish the entire editor crash and independent-release matrix. Interactive
state/worker adapters, other source-changing commands and remaining manifest
preflight dependencies still need the common final boundary.
** choices determining the future
No architectural question. Continue fixed census, committed-incident ledger,
per-buffer settlement, durable query waits, command integration and final
acceptance.

* Simplify/cohere checkpoint: both clients bind authority to a server session
** what's done and went well
Both clients require protocol version 2 and a valid server-session UUID before
installing handshake authority. Buffer records retain their original session;
reconnect does not renew existing authority. Census and canonical save intents
carry that origin, while transport-only requests carry the active connection
session. Queued requests are stamped after verification. Stale responses are
rejected before replacing authored text or undo.

The complete Emacs suite passes 463/463 with native undo coverage; the complete
Neovim suite passes without failures or errors. Logs are
'emacs-protocol-v2-final.log' and 'nvim-protocol-v2-full.log'.
** Changes to the plan's envisioned order.
Client session enforcement follows the independently committed server protocol
and durable-save boundary. No further change in order.
** what's imperfect about what's done
The passing client suites establish session validation and text preservation;
the remaining maintenance lifecycle and process-crash acceptance are separate.
** choices determining the future
Continue the accepted plan. The user's budget preference survived compaction:
use short, bounded briefs for simpler delegates, prefer Terra with high
reasoning for suitable new coding tasks, and reuse existing context when that
avoids redundant orientation. Retain stronger agents for cross-cutting work.

* Simplify/cohere checkpoint: external fixtures no longer require TypeDB
** what's done and went well
All seven active external Rust target groups use explicit Rust-graph fixtures
and pass serially: 399 tests. The library run passes 380 tests with one ignored.
Active Rust tests and fixture helpers have no TypeDB driver, database-name, or
process-global graph dependencies. Useful graph queries remain covered;
TypeDB-specific history lives outside active test discovery.
** Changes to the plan's envisioned order.
Checkpoint the fixture migration before final aggregate editor/server gates.
** what's imperfect about what's done
The graph ancestry expectations now specify the documented breadth-first
repeat placement. A restricted-save expectation permits an omitted no-op
SaveNode and instead checks the effective graph state. Neither migration run
establishes the remaining real-editor maintenance lifecycle schedules.
** choices determining the future
Continue the agreed integration and acceptance work without a new decision.

* Simplify/cohere checkpoint: setup and platform service cleanup
** what's done and went well
Current setup, usage, command and Windows documentation no longer requires
TypeDB. The Windows image and launcher consistently use the existing
'run-servers.sh' and start Skg alone. Obsolete troubleshooting material is
preserved under '_burying/not_using/obsolete/typedb/docs/troubleshooting/'.
Terra with high reasoning handled this bounded cleanup from a short brief.
** Changes to the plan's envisioned order.
No change; this independent setup cleanup ran beside lifecycle integration.
** what's imperfect about what's done
Shell syntax, the Python helper, whitespace and relevant local links pass.
PowerShell syntax parsing was unavailable because 'pwsh' is not installed;
no Windows execution claim is made.
** choices determining the future
Continue integration and full acceptance on the available platform.

* Simplify/cohere checkpoint: fixed census and committed incident ledger
** what's done and went well
The coordinator separates current graph-changing authority from older committed
incidents. Pair readiness moves an incident into its independent ledger entry;
reports, evidence transfer and exact buffer acknowledgments can continue while
a newer incident or save proceeds. Frozen census validation includes exact dirty
and undo obligations. Replayed terminal and buffer acknowledgments cannot
mutate a successor incident with the same buffer IDs.

The owner checks a readiness proposal against the graph and manifest it has
actually published. Journal version 2 preserves and checks the exact legacy v1
bytes before migration, retains unsupported or unprovable obligations for
inspection, and refuses malformed active authority. All 68 focused coordinator,
journal and owner tests pass in '/tmp/skg-incident-ledger-final-tests2.log'.
** Changes to the plan's envisioned order.
The pure lifecycle and owner publication checks are checkpointed before all
server/client adapters finish adopting the incident ledger.
** what's imperfect about what's done
An initial combined run caught a new cross-incident fixture with inconsistent
URI/revision/token bases. Corrected exact-base fixtures pass. Abort handling
around unresolved external effects still needs the planned integration guard.
** choices determining the future
Continue the accepted lifecycle integration and restart evidence reconstruction.
