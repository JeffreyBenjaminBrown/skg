* HOW TO USE THIS FILE
  Open problems only. As soon as a problem here is *done* -- fixed,
  resolved, or obsolete -- MOVE its entry out of this file and PREPEND
  it to the top of TODO/DONE/problems.org. Do not let finished entries
  accumulate here.
* Problems noted while diagnosing the 2026-07-14 power-loss startup failure
  Context: Jeff's machine overheated and cut power mid-'cargo build',
  truncating 3 of 501 rlibs in target/. Servers then refused to start,
  permanently and identically, on unchanged code and unchanged data.
  Fixed by 'cargo clean -p skg -p tantivy -p typedb-driver'. Written up
  in docs/troubleshooting/build-artifacts-corrupted-by-power-loss.org.
** start-servers.sh misreports a LINK failure as a COMPILE failure
   'preflight_check_config' (bash/start-servers.sh) wraps
   'cargo build -q --bin skg' and, on failure, prints "ERROR: skg
   failed to compile (see errors above)". In the 2026-07-14 incident the
   crate compiled fine; it failed to *link*, against a truncated rlib
   ("file too short"). The wording sends you looking for a source bug
   that does not exist -- and since the user's premise is "the code
   hasn't changed", the message reads as nonsense and pushes them toward
   TypeDB, which was innocent. Suggested fix: on build failure, add a
   line pointing at
   docs/troubleshooting/build-artifacts-corrupted-by-power-loss.org and
   at 'python3 docs/troubleshooting/check-rlibs.py', noting that a
   linker error on unchanged code usually means truncated artifacts in
   target/. Cheap, and it would have collapsed this whole investigation
   into one command.
** Cargo cannot self-detect its own truncated artifacts (design hazard)
   Cargo judges freshness by fingerprint + mtime and never checksums its
   output. If a machine dies AFTER a fingerprint is written but BEFORE
   the artifact finishes streaming to disk, Cargo believes a truncated
   file is fresh and re-hands it to the linker forever. So the failure
   is permanent, not flaky, and 'cargo build' cannot recover on its
   own. Nothing to fix in skg -- but worth knowing, because the
   "re-run it and see" instinct is useless here, and the natural next
   inference ("must be the data / must be TypeDB") is wrong.
   check-rlibs.py exists to short-circuit that.
** Empty section header in typedb-will-not-start.org
   docs/troubleshooting/typedb-will-not-start.org ends with the bare
   header "* Database Corruption (Non-Accumulation Issues)" and no
   content beneath it. Either write it or drop it; as it stands it
   promises help that isn't there, to a reader who is already stuck.
** Broken command paths in the troubleshooting docs
   typedb-will-not-start.org tells the reader to run
   'bash troubleshooting/typedb/check-typedb-status.sh' (lines 30, 146,
   152 and the caution at 61). There is no 'troubleshooting/' directory
   at the repo root; the scripts live under 'docs/troubleshooting/'. As
   written, every one of those commands fails with "No such file".
   database-cleanup.md has the same stale prefix.
** database-cleanup.md names the wrong development database
   It states the dev database is 'skg-dev' ("Database Naming
   Convention", and the 2025-11-18 "Configuration Issue Fixed" note).
   data/skgconfig.toml actually sets db_name = "skg-real". The naming
   *rule* it teaches is still right (never prefix a real db with
   "skg-test", or the cleanup utilities will eat it); only the example
   name is stale.
** Server logs grow without bound
   data/logs/ currently holds ~500 MB across three files that are only
   ever appended to, never rotated or truncated:
   server.jsonl (275 MB), server-to-user.log (124 MB),
   cargo-watch.log (105 MB, appended with '>>' by start-servers.sh).
   Nothing trims them. They are also the files a crash leaves NUL-padded,
   which makes them progressively less pleasant to read. Suggested fix:
   rotate on startup (keep one previous generation), or cap by size.
* Problems noted during the fork-fixes arc (TODO/fork-fixes.org)
** The git_diff_view test matcher ignores bare metadata atoms
   'assert_buffer_contains' (tests/git_diff_view/common.rs) matches
   each expected line by star depth, title, and PARENTHESIZED
   metadata fragments only ('extract_metadata_fragments'). Bare
   atoms -- 'folded', 'focused', 'indef', 'bodyFolded' -- are
   invisible to it, in both directions: an expected atom missing
   from the actual line, or a stray atom appearing in it, cannot
   fail these tests. Found 2026-07-08 while adding the
   begin-folded rule for hidden cols: a probe atom inserted into an
   expected line still passed. Regressions in fold/focus/indef
   marking on diff views are therefore unpinned by that suite
   (tests elsewhere, e.g. tests/hidden_from_subscriptions.rs, use
   exact assert_eq! and do pin them). Suggested fix: also compare
   the bare-atom sets of the two lines in 'line_matches'.
** Subscribee-as-such hides may misfire under a restricted source-set
   The signal loop in 'resolve_visibility'
   (server/from_text/local_instruction_collection/resolve_visibility.rs)
   infers hides as: subscribee's disk contains, minus the children the
   buffer shows, minus the subscriber's post-save contains. Under a
   RESTRICTED source-set, rendering omits a subscribee's
   inactive-source members from the view, so they are absent from the
   signal's 'visible' list for a reason other than deletion -- yet the
   loop would infer hides for them, silently writing
   hides_from_its_subscriptions entries for members the user never
   saw. The new sibling pass 'infer_hides_from_contains_removals'
   guards exactly this with 'member_is_visible'; the older signal loop
   does not. Narrow trigger (needs a definitive subscribee-as-such
   saved under a restricted set, with the subscribee containing
   inactive-source members); not fixed in the fork-fixes arc because
   it is prior, separate behavior. Suggested fix: apply the same
   member_is_visible filter to that loop's 'inferred_hides'.
* Problems noted while writing forking-and-privacy.org
** Fork deliberate-pick rule is enforced only client-side
   docs/forks.md promises "approving before you have set every
   clone's source is refused", to keep a fork from silently landing
   in a default source. The refusal lives in 'skg-approve-fork'
   (checks for the PICK-A-SOURCE placeholder); the server's only
   backstop is that the placeholder is not a configured source. A
   client that re-sends the save with (fork-approved . "true") and NO
   (fork-sources ...) field commits the clones into the
   inferred-or-default source ('fork_spec_from_buffer_node' falls
   through user-set -> inferred -> default) -- exactly the silent
   default the docs rule out. Immaterial while Emacs is the only
   client; if other clients appear (nvim/), consider having the
   server refuse an approved save whose fork_sources map does not
   cover every fork spec.
** Cosmetic: api-and-formats.md says search filters "before grouping"
   The text-search section says the active source-set is applied
   "before grouping, ranking, alias/title selection, and display
   truncation". In code, 'group_matches_by_id' runs first and
   'filter_match_groups_to_active_sources' then filters the groups,
   before ranking and truncation. Behaviorally identical (groups are
   per-ID and source is a function of ID), so this is a doc wording
   nit only.
* Problems noted during the forks arc (TODO/forks/)
** Quasi-duplication: "Source '...' not found in config" across ~9 sites
   The plan asked to consolidate the duplicated "source not found"
   error into one 'SourceValidationError'. The recon found the
   duplication is wider than the three sites named (server/util.rs,
   dbs/filesystem/multiple_nodes.rs x2, plus copies in not_nodes.rs,
   git_ops/read_repo.rs, diff_analysis/snapshot.rs) -- ~9 literals of
   the same format string -- across three carrier types (io::Error,
   Result<_, String>, BufferValidationError). A clean consolidation
   needs an enum with both an ID-less 'NotInConfig(SourceName)' shape
   (the fs layer has no node id) and ID-bearing shapes, plus
   From-impls bridging to io::Error / Box<dyn Error>. I added typed
   fork-specific errors (ForkSourceUnresolved / ForkAlreadyExists /
   ForkSourceInactive on BufferValidationError) but DEFERRED the broad
   string consolidation -- it is a nice-to-have touching fs/git/diff
   layers, out of proportion to the fork work. Worth doing on its own.
** Minor: confirmation-buffer sourceHerald shows the raw source name
   'build_fork_confirmation_buffer' emits the clone's '(sourceHerald
   ⌂:NAME)' with the raw SourceName, not the configured abbreviation
   ('SkgfileSource::herald_label') that every other rendered view uses.
   Left raw deliberately: the client's C-c s s rotation also writes
   '⌂:<raw-name>', so the confirmation flow is self-consistent; fixing
   only the server side would make the initial display disagree with the
   post-rotation display. Cosmetic; the load-bearing '(source X)' atom is
   unaffected. (Noted in case a future change routes BOTH sides through
   herald_label.)
* Problems found during herald overhaul research
** (mostly fixed) Save validation and extraction use different edit-request surfaces
   The save extraction path uses 'collect_savenode_candidates' to define
   which view positions can produce save/merge instructions. Some
   validation still walks the whole viewforest directly, especially
   merge validation and contradictory instruction collection.

   That can make validation and execution disagree for edit metadata
   placed under display/scaffold positions: validation may see it even
   when extraction ignores it, or vice versa if the candidate policy
   changes. The cleaner fix is probably to validate write-intent
   metadata through the same candidate/role policy used by extraction,
   and explicitly reject edit requests outside that surface.

   Mostly fixed 2026-06-10 by local instruction collection: extraction
   now recurses into every position, so a definitive node's self-edits
   (including edit requests) are honored wherever validation can see
   them. The residual gap is positions where edit requests are
   deliberately ignored rather than rejected: a Delete or nodeMerge
   request on a subscribee-as-such is silently dropped, which validation
   could instead flag.
** Save rerender may not insert missing HiddenInSubscribeeCol
   While experimenting with the "delete as-a-subscribee content
   implies hide" feature, I saw a case where the subscriber's
   'hides_from_its_subscriptions' list had gained the hidden child,
   but the returned saved view still did not show a
   HiddenInSubscribeeCol under the subscribee.

   The likely reason is that the save rerender path completes an
   existing HiddenInSubscribeeCol in postorder, but a buffer that
   initially had no hidden children also has no scaffold to complete.
   The eventual feature implementation probably needs to ensure that
   a HiddenInSubscribeeCol is inserted when the updated hide list
   intersects a subscribee's content.
* Problems found during the staged/unstaged diff axis refactor
** (dormant) 'mk_removed_child_viewnode' hardcodes unstaged=Minus axes
   Dormant as of 2026-06-12: 'mk_removed_child_viewnode' now lives in
   server/to_org/expand/unused_but_nice.rs ("Held for someday, not
   currently wired in"), so the bug is unreachable in the current
   renderer. NOT closed: the per-stage-sign fix must land before that
   module is ever revived.
   server/to_org/expand/definitive.rs lines 394, 398.
   When a parent .skg file is deleted, its children are loaded from
   git HEAD and rendered as phantoms. Both existence and membership
   axes are hardcoded as 'staged: None, unstaged: Some(Minus)',
   which misattributes already-staged parent-deletions to the
   unstaged side. Sibling bug to the one just fixed in phantom_axes
   (types/phantom.rs). Fix is analogous: derive per-stage signs from
   the parent's file-level diff status (staged vs unstaged maps in
   SourceDiff), rather than assuming the change is on the unstaged
   side. Additionally, for a deleted-parent scenario the child's
   membership axis should probably mirror the parent's existence
   axis (if parent removed staged, children are 'removed from
   contains' staged too), which is a bit more nuanced than the pure
   contains_diff lookup used in phantom_axes.
** Three NodeComplete parses per changed file in 'compute_diff_for_source'
   server/git_ops/diff.rs.
   For each changed file we now load HEAD, index, and worktree YAML
   blobs and parse them into NodeCompletes. A small repo with N changed
   files does 3N parses. At current sizes this is fine but worth
   measuring on a larger garden.
** 'NotInGit' is per-source but stamped per-truenode
   server/to_org/render/diff.rs / process_truenode_diff.
   Every truenode in a non-git source gets 'not_in_git = true'. The
   wire-level emission is a per-truenode 'notInGit' atom too. A
   per-source banner / source-herald prefix would scale better.
* Performance
** 'fetch_all_graphnodestats' scales linearly in pids, ~60ms each
   server/dbs/typedb/search/all_graphnodestats.rs.

   The file header promises "one TypeDB query PER PID, all in parallel"
   and cites ~15ms per query. In practice the TypeDB server (or the
   driver) caps effective concurrency at roughly 4, so the total time
   is (pids * ~250ms) / 4. Small views finish in well under a second;
   medium-big views take multiple seconds.

   Measured on data/public/ (29k .skg files):
   - visit 'science' (root with 6 contents, ~10 pids in view): 0.68s total,
     with fetch_all_graphnodestats ~0.35s.
   - visit 'to watch' (root with 3 contents, ~95 pids in view): 5.5s total,
     with fetch_all_graphnodestats ~5.0s.

   Verified by forcing join_all -> a sequential await loop: per-query
   time was ~250ms, total scaled 1:1 with pid count ('science' 2.5s,
   'to watch' 23.5s). So join_all IS achieving parallelism; there is
   just a ~4x ceiling that eats the gains as views grow.

   Per-pid breakdown (emitted from temporary tracing in
   fetch_one_pid_stats):
   - tx.transaction(..) await  : ~8ms
   - tx.query(..) await returns: ~270ms avg (it already waits some)
   - first stream.next() await : +~4.5s (!) -- this is where to-watch
     spends its time; most pids wait in a TypeDB-side queue before
     their stream yields a document
   - draining the stream       : negligible (single-row result)

   The code comment "1 PID: 15ms, 24 PIDs: 4300ms" compared only the
   old N-way-disjunction approach against the new per-pid approach.
   The new approach is better than the old one but is still O(N), just
   with a smaller constant and an unknown parallelism cap.

   Options to try, in decreasing order of likely payoff:

   1. Batch many pids into a single TypeDB query. Something like

        match
          $node isa node, has id $id;
          { $id == "pid1" } or { $id == "pid2" } or ... ;
        fetch { "id": $id, "containers": [...], ... };

      returning one document per pid. Even if TypeDB evaluates the
      disjunction poorly for joins, a fetch that only *dispatches* on
      id should still be 1 query instead of N. Would remove the per-
      transaction and per-round-trip overhead entirely.

      TODO (Jeff speaking): We tried (1) once long ago. It was slower.

   2. Share one transaction across all pids (driver.transaction once,
      then run N queries on it). Might bypass whatever per-transaction
      bottleneck is capping concurrency at ~4.

   3. Split fetch_one_pid_stats into 6 separate relation-specific
      aggregate queries ("find all containers of nodes in {pids}",
      "find all contents of nodes in {pids}", etc) with the id set
      passed in as a TypeDB collection. Each query does one relation,
      across all pids, and folds results pid-by-pid in Rust.

   4. Cache. The same pid can appear in many views within a session;
      results are stable between writes. A small in-memory cache keyed
      by pid and invalidated on save would eliminate repeats.

   Until this is fixed, any view with more than a few dozen nodes
   (i.e. any real working set above initial_node_limit/2) will have
   multi-second first-render latency.
* Other problems
** Windows packaging request names scripts as top-level =bash/= scripts, but they live in =data/bash/=
   The requested files =pull-all.sh=, =push-all.sh=, =remote-all.sh=,
   and =status-all.sh= are not present under top-level =bash/= in this
   checkout. They exist under =data/bash/=, which appears to be
   gitignored/private data.

   If these are intended to be packaged for new users, they should
   probably live in a tracked template/starter-data location, or the
   Windows packaging plan should explicitly copy them from the private
   =data/bash/= source.

** 'bash/start-servers.sh' process management is not safe for multiple Skg servers in one container
   Found while writing docker-unification-strategies.org.

   'start_skg_with_restart' runs broad process kills:
   - 'pkill -f "cargo watch.*skg"'
   - cleanup runs 'pkill -f "cargo watch.*skg"' and
     'pkill -f "cargo run --bin skg"'

   That is fine-ish when each Skg instance has its own container, but in a
   unified container it means starting or stopping one instance can kill the
   production server or another coding session.

   Fix direction: manage Skg processes per config/data-root, probably with
   PID files under the data root's logs/run directory, and split TypeDB
   lifecycle management from Skg server lifecycle management.
** Long-running SKG server can hide code/protocol fixes
   On 2026-05-04, the server process listening on port 1730 was still
   an older / already-running binary. It accepted 'titles by ids'
   requests and logged them, but did not send responses, while the
   current source-level handler test sent a parseable response. This
   makes client bugs look nondeterministic after code changes. A small
   version/build-id endpoint, or logging the executable build hash at
   startup, would make stale-server cases obvious.
   PARTLY RESOLVED 2026-07-03 (fable-cleans-up): the logging remedy is
   done -- startup now logs the executable's path and age ("Server
   binary exe=... binary_age=...", server/main.rs, right after
   init_tracing). Open only in case a client-visible version endpoint
   is still wanted.
** Emacs client can hold stale TCP state after server/data replacement
   Observed 2026-05-06 after stopping the server, replacing data/,
   and restarting. The Rust server was listening on 1730 and a fresh
   batch Emacs process could run 'skg-connection-verify', but the live
   user session could not.

   Likely recovery is 'M-x skg-connection-end' followed by
   'M-x skg-connection-verify' or re-running 'skg-client-init'.
   The client could be more robust if request send failures forced
   'skg-rust-tcp-proc' to nil and retried once.

   Related: 'skg-client-init' uses '(defvar skg-port ...)' rather than
   '(setq skg-port ...)', so reinitializing with a replaced config will
   not update an already-bound port variable. That is harmless when the
   port stays 1730, but surprising when switching configs.
** Initialization warning files can be easy to miss
   On 2026-05-06, startup completed and logged "Server ready", but
   data/ also contained:

   - initialization-error_duplicate-ids.org: duplicate id
     0a6dcf44-6c2c-432a-90a7-babfbb3e0b7d in pers-deprecated and public.
   - initialization-error_unreadable-skg-files.org: unreadable
     /home/ubuntu/data/private for source private.

   These did not block connection verification, but they are important
   enough that the client should probably surface them after init.
** Replacing data/skgconfig.toml can leave server and client on different ports
   Observed 2026-05-06: data/skgconfig.toml said 'port = 1731',
   but the already-running server process was still listening on 1730.
   'cargo-watch watch -w server/ ... data/skgconfig.toml' watches
   server/ only, so replacing data/ or editing skgconfig.toml does
   not restart the server with the new config.

   The result is confusing because '(skg-client-init "../data/skgconfig.toml")'
   correctly points Emacs at the new config port, while the live Rust
   process remains bound to the old one.

   Possible fixes: watch the config file too, log the active port and
   config path in a user-visible way, or add a client diagnostic that
   explicitly says "config wants port X but no server is listening".
** One TypeDB process has a fixed idle CPU tax
   Investigated on 2026-05-14;
   see 'TODO/typedb-performance-research.org'.
   After Skg is ready and no requests are arriving, TypeDB CE 3.10.3
   still keeps TypeDB-owned threads waking from timer sleeps and using
   a few percent of a CPU. This continues after shutting Skg down, so
   it is not a Skg request loop or the live Rust driver connection.

   The practical issue is multiplicative: three independent Skg
   containers mean three TypeDB server processes and roughly three
   fixed idle taxes. The likely mitigation is one shared TypeDB server
   with distinct database names per Skg instance, plus separate Skg
   ports/configs.
** Diff analysis still does whole-source work for tiny diffs
   Investigated on 2026-05-22;
   see 'faster-diff-analysis.org'.

   After the changed-file prototype and endpoint snapshot cache, clean
   diffs are fast and repeated one-change reports are much faster. The
   next win is cached inverse indexes or reusable 'GraphFacts' keyed by
   the same endpoint identities.
** GraphStats rendering still lacks parent-kind context
   Observed on 2026-05-31 while splitting 'ParentIs' from 'Birth'.

   'server/org_to_text.rs' renders a TrueNode's graphStats from only
   the TrueNode metadata. After 'Container' and 'Collector' collapsed
   to 'ParentIs::Affected', relation collection children and ordinary
   content children can no longer be distinguished there.

   The local workaround hides an affected node's container count when
   it is 0 or 1, which preserves the old rendered behavior for relation
   collection members and ordinary one-container content. A more exact
   implementation would pass parent tree context into metadata
   rendering, so a scaffold child and a normal Vognode child can use
   different graphStats suppression rules.
* Problems found while taking over the local-view-update dialog (2026-06-02)
  Found while answering the open questions in
  TODO/local-view-update/problems.org. Each is cross-referenced there.
** Diff scaffolds are not added to nodes made definitive by a view request
   server/update_buffer/complete.rs, server/to_org/expand/definitive.rs.
   'ensure_diff_scaffolds' (complete.rs:50) runs once, before
   'execute_truenode_view_requests' (complete.rs:56), and only for nodes
   that are already definitive and non-phantom. A node that becomes
   definitive *during* a definitive view request
   ('execute_definitive_view_request') is never passed to
   'maybe_prepend_diff_view_scaffolds', and the content it expands via
   'extendDefinitiveSubtreeFromLeaf' never gets 'set_diff_status' either.
   So interactively expanding a node definitively while git diff mode is on
   yields content with no TextChanged/IDCol/AliasCol scaffolds and no diff
   axes until the next full rerender.

   Needs a test to confirm reachability (diff mode + a definitive view
   request on a previously-indefinitive node with changed text/ids/aliases
   or changed content). The planned "separate diff pass over Vognode::Normal"
   (TODO/local-view-update) would fix it by visiting every Normal vognode
   regardless of when it became definitive.
** Collateral graphStats can go stale when only a node's relationships change
   server/update_buffer.rs / find_collateral_view_uris.
   Collateral views are selected by the PIDs in the SavePlan's DefineNodes.
   If saving buffer X changes A.contains to include B (only A is saved),
   then a different view rooted at B is not rerendered, even though B's
   container count (graphStats.containRels) changed. B's herald is stale
   until B's view is next rerendered. Pre-existing and arguably acceptable;
   fixing it would require selecting collateral by the neighbors of changed
   nodes, not just the changed nodes.
** (not a bug, but a simplification) Lock over-approximates the rerender set
   server/serve/handlers/save_buffer.rs, elisp/skg-request-save.el.
   The early lock message ('uris_of_views_to_lock') locks every open view
   sharing any PID with the saved buffer, but only views sharing a
   *changed* PID receive a 'collateral-view' message
   ('find_collateral_view_uris'). I checked the client: a buffer that is
   locked but never rerendered is still unlocked, because
   'skg--save-result-handler' calls 'skg--unlock-all-save-locked' on
   save-result (skg-request-save.el:171, and again in its unwind-protect at
   174). So there is no permanently-locked bug.

   It is therefore only an opportunity to simplify: narrowing the lock to
   the changed-PID set would make lock and rerender coincide, removing the
   transient lock on buffers that won't change. See the collateral
   resolution in TODO/local-view-update/problems.org.
* Performance: the save-buffer pathway (see profiling-save.org)
  Profiling the save of an insert-a-child edit (node 846207ef, 'doing')
  found four inefficiencies, in impact order. Full evidence, numbers,
  and recommendations are in the top-level 'profiling-save.org'.
  (Two of the four -- the override full-scan and the Tantivy double
  commit -- are fixed; their entries are in TODO/DONE/problems.org.)
** Override validation deep-clones the whole in-Rust graph per save
   'validate_override_invariants_after_save' (server/save.rs) builds
   its simulated post-save graph via '(*graph_snap) . clone ()' -- a
   deep clone of every node -- before applying the save's DefineNodes
   and running the (now incremental) touched-only validation. The
   validation itself is O(touched), but the clone is O(total graph)
   in time and memory, so every save still pays a whole-graph cost.
   Fix direction: simulate with a lightweight overlay (the base
   snapshot plus a small map of changed nodes consulted first)
   instead of cloning, or clone only the neighborhood the validator
   can reach. Successor to the fixed "re-scans the whole graph"
   entry, discovered while verifying that fix (2026-07-03).
** TypeDB relationship update is delete-all-then-recreate-all (~15%, O(children))
   'update_typedb_from_saveinstructions' (server/save.rs) deletes and
   recreates ALL outbound edges of each saved node every save, so
   saving an N-child node is O(N) and building one up is O(N²).
   'create_all_relationships' grew from 48 ms (~25 children) to 137 ms
   (~100). Fix: diff old vs new edges (the in-Rust snapshot has the old
   set) and apply only the delta.
** Save cost degrades 3-4x within a session of repeated saves
   404 ms -> 1220 ms over 11 consecutive saves; a 'rebuild dbs' resets
   it. Cause: each tiny Tantivy commit spawns a segment and merges fall
   behind. Mostly fixed by the single-commit change (see the Tantivy
   entry in TODO/DONE/problems.org); also consider debouncing commits
   (Tantivy is a rebuildable cache) and dropping the forced per-commit
   'reader.reload()' in production.
* Problems found while bumping docker-typedb-rust to TypeDB 3.11.5 (2026-06-08)
  Found while updating docker-typedb-rust/typedb.nix from 3.10.3 to 3.11.5
  and adding missing CLI tools to docker.nix.
** (stale-on-bump) idle-CPU note above cites "TypeDB CE 3.10.3"
   The "One TypeDB process has a fixed idle CPU tax" entry references
   3.10.3 by version. Left as-is because it records a dated observation,
   but worth re-measuring on 3.11.5 — its release notes mention query
   profiling and server-config changes that could move that baseline.
* Problems found during full-schema metaplan 2 research (2026-06-10)
** Sharing-col members from inactive sources make a buffer unsavable
   Under a restricted source-set,
   'apply_source_set_to_viewforest' ('server/source_sets.rs')
   converts EVERY Active viewnode from an inactive source into an
   InactiveNode placeholder -- including members of sharing cols
   (subscribeeCol, overriddenCol, and the read-only relation cols).
   But buffer validation ('validate_inactive_node' in
   'server/from_text/buffer_to_viewnodes/local.rs') requires an
   inactive placeholder's parent to be a gnode. So a buffer whose
   subscribeeCol contains a member from an inactive source cannot be
   saved at all: the save aborts with "Inactive placeholder must
   have a TrueNode parent."

   Worse, the failure mode if that validation were relaxed without
   further work is silent data loss: 'relation_col_members'
   ('server/from_text/local_instruction_collection/traverse.rs')
   collects only Active children, so the inactive member would be
   dropped from the 'SetSubscribesTo' / 'SetOverrides' list, which
   lowers to 'MSV::Specified' and overwrites the disk list
   ('supplement_unspecified_fields_from_disk' fills only Unspecified
   fields). The subscription/override to the invisible node would be
   silently deleted.

   Planned fix: TODO/full-schema/metaplan_2.org, section "preserve
   source-set safety for sharing relations" (omit inactive members
   from col display; merge visible-subset edits with disk's
   inactive members at save; assert no instruction touches an
   inactive source).
** Architecture note: "drawn in place of another node" is override-specific data
   Override substitution (TODO/full-schema/
   11_override-rendering-and-navigation.org, executed 2026-06-11)
   lets a viewnode stand in for a different graph node than the one
   whose ID it carries: the buffer draws overrider R where the
   container's contains list says N. The representation is
   'ViewNodeStats.overridesHere : Option<ID>' (and a parallel field
   on 'InactiveNode', for substitutes a source-set switch retains),
   carrying the original's ID -- data, not the earlier-planned
   bool-plus-convention, which was rejected 2026-06-11 because
   inverting the resolution at save time is ambiguous when one
   overrider stands for two members of the same parent. Save
   extraction, the content distinctness validators and the content
   reconciler's orderkey all read it through 'collected_id ()'.

   The name is override-specific. If a second substitution-like
   feature ever appears (redirects, merge previews, etc.), do not
   add a second marker field beside it; generalize this one (e.g.
   rename to 'drawn_in_place_of', adding a reason enum if consumers
   need distinguishing). Deferred until that second consumer exists
   (Jeff, 2026-06-10).
** (probably fixed) Flaky test: 'ids::save' group in tests/git_diff_view.rs
   'test_delete_id_scaffolds_respawns' (since renamed
   'test_delete_id_scaffolds_aborts') fails intermittently when the
   whole 'git_diff_view' target (or even just its 'ids::save' group)
   runs under plain 'cargo test', and passes in isolation and under
   'cargo nextest run' (which gives each test its own process).
   Confirmed pre-existing: it reproduces at commit 63feeef1, before
   the full-schema stage-8 work. Probably a shared-resource race
   among the tests in that target (the kind common-gotchas.md
   warns about); each uses its own db name, so the shared resource
   is something else (possibly git or tempdir state under
   run_save_test). Worth a look before trusting plain 'cargo test'
   runs of that target.
* Problems found while planning the diff-mode-policy stage (2026-06-11)
** 'skg-view-diff-mode' branches on a "\nWarning:" substring
   elisp/skg-request-git-diff-mode.el.  The git-diff-mode response
   handler pops a window ('skg-big-nonfatal-message') iff the
   response 'content' contains the literal substring "\nWarning:";
   otherwise it echoes via 'message'.  Any future first-message text
   containing that substring -- a refusal, an error -- pops a window
   unintentionally.  The diff-mode-policy stage's refusal texts avoid
   the substring; a typed flag in the response would be the real fix,
   deferred until a second message kind needs the pop.
* Problems found while executing the diff-mode-policy stage (2026-06-11)
** A stale saved buffer can show a removed read-only-col member as live
   Awareness, handled: a read-only col member whose edge vanished
   outside Emacs can still sit in a user's buffer as an Active
   child; the reconciler matches it to the goal list's phantom
   position by ID and would have kept it Active (an undecorated
   lie).  'apply_membership_axes_to_col_members' therefore stamps
   the full axes onto a net-removed Active member and flips it to a
   phantom ('normal_to_phantom').  Mentioned here because the flip
   path is reachable only through that stale-buffer gesture, so a
   test exercising it directly would need to fabricate the state.
** (probably fixed) Flaky test: whole-suite 'cargo nextest run' failed once in text::view
   'text::view::test_title_diff_staged_shows_staged_scaffolds'
   failed once during a full parallel 'cargo nextest run'
   (2026-06-11), then passed on every retry (5x in isolation, full
   suite re-run).  Same family as the pre-existing 'ids::save'
   flake above: per-test dbs, so the shared resource is something
   else.  Not chased.
** Save-time override-monogamy check depends on neighbor re-saving
   Found while writing the stage-13 nodeMerge monogamy test
   ('merge_that_collides_two_overriders_is_rejected' in
   tests/save/override_invariants.rs).
   'validate_touched_override_invariants' checks monogamy only from
   the OVERRIDER (outbound) end of each touched node: for each
   touched user-owned node T and each X that T overrides, it flags X
   if X now has >1 user-owned overrider.  It never inspects a touched
   node's INBOUND overriders directly.  A nodeMerge that fuses two
   overridden nodes (R1->N1, R2->N2; merge N2 into N1) collides R1
   and R2 on the merged node, but neither N1 nor N2 is an overrider,
   so the collision is caught ONLY because the acquiree's inbound
   neighbor R2 is re-saved as a neighbor savenode
   ('neighbor_savenodes_for_nodeMerges' ->
   'affected_neighbors_of_nodeMerge', which scans
   OUTBOUND_RELATIONSHIP_TYPES and so includes overrides_view_of
   where the acquiree plays the object).  That re-save puts R2 in
   'save_instructions', making the colliding edge "touched".
   If a future change ever narrowed neighbor re-saving (e.g. skipped
   inbound-override neighbors as "unchanged"), this collision would
   pass the save-time check silently; the whole-graph
   'validate_override_invariants' run at init/rebuild would still
   reject it on the next boot, so disk could hold a monogamy
   violation until then.  Not a present bug -- the wiring holds
   today, and the test pins it as it actually ships.  A defensive
   fix would add an inbound (target-end) monogamy check for touched
   overridden nodes.
** De-novo PartnerCol rendering depends on the global graph handle
   Found while writing the stage-13 matrix target
   (tests/partner_col_matrix.rs).  The plan
   (TODO/full-schema/13_test-rel-matrix.org) asserted the target
   "should not need the global graph handle".  Not so: rendering a
   node de novo shows its inbound read-only cols (subscriberCol,
   overriderCol, hiderCol) and even its outbound hiddenCol and
   overriddenCol ONLY when the process-global in-Rust graph handle
   ('snapshot_global') is installed.  Without it, those cols render as
   nothing -- just the owner headline.  (The subscribeeCol is the lone
   exception: it is built from the owner's own outbound subscribes_to,
   which the de-novo env graph carries, so it appears regardless.)
   This is a documentation/plan mismatch rather than a product bug --
   production always has the handle -- but it is surprising that the
   PartnerCol creation path reads the global snapshot rather than the
   env's own in-Rust graph (which 'multi_root_view_inner' builds fresh
   from disk and would suffice).  Worth either a glossary note ("col
   scaffolds read snapshot_global") or routing col creation through
   the env graph so test harnesses without the global handle still see
   cols.  The matrix target works around it by installing the handle
   in its render-heavy function only.
   PARTLY RESOLVED 2026-07-03 (fable-cleans-up): the glossary-note
   remedy is done (glossary.md, "Col scaffolds read the process-global
   graph handle").  Open only for the rerouting option (col creation
   through the env graph, so harnesses without the handle still see
   cols).
* Problems found while investigating test-suite speed (2026-06-12)
  See TODO/faster-tests.org for the full investigation.
** (mostly fixed) Six near-duplicate test-db setup paths
   The same connect / create-db / define-schema / populate / teardown
   sequence is implemented in: 'run_with_test_db' and
   'run_with_test_db_from_config' (server/test_utils.rs),
   'setup_test_dbs' (tests/git_diff_view/common.rs), 'setup_test' and
   'setup_temp_test' (tests/hidden_from_subscriptions.rs), 'setup'
   (tests/move_source.rs), 'setup_test_dbs'
   (tests/collateral_source_move.rs), and
   'run_with_source_set_test_db' (used by tests/source_sets.rs).
   They differ only in whether config comes from a TOML file and
   whether fixtures are copied to /tmp. Quasi-duplication worth
   collapsing -- and the natural moment is the shared-db-session
   refactor proposed in TODO/faster-tests.org.
   Mostly fixed 2026-06-12: hidden_from_subscriptions' and
   move_source's private helpers are deleted (their tests use
   SharedDbSession). Still alive, each with remaining callers:
   'setup_test_dbs' in git_diff_view/common.rs (the two unconverted
   single-test files), 'run_with_source_set_test_db'
   (inactive_suppression, partner_col_matrix), and
   collateral_source_move's local setup.
* Problems found while adding skg-stage-moves (2026-06-13)
** (latent, by-design-dependent) Diff helpers use a repo-wide pathspec
   'get_staged_changed_skg_files' and 'get_unstaged_changed_skg_files'
   (server/git_ops/read_repo.rs) diff the WHOLE repo with
   'pathspec("*.skg")', not the source's own subdirectory. So if two
   configured sources ever live inside one git repo (e.g. as sibling
   subdirectories of a single repo root), 'compute_diff_for_source'
   would attribute that repo's entire set of changed .skg files to
   each such source, double-counting them across sources. This is
   harmless under the apparent deployment model (one git repo per
   source) and only bites if a repo spans multiple sources.
   'find_and_stage_moves.rs' avoids the issue by scoping its HEAD
   scan to each source's own subtree ('head_skg_ids' /
   'path_relative_to_repo'); the older diff path does not. If
   multi-source repos are to be supported, the diff helpers should
   take a source-relative pathspec too.
* Problems found while planning forks (2026-06-13)
  Found while reading the sharing code to write 'TODO/forks/plan.org'.
  The first is a real, fork-independent bug; it is also the prerequisite
  the fork/clone feature depends on, and is discussed at length in that
  plan.
** The subscribee-as-such display subtracts only hides, not the subscriber's own contains
   'content_goal_list' (server/update_buffer/reconcile/content.rs:398-419)
   computes the content shown under a subscribee-as-such as
   'subscribee.contains MINUS subscriber.hides_from_its_subscriptions'.
   It does NOT subtract the subscriber's own 'contains'.

   Consequence (the move-into-graph integration path, vision.org
   example 2): 'resolve_visibility' (server/from_text/local_instruction_collection/resolve_visibility.rs:61-66)
   deliberately does NOT create a hide when the user moves subscribee
   content into the subscriber's own graph ("hiding one's own content
   would be silly"). So that content is in 'R.contains' but not in
   'R.hides', and the formula above still shows it under the
   subscribee-as-such. It therefore appears twice: once as R's
   integrated content, once as "unintegrated" subscribed content. This
   contradicts vision.org example 2 (which expects it to drop out of the
   subscribee view) and 'docs/sharing-model.md' para 2 ("an implicit
   hides_from_its_subscriptions relationship from the clone to all of
   its normal content ... any branch that shows up as ordinary
   integrated content does not need to also show up in the
   subscriptions").

   Test gap that hid it:
   'tests/hidden_from_subscriptions.rs::test_moving_foreign_subscribee_content_to_subscriber_does_not_hide'
   moves 'e1' into the subscriber and asserts only that (a) no hide is
   created and (b) 'e1' appears as content of 'r'. It never asserts 'e1'
   is removed from the subscribee-as-such branch, and its
   'contains("** (skg (node (id e1) ...")' check would even match a
   deeper '**** ' occurrence, so a duplicate passes unnoticed.

   This is also a hard blocker for forks: a fresh clone C has
   'C.contains = N.contains' and 'C.hides = {}', so the subscribee-as-
   such view of N under C would re-show every child C just integrated.

   Proposed fix (see 'TODO/forks/plan.org', "Prerequisite"): make
   'content_goal_list' for a subscribee-as-such subtract the
   subscriber's own 'contains' too --
   'subscribee.contains - subscriber.hides - subscriber.contains' --
   implementing 'sharing-model.md's "implicit hides to all normal
   content" without writing redundant data. Mirror the change anywhere
   else the subscribee-as-such membership is derived, and audit the
   HiddenIn/HiddenOutside goal lists ('goal_list.rs') so a node that is
   both integrated and explicitly hidden does not double-count.
   (If a filter I missed already excludes the subscriber's content
   downstream, this is moot -- but I could not find one.)
** 'overrides_view_of' has never been written by a save; its round-trip is untested
   Per 'TODO/DONE/sharing/status-before-plan.org', overrides were
   "dead weight": no buffer path populated 'overrides_view_of'. Since
   then the 'overriddenCol' became a writable surface (the 'SetOverrides'
   intent, server/from_text/local_instruction_collection/traverse.rs:307-323),
   but no production save actually exercises it yet -- the fork/clone
   feature would be its first real writer. The 'overridesHere'
   round-trip marker is validated VISIBILITY-UNGATED
   (server/from_text/buffer_to_viewnodes/validate_tree.rs) but RENDERED
   visibility-GATED (server/dbs/in_rust_graph/override_resolution.rs),
   so a freshly-created overrider whose source is inactive under the
   active source-set can silently drop its substitution. Needs dedicated
   create-an-override round-trip and source-set tests before the fork
   relies on it. (Cross-referenced in 'TODO/forks/plan.org' P6.)
* Problems found during the org-export feature (2026-06-13)
** export marker bodies are not valid TOML
   The instruction node 3d9aa9be-... says a marker child's body "is
   interpreted as .toml", and both it and the one real marker
   (512ea9da, body =target_filepath = org_export_is_partial=) write
   the value UNQUOTED. That is not valid TOML: unlike YAML, TOML
   requires string values to be quoted, so =toml::from_str= would
   reject these bodies.

   Resolution (agreed with Jeff, TODO/export-to-org/discussion.org
   Q3): the exporter (server/export_org.rs 'parse_target_filepath')
   does a tolerant =key = value= parse instead of strict TOML -- the
   value may be bare or quoted, and is forward-compatible with quoted
   TOML. A bare value containing whitespace is refused with an
   explanatory message; absolute paths and '.'/'..' components are
   refused so a target cannot escape org-exports/. The instruction
   node's wording ("interpreted as .toml") is now slightly
   inaccurate; if it ever matters, either quote the values in the
   data or soften the wording to "a tolerant key = value line".
** heading-anchor links rely on heading-text uniqueness
   Deep links use org's =[[./file.org::*Heading][label]]= search,
   which matches the FIRST headline with that text. When two
   DISTINCT nodes in one exported file have the same title, links to
   the second resolve to the first. The exporter emits one warning
   per such collision, but org has no per-node anchor available
   without property drawers (which we omit, since exported files
   must not require org-roam). Headings are rendered as the title's
   plain-label form (links collapsed to labels), so a heading's text
   always equals the anchor used to link to it -- the earlier
   heading-vs-anchor divergence is gone.
** a literal bracket in a title can break a deep link
   Anchors and labels are interpolated verbatim into
   =[[*anchor][label]]=. A title containing a literal =]= (e.g.
   "Notes [draft]") yields a malformed org link. The textlink regex
   keeps labels free of =][=, but a title is arbitrary user text.
   Rare; not escaped. Robust fix would require org link-syntax
   escaping of anchors/labels.
* Problems found while adding data/bash/id-histories.sh + lib.sh (2026-06-20)
** windows/starter-data/bash/ copies still use the old list-file pattern
   The Windows starter bundle ships its own copies of these scripts
   (=windows/starter-data/bash/{push,pull,status,remote}-all.sh=) that
   still =for i in $(cat list-of-repositories.txt)=. They are now
   divergent from =data/bash/= (which derives all/owned from
   skgconfig.toml via lib.sh) and still depend on a list file. Docs that
   describe the bundle (=to-windows.org=, =windows/run.ps1=,
   =windows/docs/windows-user.org=) reference the list file too. If the
   lib.sh approach is the intended one, the Windows bundle and its docs
   should be regenerated/updated to match. Left untouched this round (out
   of scope: the request was about data/bash/).
* Problems found while diagnosing a diff-mode render crash (2026-06-17)
  Trigger: in git diff mode, visiting a deciduous node aborted document
  generation with "Error generating document: find_source: no source for
  8c24623a-736f-4f07-ba5a-624c3328f5ee". Investigation in this session.
** (open) No audit catches a container whose contains points at a missing .skg file
   The ordinary save path does NOT create such dangling references:
   'apply_delete_propagation_cleanup' (server/save.rs:370-428) runs
   before the FS write and, via the in-Rust inverse indexes
   (contained_by / subscribers_of / hiders_of / overriders_of, built
   from all .skg files at init), synthesizes a verbatim Save for every
   referencer -- even ones not open in any buffer -- then strips the
   deleted id from all their lists. So a correct save rewrites the
   container on disk atomically with the deletion. BUT nothing detects a
   dangling reference once it exists: the daily audit
   (server/dbs/in_rust_graph/audit.rs) compares the in-Rust graph to
   TypeDB only and DELIBERATELY does not report dangling outbound
   references; it never compares against the filesystem.

   A scan of all sources (this session) found the state IS reachable in
   real data: at HEAD, 3 ids are referenced but exist in no source
   (01545418..., 8c24623a..., f0a77ca4...); in the worktree, 1 still is
   (01545418...). 8c24623a / f0a77ca4 are HEAD-only (already scrubbed in
   the worktree, like the case here) -- they are partial-commit
   artifacts that vanish once the scrubbed container is committed. But a
   *worktree-level* dangling ref (01545418...) means some path other than
   a clean save produced it: the cleanup is blind when the in-Rust graph
   is stale relative to disk (a referencer added by direct file editing
   without a rebuild is invisible to the inverse indexes -- save.rs:396),
   and direct edits / imports bypass it entirely. Worth: (a) a
   filesystem-aware consistency check (init/rebuild or the audit) that
   reports a contains/subscribes/overrides/hides member with no .skg file
   in any source, and (b) deciding whether such a check should auto-repair
   (drop the pointer) or only warn. Note textlinks_to body references are
   intentionally never scrubbed (save.rs:363-369) and degrade to
   PhantomUnknown when followed; a contains pointer had no such fallback
   until the fix above.
** Root cause of THIS instance: a partial commit, not a save bug
   Recorded for completeness (not a code defect). The node 8c24623a
   ("earplugs") was a leaf whose only referrer was d40db175 ("backpack").
   HEAD commit 200b688 deleted 8c24623a.skg but did not touch d40db175,
   whose committed contains still lists 8c24623a; the worktree edit that
   drops it (part of a wholesale "DONE backpack" rewrite) is uncommitted.
   So HEAD is internally inconsistent while the worktree is consistent --
   precisely the input that made diff mode (HEAD<->worktree) build a
   removed-member phantom for a node that exists nowhere. Data repair:
   commit the pending d40db175.skg (the worktree is already correct), or
   any commit that makes HEAD's d40db175 stop referencing 8c24623a.
* Problems found while planning uniform heralds (2026-06-21)
  Found while researching TODO/uniform-heralds/vision.org to write
  TODO/uniform-heralds/discussion.org. None is required by that vision;
  recorded here so they are not lost.
** The TypeDB-fallback graphStats are not source-set-aware
   Already flagged in code (the PITFALL/TODO above
   'fetch_all_graphnodestats_from_typedb',
   server/dbs/typedb/search/all_graphnodestats.rs): the TypeDB path
   counts inactive-source related nodes, unlike the in-Rust path. The
   running server always uses the in-Rust path, so this only bites
   tests/startup paths that bypass the global handle. Expanding the
   stats (the uniform-heralds work adds directional H/S/O counts) would
   widen the divergence between the two implementations; if the TypeDB
   fallback is truly only for those paths, consider whether it still
   earns its keep, or at least keep the two in lockstep.
** Only two of the owner-relative relations are computed today
   'set_relation_relative_stats' (server/update_buffer/viewnodestats.rs)
   computes 'grandparentOverrides' and 'grandparentSubscribes' but none
   of the other owner-relative relations (e.g. grandparent-contains,
   grandparent-hides), and it can only reach one gnode-hop above a col
   (via 'VisibleParent::Col'), so it cannot serve the deeper filter cols
   (HiddenInSubscribee, HiddenOutsideOfSubscribee) at all. Today's
   heralds are therefore silently incomplete for hidden-col members, not
   merely non-uniform. The uniform-heralds plan fixes this as a side
   effect (generalizing owner location through an owning-ancestor table
   in server/update_buffer/ancestry.rs).
** Minor: TypeDB path computes from_leaves by subtraction
   In 'fetch_one_pid_stats'
   (server/dbs/typedb/search/all_graphnodestats.rs),
   num_links_in_from_leaves = num_links_in_total -
   num_links_in_from_containers. This is only as correct as the two
   sub-fetches' dedup agreeing; the in-Rust path computes it directly
   and is authoritative. Not worth fixing on its own, but if the
   surprising-links a(b,c) split replaces this two-way partition, the
   subtraction goes away regardless.
* Problems noted during override-chains planning (TODO/override-chains/)
** TODO Override invariants are validated by two divergent implementations
   Monogamy (and, once
   [[./override-chains/1_plan.org][the override-chains plan]] lands,
   cycle-freedom) is checked in BOTH 'validate_override_invariants' (a
   full O(V) scan) and 'validate_touched_override_invariants' (an
   incremental O(touched) scan), in
   server/dbs/in_rust_graph/override_invariants.rs. They already hand-roll
   the same "follow user-owned overrider edges, resolving extra-ids via
   'pid_of'" logic twice; adding cycle detection duplicates it a third and
   fourth time. A divergence (e.g. one resolves extra-ids, the other
   forgets) would let bad data through the fast save path and only catch it
   at the next rebuild. Opportunity: extract one shared primitive (e.g.
   'user_owned_override_cycle_through(pid)', or have the validators reuse
   'resolve_override''s seen-set/=cycle_detected=) so the save-time and
   init/rebuild gates cannot disagree. Pre-existing (monogamy is already
   duplicated); surfaced while planning override chains.
** The CompoundOverrideChain warning is near-dead under current invariants
   'CompletionWarning::CompoundOverrideChain' (server/update_buffer/warnings.rs)
   fires when override resolution traverses a path of length > 1. But the
   current "no user-owned chains" invariant forbids exactly such data on
   disk, so today this warning can only fire on legacy / hand-edited /
   in-memory-test graphs -- effectively dead in normal operation, while its
   text already says chains are "legal". This self-inconsistency is
   resolved by the override-chains plan (which legalizes chains); noted here
   only so the warning's fate (keep as informational / narrow / drop, and a
   rename away from "legacy"/"compound") is decided deliberately rather than
   left as an accident. See
   [[./override-chains/1_plan.org][the plan]], Work item 4.
* Problems noted while diagnosing TypeDB's idle CPU (2026-07-09)
** TypeDB burns ~2000 timer wakeups/second at idle, one loop per open database
   Each open database gets a thread that polls on a 1 ms sleep
   ('hrtimer_nanosleep', ~920 wakes/s), doing nothing when there is
   nothing to sync. Two databases are open in the normal Skg setup
   ('_system' plus 'skg-real'), so an idle Skg costs ~2000 wakeups/s
   forever. This is not a CPU problem in any meaningful sense -- a
   no-op 'sleep(1ms)' loop reproduces the entire cost -- but on a
   laptop it holds the package out of deep C-states and drains
   battery. Measured and written up in
   'TODO/typedb-performance-research.org'. Suggested fixes, in order of
   payoff-to-risk: (a) run TypeDB only when Skg is in use; (b) never
   run more than one TypeDB server, since each adds a '_system' poll
   thread on top of its per-database ones -- this is the real reason
   behind 'TODO/docker-unification-strategies.org'; (c) upstream, make
   the WAL committer event-driven rather than a 1 kHz poll.

** Idle CPU percentages in this repo's notes are not comparable across readings
   'TODO/typedb-performance-research.org' recorded "4-5%" and a later
   session recorded "16%" for the same unchanged TypeDB. Both were
   correct measurements. %CPU is billed in seconds while a timer wakeup
   costs a fixed number of cycles, so on this laptop -- unplugged, with
   idle cores parked at 400 MHz ('min_perf_pct: 10') -- the identical
   loop reads ~10x higher than it does with the cores warm. Any future
   note that quotes an idle CPU percentage should also quote the CPU
   clock, or should quote wakeups/second instead
   ('voluntary_ctxt_switches' in '/proc/<tid>/status'), which is
   clock-invariant. Compounding this, the host runs a PREEMPT_RT kernel
   ('nix-jbb/config/audio-configuration.nix' sets 'kernel.realtime =
   true' for audio), which makes each timer wakeup cost several times
   more cycles than a stock kernel would.

** 'ps %CPU' keeps being mistaken for current CPU
   Noted as a pitfall once already in
   'TODO/typedb-performance-research.org', and it bit again today:
   'ps' reported 2.2% for TypeDB while 'top' reported 14.9%, because
   'ps %CPU' is a process-lifetime average. The troubleshooting scripts
   in 'docs/troubleshooting/typedb/' do not currently offer a
   correct-by-construction way to sample current CPU. A small helper
   that computes a delta from '/proc/<pid>/stat' over an interval would
   stop this recurring.
* Problems noted while writing the autofork discussion (2026-07-11)
  Found while researching TODO/user-owned_autofork_chain/2_discussion.org.
  Both are documentation defects, not code bugs.
** glossary.md's read-only-set example mislabels the subscriberCol
   In the "read-only set" entry, the example view shows R and T (the
   subscribers of N) under a =subscriberCol=, but the prose then says
   'The subscriberCol ("collection of subscribees") is a read-only
   set' -- it is a collection of SUBSCRIBERS -- and continues "the
   user could flip the order of R and T in the subscribeeCol", naming
   the wrong col. Two-word fix ("subscribers", "subscriberCol").
   Adjacent cosmetic typos in the same region: "if nodes R and T
   subscribe to nodes N" (should be "node N"), and "subsribee1" /
   "subsribee2" in the earlier "subscribee as such" diagram.
** schema.tql's 'subscribes' comment overstates: subscribing is not always forking
   The comment on the =subscribes= relation opens "A subscriber forks
   a subscribee: it clones the subscribee's content and stays
   subscribed". That describes the fork gesture, which CREATES a
   subscription, but a subscription need not come from a fork: the
   writable subscribeeCol lets a user add a =subscribes= edge with no
   clone and no override ('visit_subscribeecol' lowers membership
   edits to =SetSubscribesTo= directly). glossary.md gets it right
   ("a fork is just subscribe + override" -- forks are a subset of
   subscribers). Suggested rewording: describe subscription on its
   own (subscribee's unintegrated additions surface under the
   subscriber), then note the fork gesture creates this edge together
   with =overrides_view_of=.
