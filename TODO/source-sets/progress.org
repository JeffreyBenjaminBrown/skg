* done
- Config model, defaults, reserved =all= behavior, and validation.
- API/docs entries for source-set config, protocol, inactive placeholders,
  title lookup, file path lookup, and diff-analysis behavior.
- Example data defines a default =public= source-set and tracks empty
  =public/= and =private/= directories with =.gitkeep=.
- Per-connection active source-set state, server protocol handlers, and
  Emacs commands for list/get/set source-set.
- Source-set switching closes/unregisters active Skg views and cancels
  in-flight search enrichment.
- Content view root requests are refused when the root is outside the
  active source-set.
- Search phase-1 filtering removes inactive-source hits before display
  ranking/truncation.
- Inactive placeholder viewnode/metadata rendering exists, with generic
  title and no inactive title/body/alias/extra-id/graphStats leakage.
- Inline diff mode can render inactive placeholders with relationship
  position markers without leaking inactive node content.
- =titles by ids= omits inactive-source titles under restricted source
  sets.
- =get file path= refuses inactive-source paths under restricted source
  sets.
- Diff-analysis refuses to run unless the active source-set is =all=.
- Initial source-set contract tests cover config, content placeholders,
  search filtering, title lookup, helper-level save behavior, helper-level
  backward traversal filtering, and inline diff placeholders.
- Real buffer parser recognizes =inactiveNode= metadata as a distinct
  inactive placeholder kind.
- Real save validation rejects inactive placeholder title/body edits and
  active children beneath inactive placeholders.
- Real save extraction treats inactive placeholders as content positions:
  unchanged placeholders preserve =contains=, moves reorder =contains=,
  deletions remove inactive IDs, and duplicate content IDs are rejected.
- Inactive-placeholder source-set save coverage now uses the real
  =buffer_to_validated_saveplan= pipeline instead of a helper-only string
  scanner.
- Initial content views under restricted source-sets build inactive child
  placeholders during BFS and do not enqueue inactive subtrees for
  traversal.
- Content completion and rerender paths carry the active source-set,
  create inactive placeholders for missing inactive-source content, and
  reapply source-set filtering before rendering rerendered buffers.
- Sourceward and containerward expansion paths have source-set-aware
  variants used by rerendered view requests; inactive path steps render
  as placeholders and stop descent.

* pending
- Truncate search enrichment ancestry before inactive containers/link
  sources, filter fork branches independently, and omit empty forks.
- Add production tests for sourceward/containerward/search-enrichment
  truncation rather than only helper tests.
- Make graphStats source-set-aware where practical.
- Add precise =PITFALL | TODO= comments for any remaining graphStats or
  herald counts that include inactive nodes.
- Ensure readable-ID mode shortens inactive IDs but does not title-annotate
  inactive-source IDs or inactive placeholder metadata.
- Add readable-ID tests for inactive placeholders and inactive-source IDs.
- Add source-set switch integration coverage, including buffer closure and
  stale enrichment cancellation.
- Add end-to-end integration coverage for restricted-source search and save.
