#+title: Uniform heralds: plan and problems

* overview

This responds to [[file:vision.org][vision.org]]. It proposes a plan, then
collects the tricky bits, the architectural choices the vision leaves
open, and the things I want to push back on. A final section lists
problems I found that are out of scope.

Throughout, "herald" means one of the short colored tokens the Emacs
client paints beside an org headline. The data is computed in Rust,
serialized as =(skg ...)= metadata atoms, and rendered by a generic
Emacs lens engine driven by a rule table. The relevant code:

- rule table + serializer: [[../../server/heralds.rs][server/heralds.rs]]
- metadata serialization: [[../../server/org_to_text.rs][server/org_to_text.rs]]
- graph-level stats: [[../../server/update_buffer/graphnodestats.rs][graphnodestats.rs]] +
  [[../../server/dbs/typedb/search/all_graphnodestats.rs][all_graphnodestats.rs]]
- view-position stats: [[../../server/update_buffer/viewnodestats.rs][viewnodestats.rs]]
- relation roles + glyphs: [[../../server/dbs/in_rust_graph/relation_accessors.rs][relation_accessors.rs]] ('PARTNER_ROLE_VOCAB')
- the ancestry table: [[../../server/update_buffer/ancestry.rs][ancestry.rs]]
- Emacs lens engine: [[../../elisp/skg-sexpr/skg-lens.el][skg-lens.el]]
- Emacs display layer: [[../../elisp/heralds-minor-mode.el][heralds-minor-mode.el]]
- types: [[../../server/types/viewnode.rs][viewnode.rs]] ('GraphNodeStats', 'ViewNodeStats', 'Birth', 'ParentIs')

* Tentative plan

** The target grammar, stated precisely

The vision describes a single positional mini-language. Let me pin it
down, because several details are implied rather than stated.

*** One token per relationship

There are exactly five relationship letters:

| letter | relation     | inbound = "X it"        | outbound = "it X" |
|--------+--------------+-------------------------+-------------------|
| C      | contains     | its containers          | its contents      |
| L      | textlinks_to | nodes that link to it   | nodes it links to |
| H      | hides        | nodes that hide it      | nodes it hides    |
| S      | subscribes   | nodes that subscribe to it | its subscribees |
| O      | overrides    | nodes that override it   | nodes it overrides |

A token reads: =[inNum][inLetters] X [outNum][outLetters]=. The
letters hug the relationship letter; the numbers sit outside them.
Worked from the vision's own examples:

- =aC=    : its parent contains it (count 1 = just the parent, so the
            number is omitted)
- =3aC=   : 3 nodes contain it, among them its parent
- =3C=    : 3 nodes contain it (none of them a tracked ancestor)
- =C4a=   : it contains 4 nodes, among them its parent
- =aC5=   : its parent contains it, and it contains 5 nodes
- =Ca=    : it contains its parent (today's "}" / 'containsParent')

The ancestor letters count generations up the VIEW tree, INCLUDING col
scaffolds (settled 2026-06-21): =a= = first ancestor = the visible
parent, =b= = second = grandparent, =c= = third, =d= = fourth, and so
on. A letter says "this specific ancestor is among the members on this
side." Because a col scaffold counts as a generation but carries no
graph relation, an =a= flag appears only when the visible parent is a
real node (ordinary content); a col member's nearest flaggable ancestor
is its owner, two-or-more generations up. (Note: these literal
lowercase letters =a b c …= are ancestor flags; the =a=, =b=, =c= in
the surprising-links form =a(b,c)L= below are placeholders for digit
counts, so the two never collide in rendered output — letters vs
digits.)

*** Number-omission rule

On each side: render the numeric count, then the ancestor letters,
EXCEPT omit the number when the count equals the number of flagged
ancestors present (so =aC= not =1aC=, but =3aC= because 3 ≠ 1). If a
side has no members, that side is empty. See [[*Problems][Problems]] for the one
genuinely ambiguous case (count 1, member is not a tracked ancestor).

*** Omission of whole tokens

A relationship token is omitted iff both its sides are empty (zero
inbound, zero outbound) — even for contains, even for roots. This is
the vision's [[file:vision.org::*omission][omission]] rule. Note the consequence: an ordinary
content node always has at least its container (the parent), so its C
token is always present — but it is present as the orange /birth/
herald (below), not as a separate blue token, so the screen does not
get noisier.

*** Surprising inbound links: =a(b,c)L=

The L token's inbound side gets a refinement. With =a= inbound links:

- =b= = of those =a=, how many differ from it, are bodyless AND
  contentless, and whose normalized title is something other than the
  link label;
- =c= = of those =a=, how many have their own content;
- the remainder (=a−b−c=) are "plain" links whose source is just a
  link with one of this node's aliases as label and nothing else.

Rendered =a(b,c)L=, with the parenthetical collapsing per the vision:
omit a zero, omit the comma when c=0, keep the comma when b=0, omit the
whole parenthetical when both are zero.

*** Birth herald: orange, first

Every drawn node has a /birth relationship/ — the single relationship
to a visible ancestor that explains why it was drawn:

- ordinary content: =contains=, inbound from its parent → its C token
  (=aC=, =3aC=, =aC5=, ...);
- a backpath-grafted partner: its 'Birth::Backpath(role)' relation;
- a member of a writable/read-only partner col: the col's relation
  toward the col's owning ancestor (e.g. a subscribee-as-such →
  =bS=, its grandparent subscribes to it; an overrider-as-such →
  =Ob=, it overrides its grandparent);
- a member of a filter col ('HiddenInSubscribee' /
  'HiddenOutsideOfSubscribee'): TWO birth heralds (settled 2026-06-21)
  — the hides herald (to the subscriber owner) first, then the
  contains herald. So the birth herald is a LIST, usually of length 1.

That ONE relationship's token is moved to the front, placed right
after the ☮ peace symbol if present, and colored orange. Every other
relationship token follows it, colored blue.

So the birth relation is shown once (orange), not duplicated. The
current separate edge heralds (today =Op=, =pO=, =Sp=, =pS=, =Hp=,
=pH=, =gO=, =gS=) cease to be their own tokens; they become
ancestor-letter annotations inside the five unified tokens (=Oa=, =aO=,
=Sa=, =aS=, =Ha=, =aH=, =bO=, =bS=).

*** Action / alias heralds

- =A= (aliasing, boolean) → =Ak=, k = number of aliases.
- =O=, =S= (overriding / subscribing booleans) disappear: the counted
  O and S tokens are strictly more informative.
- =H= (hiding boolean) likewise folds into the H token.
- =I= (extraIDs boolean) → =Ik=, k = number of extra IDs, mirroring
  aliases (settled 2026-06-21).

*** Which ancestors to check (efficiency)

The expensive part is not the counts (they are single set-size reads
off the in-Rust inverse indexes) but deciding which ancestor letters
to flag. The rule:

- For every node, check its relationship to its immediate visible
  parent — one hop. This yields the =a= flag. (This is exactly what
  'set_relation_relative_stats' does today for six edges.)
- For a col member, additionally check the relationship to its /owning
  ancestor/ and every intervening vognode. The owning ancestor is the
  LAST entry of the col's 'required_ancestry' chain (fact-checked in
  [[*Problems][Problems]] — no new table needed); the intervening vognodes are that
  chain's other real-node entries. This yields the =b=/=c=/=d=/...
  flags. The depth is small and fixed per col kind.

** What stays out of scope (unchanged)

Per the vision, only the relationship heralds, the alias/action
heralds, and the surprising-links refinement change. These keep their
current behavior: 'cycle' (⟳), 'overridesHere' (=Oh=, which is
LOAD-BEARING for save extraction, see [[../../server/types/viewnode.rs][viewnode.rs]] 'collected_id'),
'sourceHerald' (⌂:label), 'parentIs' independent (⊥) / absent,
'textChanged', the staged/unstaged diff axes, 'notInGit', 'editRequest'
(delete / merge), 'viewRequests', the deleted / unknown / inactive /
deadScaffold placeholders, the read-only-col ☮ scaffold markers, and
the col labels themselves ("aliases", "subscribeeCol", ...). The node
'indef' ☮ stays but is repositioned to lead, so the birth herald can
sit right after it.

** Implementation, in order

In order (the early "step 0" items first):

0. Make empty bodies impossible (settled 2026-06-21; prerequisite for
   the surprising-links =bodyless= test). Strip leading and trailing
   whitespace-only lines from every body (an "empty" line may contain
   multiple whitespace characters); a body that is empty after
   stripping becomes 'None'. After this, "bodyless" is exactly
   'body == None'. See the open TODO under [[*Problems][Problems]] for where to apply
   this (save-time and/or import) and whether to bulk-normalize
   existing data.

1. Pin the remaining open questions in [[*Problems][Problems]] (this document): the
   surprising-links title-normalization rule and double-label case,
   the filter-col contains-ancestor, and step-0 scope. The
   ancestor-distance vocabulary (=a/b/c/...=, counting scaffolds), the
   =Ik= and blue/omit decisions, and the rest are settled.

2. Locate col owners WITHOUT a new table. Fact-checked (see [[*Problems][Problems]]):
   the owning ancestor is always the LAST entry of the col's
   'required_ancestry' chain ([[../../server/update_buffer/ancestry.rs][ancestry.rs]]), so no owner-depth table
   is needed. We need only the member→owner relation/role:
   'PartnerCol::relation_member_role' ([[../../server/to_org/complete/partner_col/kind.rs][kind.rs]]) supplies it for the
   six edge-backed cols; the two filter cols ('HiddenInSubscribee',
   'HiddenOutsideOfSubscribee') return None and get an explicit HIDER
   role (and also a contains herald toward a containing vognode). The
   intervening vognodes (for the deeper ancestor flags) are the
   chain's other real-node entries.

3. Expand the stats computation.
   - In [[../../server/dbs/typedb/search/all_graphnodestats.rs][all_graphnodestats.rs]] / 'fetch_all_graphnodestats_in_rust':
     replace the three booleans ('has_subscribes', 'has_overrides',
     'has_hides') with directional counts (inbound from the inverse
     indexes 'subscribers_of' / 'overriders_of' / 'hiders_of';
     outbound from 'NodeRust' fields). Keep the contains counts.
     Compute the surprising-links =a(b,c)= split by RE-PARSING each
     source's title+body on demand ('textlinks_from_node'; option A,
     settled 2026-06-21 — no data-model change). "Bodyless" means
     'body == None' (made meaningful by step 0).
   - In [[../../server/update_buffer/viewnodestats.rs][viewnodestats.rs]]: generalize the per-relation ancestor
     flags. Replace the ad-hoc 'grandparentOverrides' /
     'grandparentSubscribes' (the only two owner-relative stats today,
     reachable only one gnode-hop up via 'VisibleParent::Col') with a
     pass that locates the owning ancestor (the LAST 'required_ancestry'
     entry) and the intervening vognodes, and records, per relation and
     direction, which tracked ancestors are members.
   - Designate the birth relationship(s) per node — a LIST, usually of
     length 1 (contains for ordinary content via 'parentIsContainer';
     the 'Birth::Backpath' role for grafted partners; the col relation
     for col members), but length 2 for a filter-col member: hides (to
     the subscriber owner) first, then contains (to the containing
     vognode ancestor).

4. Token assembly + serialization. Add a Rust function (a small new
   module, or within [[../../server/org_to_text.rs][org_to_text.rs]]) that turns counts + ancestor
   flags into the compact token string per relationship, applying the
   number-omission and whole-token-omission rules and the =a(b,c)L=
   form. Emit the birth list (one or more orange tokens — a filter-col
   member emits two) under a distinct atom (so the rule table can place
   it first and color it orange), and each remaining nonempty token
   under a per-relation atom (blue). Emit =Ak= and =Ik=. Remove the
   now-folded atoms ('containers', 'contents', 'linksInFrom*',
   'containsParent', 'overriding', 'subscribing', 'hiding', 'aliasing',
   'extraIDs', and the edge stats).

5. Rule table + conformance + fixture. In [[../../server/heralds.rs][heralds.rs]]: add rules for
   the new atoms (birth orange, ABUT so it hugs the ☮; per-relation
   blue), reorder the 'node' rule so 'indef' (☮) leads and the birth
   herald follows, and update the compile-time guards in
   'emittable_metadata_atoms'. Regenerate the elisp fixture with
   =cargo run --bin emit-herald-rules > tests/elisp/herald-rules.sexp=.
   Per the Emacs research, NO elisp display/ordering/color logic needs
   to change — order and color are 100% server-driven.

6. Emacs touch-ups (small). Update 'skg-strip-heralds-from-sexp'
   ([[../../elisp/skg-sexpr/skg-sexpr-search.el][skg-sexpr-search.el]]) to strip the new herald atoms before save
   (it currently strips 'containsHerald' / 'linksHerald' /
   'sourceHerald' / viewStats). Update the elisp display tests
   ([[../../tests/elisp/test-heralds-minor-mode.el][test-heralds-minor-mode.el]] etc.) to the new tokens.

7. Update the golden tests in layers (parser/serializer, then Rust
   view-render goldens, then elisp, then integration). This is the
   bulk of the mechanical work: roughly 400+ =(skg ...)= golden lines
   across ~17 Rust test files, heaviest in [[../../tests/hidden_from_subscriptions.rs][hidden_from_subscriptions.rs]]
   (92), [[../../tests/definitive_view_cascade_and_budget.rs][definitive_view_cascade_and_budget.rs]] (94), [[../../tests/initial_view_bfs.rs][initial_view_bfs.rs]]
   (53), [[../../tests/content_view.rs][content_view.rs]] (29), plus the [[../../tests/git_diff_view][git_diff_view]] subtree. The
   conformance test in [[../../tests/unit/heralds.rs][heralds.rs (unit)]] is the gate that keeps the
   table and the emitted vocabulary in sync.

** Current → new mapping (reference)

| current atom / glyph                          | new                                  |
|-----------------------------------------------+--------------------------------------|
| graphStats 'containers' N / 'contents' M ({)  | C token counts                       |
| viewStats 'containsParent' (})                | =Ca= inside C token                  |
| birth backpath 'container' (})                | C token, orange (birth)              |
| graphStats 'linksInFromContainers'/'..Leaves' (→) | L token inbound, now =a(b,c)=    |
| birth backpath 'linkSource'/'linkDest' (←/→)  | L token, orange (birth)              |
| graphStats 'overriding' (O bool)              | O token counts                       |
| viewStats 'overridesParent'/'parentOverrides' (Op/pO) | =Oa=/=aO= inside O token      |
| viewStats 'grandparentOverrides' (gO)         | =bO= inside O token                  |
| graphStats 'subscribing' (S bool)             | S token counts                       |
| viewStats 'subscribesParent'/'parentSubscribes' (Sp/pS) | =Sa=/=aS= inside S token    |
| viewStats 'grandparentSubscribes' (gS)        | =bS= inside S token                  |
| graphStats 'hiding' (H bool)                  | H token counts                       |
| viewStats 'hidesParent'/'parentHides' (Hp/pH) | =Ha=/=aH= inside H token            |
| graphStats 'aliasing' (A bool)                | =Ak= (count)                         |
| graphStats 'extraIDs' (I bool)                | =Ik= (count)                         |

The existing 'PARTNER_ROLE_VOCAB' glyphs already encode direction
(=Op=/=pO= etc.), and the birth herald is /already/ rendered orange
('birth_rule_children' emits orange glyphs) — so the relabel to the
=a/b/c= scheme (=Op= → =Oa=, =pO= → =aO=, =gO= → =bO=, ...) and the
recolor are small. The substantive work is: (a) add the missing
directional counts for H/S/O, (b) refine L into =a(b,c)L=, (c)
generalize the owner-relative flags beyond gO/gS using
'required_ancestry', and (d) reorder so the orange birth list leads.

* Problems

** DONE Ancestor-letter distance: the hiddeninsubscribee example is off by one

The herald letters =p/g/b/t= must measure SOME distance. The existing
code already uses =gO=/=gS= for a subscribee-as-such whose owner is the
/grandparent/ — i.e. the letter counts TREE ancestors (member → col
scaffold → owner = 2 levels = "g"), which also matches what a user sees
(two headline levels up). Good.

But the vision says a hiddeninsubscribeecol member's owning ancestor is
its "great-grandparent". The actual view tree is

: subscriber → SubscribeeCol → subscribee → HiddenInSubscribeeCol → member

so the subscriber (the owner) is FOUR headline levels up =
great-/great/-grandparent = =t=, not =b=. The code reconcilers read it
at col-index 2 (member-index 3), confirming four hops.

Question for you: are the letters tree distance (matching =gS=/=gO=
and the visible headline count), making the hiddeninsubscribee owner
=t=? If so the vision's "great-grandparent" is just a slip and the
plan should use =t=. (If instead you intend the letters to count only
gnode ancestors, skipping col scaffolds, then a subscribee-as-such's
owner would be =p=, which contradicts the existing =gS=/=gO= — so I
don't think that's what you mean. Please confirm.)

*** DONE You're right, I miscounted.
And yes, they should count all viewnodes, including scaffolding.

Also, I've reconsidered the generation-distance vocabular.
Let's just use 'a' for 'first ancestor = parent',
b = grandparent, c = great-grandparent, etc.

** DONE extraIDs (=I=): drop it, or show =Ik=?

The vision lumps "I, O and S" together as removable. O and S clearly go
because the counted O/S tokens replace them. But =I= (extraIDs) is not
a graph relationship and has no token to fold into. Two readable
options: drop the bare boolean entirely (the idCol surfaces the extra
IDs on demand), or mirror aliases and show =Ik= (k = number of extra
IDs). I lean toward =Ik= for symmetry with =Ak=, but it is your call.

*** DONE Use Ik.

** DONE Non-birth relationship heralds: uniformly blue loses the red "alarm"

The vision says non-birth relationship heralds are blue. Today the
parent-relative edges (=Op=, =pO=, =Sp=, =pS=, =Hp=, =pH=) render RED —
deliberately, because a node overriding/subscribing-to/hiding its own
parent is surprising and worth flagging. Folding them into uniformly
blue tokens removes that signal. Are you happy to lose the red alarm
for these surprising same-relation-with-an-ancestor cases, or should a
token that carries a =p=/=g=/=b=/=t= flag on a "surprising" relation
(H/S/O, not C) stay red while pure-count tokens go blue? I can do
either; the vision as written says blue.

*** DONE let's lose it

** DONE Outbound link count (=L5=): show it or omit it?

The grammar admits =L5= (it links to 5 nodes), but the vision only ever
discusses inbound links (=a(b,c)L=). Outbound links are visible in the
node's own body text, so an outbound count may be redundant noise. I
propose to omit the outbound side of the L token by default (show only
=a(b,c)L=). Confirm, or say you want =L5= too.

*** DONE I agree, omit those.

** ONGOING Surprising-links =b=: how do we recover each link's label?

Computing =a= and =c= is cheap and already essentially done
('sources_with_content'). Computing =b= needs, per inbound edge, the
LABEL the source used in =[[id:thisnode][label]]= — and that label is
discarded everywhere except in the source node's raw title/body text.
'textlinks_in' is a 'HashSet<ID>' of source pids only (no labels, no
multiplicity); 'NodeRust.textlinks_to' drops the label at construction.
Two options:

- (A) Re-parse each source's title+body on demand (the existing
  'textlinks_from_node' regex), in memory, no disk/TypeDB. Cheap per
  node; pays a regex pass per inbound source.
- (B) Add the label to the 'textlinks_in' index. More invasive (touches
  add/remove/migrate of the inverse indexes) but O(1) per edge, and it
  also fixes the current multiplicity loss (a source that links twice
  collapses to one pid today).

I lean toward (A) first (no data-model churn), unless you already want
(B) for other reasons. Which do you prefer?

Sub-questions for =b=:

- "Normalized title" has no canonical helper in the code. The closest
  existing practice ('replace_each_link_with_its_label' then trim +
  lowercase, as used for Tantivy/search) is my proposed definition.
  OK?
- "Bodyless": does that mean 'body == None', or also a whitespace-only
  body? ('NodeRust.body' is 'Option<String>'.)
- If one source links to this node twice with two different labels,
  which bucket does it land in? (Today it is invisible — the HashSet
  collapses it.)

*** DONE let's go with (A). Bodyless <=> body = None.
Also, as a step 0
(there might be many such steps 0; I just mean something to do early)
we should make empty bodies impossible.
Strip all leading and trailing empty lines from every body,
where empty means entirely whitespace
(so an empty line can be multiple characters).
*** DONE (me) Confirm the title-normalization rule for =b='s "title != label"
My proposed definition: run 'replace_each_link_with_its_label' on the
source's title, then trim and lowercase (matching the existing
Tantivy/search practice), and compare to the link label normalized the
same way. OK, or do you want a different rule? Sub-case: if one source
links to this node TWICE with two different labels (today invisible —
'textlinks_in' is a 'HashSet', but re-parse (A) sees both), does the
source count as "surprising" if ANY of its labels is surprising, or
only if ALL are? Absent guidance I'll default to "surprising iff every
label it uses is surprising".
**** TODO that's perfect
*** ONGOING (me) Where to apply step-0 body normalization, and existing data?
Strip at save-time (the save pipeline normalizes each body before
write), at import, or both? And do we bulk-normalize the existing
on-disk bodies once (a rebuild/migration), or only normalize each node
on its next save? The =bodyless= test is only fully reliable once
existing data is normalized — so I lean toward a one-time pass plus
save-time enforcement. Confirm.
**** TODO yes and
Strip at save-time and at import.
Add a script at data/bash/trim-node-bodies.org
that trims every .skg file found
(prompting the user for which folder to search recursively for .skg files).
Where the result would be an empty string, remove the body field entirely.
*** ONGOING (me) How do ancestor flags compose with =a(b,c)L= on the inbound side?
When an inbound link source is also a tracked ancestor — e.g. a
backpath linkSource graft, where the L token IS the birth herald — the
inbound side carries both an ancestor letter and the =(b,c)= split. I
propose the ancestor letter prefixes the count, e.g. =b3(1,1)L=. This
is rare; say if you'd rather suppress the letter there.
**** TODO let's suppress
** ONGOING Birth heralds for the two filter cols

'HiddenInSubscribee' and 'HiddenOutsideOfSubscribee' members have no
single relation role ('relation_member_role' returns None); their
membership is /derived/ from the subscriber's hide state. Their birth
relationship is "the subscriber hides this" — an H token with the owner
several hops up (=tH= / =bH=, modulo the distance question above). I
think this is consistent and fine, but it is the one place the vision's
clean "five relationships" mapping needs a derived (not edge-backed)
fact. Confirm you want these rendered as H tokens against the
subscriber ancestor.

*** TODO good catch
For them, let's frontload (in orange)
*both* the hides herald and the contains herald,
with hides first.
This might mean needing to use a list instead of a singleton somewhere;
that seems fine.
*** ONGOING (me) What is the contains ancestor for a HiddenOutsideOfSubscribee member?
Folded into the plan (birth herald is now a list). One detail to
settle: for a 'HiddenInSubscribee' member the contains herald points at
the subscribee that contains it (its =b= ancestor) — clean. But a
'HiddenOutsideOfSubscribee' member is, by definition, NOT contained by
anything the subscriber subscribes to; its 'required_ancestry' chain
=[Col(Subscribee), NormalVognode]= has no containing vognode except the
subscriber (the hider) itself. So its orange contains herald has no
in-view container to flag. Options: (i) show the node's graph container
count with no ancestor letter, or (ii) omit the contains herald for
these, leaving only the orange hides herald. I lean toward (ii) — omit
when there is no in-view container. Confirm.
**** TODO oops
Good point. For a hiddenOut we should only frontload 'hides'.
There is no 'contains' ancestor.
** (likely solved): The number-omission edge case (count 1, no flagged ancestor)

When a side has count 1 and that single member is NOT a tracked
ancestor, my rule renders =1C= (bare number). The vision never shows
this shape because in practice the lone container of a content node IS
its parent (=pC=). The only way to hit =1C= is viewing a node where its
single container is off-screen / not an ancestor (e.g. a backpath). I
think =1C= is the correct, unambiguous rendering there; flagging it
only so you can veto it.

*** DONE good

** (likely solved): Token strings are assembled in Rust, not in the rule table

The compact grammar (conditional numbers + variable ancestor letters on
both sides + the =a(b,c)= form) is more positional than the Emacs lens
engine's INTERC directive can express. Rather than extend the engine, I
plan to assemble each token string in Rust and emit it as a value the
rule table echoes (via ANY/IT), with the table still owning color
(orange/blue) and order (birth first). This keeps Emacs a dumb renderer
and is consistent with [[../../server/heralds.rs][heralds.rs]] already being the Rust-side
presentation authority. The cost: the grammar lives in Rust code, not
in the declarative table, so the table no longer self-documents the
token shape. I think that is the right trade; tell me if you'd rather
push the grammar into the lens engine.

*** DONE agreed

** (likely solved): The "owning ancestor" table the vision references does not exist yet

The vision says "a table somewhere in the rust code defines them all."
It does not — the only "owning ancestor" mentions in the repo are in
vision.org. What exists is 'required_ancestry' in [[../../server/update_buffer/ancestry.rs][ancestry.rs]] (the
per-col ancestor-/kind/ chain) plus 'relation_member_role'; the owner
DEPTH is currently encoded ad hoc in each reconcile call site
(index 0/1/2). Fact-checked (below): the owner is always the LAST entry
of 'required_ancestry', so NO new owner-depth table is needed — step 2
just reads the last entry, and the member→owner ROLE still comes from
'relation_member_role' (plus an explicit HIDER for the two filter
cols).

*** ONGOING the owning table is always (I think) the last of the required ancestors
That is, the farthest ancestor.
Please fact check me, but if I'm right,
that means we don't need another table.
**** TODO (me) Confirmed — the owner is always the last required ancestor
I checked all three chains in 'required_ancestry':
- ANC_NORMAL =[NormalVognode]= — owner = that vognode (the 6 simple
  partner cols + the 2 QualCols);
- ANC_HIDDEN_OUTSIDE =[Col(Subscribee), NormalVognode]= — owner = the
  trailing subscriber;
- ANC_HIDDEN_IN =[NormalVognode, Col(Subscribee), NormalVognode]= —
  owner = the trailing subscriber.
In every case the owning gnode is the LAST entry, so no owner-depth
table is needed. The one caveat: 'required_ancestry' records ancestor
KINDS, not relations, so the member→owner RELATION still comes from
'relation_member_role' (and the explicit HIDER we assign the two filter
cols). The intervening vognode for the contains herald you asked for
(HiddenInSubscribee's subscribee) is the chain's first NormalVognode
entry, so it too falls out of the same chain — no extra bookkeeping.

** (likely solved): Display order and color are entirely server-side

I confirmed the Emacs side does no sorting, grouping, filtering, or
recoloring — token order is exactly the rule-child order in
[[../../server/heralds.rs][heralds.rs]], and all five colors (incl. orange) are already wired to
faces. So "move the birth herald to front, after ☮, orange" is a pure
[[../../server/heralds.rs][heralds.rs]] change. The only required Emacs edits are the
strip-before-save function and the display tests (step 6).

** Pushback: the vision under-specifies, but is not over-complex

I do not think any part of the vision is unnecessary or gratuitously
complex — the unification genuinely removes a mishmash (=}=, ={=, =→=,
=←=, plus eight separate edge heralds) and replaces it with ≤5 learnable
tokens. The real risk is the opposite: the compact grammar is dense.
=3pC4p= is precise but cryptic until learned. That is a deliberate
density trade you are clearly choosing; I just want it on record that
the win is "fewer tokens, more to learn per token." The blast radius is
also large (≈400 golden test strings), so this is a sizable change even
though the runtime logic is contained.

*** DONE agreed
