#+TITLE: Progress: the Neovim client
# Running log kept during execution of ./plan.org, per
# ../../coding-advice/when-to-stop.org. Categories per that document.
# Newest entries go at the top of each section.

* what's done and went well
- 2026-07-02: The pure logic tier is ported with mirrored tests, all
  green: sexpr/parse (the read/prin1 stand-in; round-trips the pinned
  herald-rules fixture and prints byte-identically to Emacs prin1 on
  wire shapes), sexpr/compare, sexpr/edit_dsl, sexpr/lens (tokens are
  chunk lists, the shape nvim virtual text wants), sexpr/org_bijection,
  sexpr/activenode_defaults, config (TOML scan). ~140 specs.
- 2026-07-02: Scaffolding: nvim/ plugin layout, log, reload,
  plenary.busted harness, bash/nvim-tests.sh.
- 2026-07-02: Plan settled (all nine questions answered; decisions
  folded into plan.org's "Settled decisions"). Execution begun on the
  'nvim' branch.

* Changes to the plan's envisioned order
(none yet)

* what's imperfect about what's done
(none yet)

* choices determining the future
- Test-parity depth is FULL (Q6 option A), not the recommended B: every
  applicable tests/elisp file gets a busted mirror and every
  tests/integration dir gets a test-nvim.lua. bash/all-tests.sh must
  echo which client suite it is running, so slow runs are attributable.
