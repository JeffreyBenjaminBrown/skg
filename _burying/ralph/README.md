# This might not be needed

Claude Code now has 'plan mode', where it builds a plan at
'.claude/plans/' (with a nonsensicaly but almost-certainly-unique name)
and then executes it.

# What this is

"Ralph" is a technique for repeatedly hammering at a task with Claude using the same spec, described (among other places) here:

https://www.aihero.dev/getting-started-with-ralph
