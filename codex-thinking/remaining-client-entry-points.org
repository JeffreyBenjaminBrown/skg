* Client entry-point audit and migration
** Command guards completed
'nvim/lua/skg/misc_requests.lua:rebuild_dbs', 'nvim/lua/skg/pull.lua:pull_all',
'elisp/skg-request-rebuild-dbs.el:skg-rebuild-dbs' and
'elisp/skg-pull.el:skg-pull-all' now permit a retained report when current graph
admission is explicitly open. Commit '30476c1b' records the tested change.
** Origin callbacks completed
Both pull adapters bind failure handlers, response handlers, origin replay and
process/job continuations to the initiating incident. An older owned child can
finish without changing the foreground record. Process/job identity checks remain;
unknown records cannot borrow another incident's context. The Emacs migration is
in '37d56ee3'; the Neovim continuation checkpoint is recorded in
[[file:../TODO/partial-reload-and-magit/progress.org][progress.org]].
** Constructor consumers completed
'nvim/lua/skg/client.lua' and 'elisp/skg-client.el' preserve the current constructor
barrier across connection reset. They no longer close it merely because a report
record remains. Bootstrap and current graph publications own transitions. Commit
'c5232ce8' records tests for open, closing and closed states.
** Verification boundary
These exact consumers have focused coverage and the preceding guard/constructor
checkpoint passed both full editor suites. Final process/editor lifecycle coverage
still remains; this bounded audit is not a claim that plan 11 is complete.
