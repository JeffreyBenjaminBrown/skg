// Binary grouping (TODO/faster-tests.org): partial-reload tests. See
// tests/grouped_unit.rs for why test files are grouped into a few
// [[test]] targets. Included from grouped_saves.rs.

#[path = "reload/title_change.rs"]
mod title_change;
