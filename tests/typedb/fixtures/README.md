The files 1.skg through 5.skg form a tree.

The files a.skg through c.skg are cyclic,
with the following structure:
  a > b > c > b
  That is, 'a' is uncontained,
  but 'b' and 'c' contain each other.
